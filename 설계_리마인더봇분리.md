# 룩플봇 2분리 — 최종 구현 스펙 (추출봇 + 리마인더봇)

> 작성 기준: `linkbot/bot.py`(653줄), `linkbot/state.py`, `linkbot/config.py` 전수 확인 + 4종 적대검증 반영.
> 이 문서 하나로 **바로 구현 가능**하도록 함수별 코드 수준까지 명시. 적대검증에서 나온 **치명 3종**(자동 헛추적 / 답장 과잉해소 / dangling NameError)을 코드에 직접 반영함.

---

## 0. 적대검증 핵심 반영 사항 (먼저 읽기)

설계 초안 대비 **반드시 코드에 들어가야 하는** 수정 4가지. 이게 빠지면 분리 직후 오작동.

| # | 문제 | 결과 | 이 스펙의 처리 |
|---|---|---|---|
| C1 | 추출봇이 단 🔴🟡🔵 reaction이 리마인더봇 `on_raw_reaction_add`를 트리거 | 사람 클릭 없이 **모든 일정 자동 추적**(재촉 폭탄) + 우선도 선택 UX 소멸 | `on_raw_reaction_add` 맨 앞에서 `payload.member.bot` + `EXTRACTOR_BOT_USER_ID` 차단 |
| C2 | 본문 못 읽는 리마인더봇이 **모든 답장**을 완료로 간주 | 질문/잡담 답장에 추적 조기 해소 → 미완료 작업 영구 누락 | 답장은 **완전 해소가 아니라 타이머 리셋**으로 격하. 완전 해소는 ✅ 전용 |
| C3 | 추출봇에서 `tracked` 제거 시 `_check_done`/`on_message` dangling | `NameError`로 `on_message` 마비 = 추출 반쪽 죽음 | `tracked` 모든 참조 동시 제거(grep 0건) + `re` import는 **유지**(line 555가 씀) |
| C4 | 이모지 문자열 비교(variation selector) 불일치 | 정식 플래그가 "추출봇 플래그 아님"으로 조용히 무시 → 전면 미동작 | `_norm_emoji`로 NFC 정규화 + `️` 제거 후 비교 |

추가 반영:
- 추출봇 `_clear_schedules`의 `clear_reaction`(전체삭제) → `remove_reaction(e, client.user)`(자기 것만)로 변경 → **추출봇 Manage Messages 불필요** + 추적중 메시지의 사람 reaction 보존(유령추적 완화).
- `EXTRACTOR_BOT_USER_ID = 1519559729687429131` (현 토큰 prefix base64 디코드값과 일치 확인. **토큰 재발급해도 User ID는 불변** → 그대로 사용).

---

## 1. 확정 아키텍처

```
┌─────────────────────────────┐         ┌──────────────────────────────┐
│  추출봇 (linkbot/bot.py)     │         │  리마인더봇 (reminder/...)    │
│  = 현 봇에서 추적 도메인 제거 │         │  = 신규 단일 파일             │
│                              │         │                              │
│  호스트: PC, 작업시간만 간헐  │         │  호스트: always-on            │
│  토큰A                       │         │  (라즈베리파이/폰/무료클라우드)│
│  message_content: ON         │         │  토큰B                       │
│  Ollama: 사용                │         │  message_content: OFF(프라이버시)│
│  state.db (flagged/kv/sched) │         │  Ollama: 없음                │
│                              │         │  state_reminder.db (tracked) │
│  담당:                       │         │  담당:                       │
│  - on_message 추출 → 시트     │         │  - 우선도 이모지 클릭→추적시작 │
│  - 🔴🟡🔵 부착(flagged 마커)  │  ──협력──▶ - reminder_tick 재촉        │
│  - 답장/edit → 시트 진행갱신  │  Discord │  - ✅ → 해소 / 답장 → 타이머리셋│
│  - resolve_loop(확인필요)     │  이벤트만 │  - 추출봇 reaction으로 플래그 │
│  - catchup/과거등록           │  (공유DB X)│   식별(봇 꺼져도 reaction 잔존)│
└─────────────────────────────┘         └──────────────────────────────┘
```

**협력 원리(공유 DB 없음):**
1. 추출봇이 일정 메시지에 🔴🟡🔵 reaction 부착 → Discord 서버에 영속(추출봇 꺼져도 남음).
2. 사람이 우선도 이모지 클릭.
3. 리마인더봇(always-on)이 클릭 수신 → `fetch_message`로 **추출봇이 같은(또는 PRIORITY 중 하나) 이모지를 달았는지** 확인 → 정식 플래그면 추적 시작.
4. 재촉 → ✅ 또는 답장으로 해소/리셋. **리마인더봇은 본문을 끝까지 안 읽음**(reference 메타데이터만).

**완료 시트 갱신 책임:** 답장(reply) 경로(`_apply_reply_update`)가 전담 → **추출봇 유지**. standalone "완료" 키워드 시트 갱신은 드롭(아래 §2, 미결정 §10-1).

---

## 2. 추출봇(`linkbot/bot.py`) — 정확한 제거/수정

> 원칙: **추적(reminder) 도메인만 제거.** `resolve_loop`/`_reextract_*`/확인필요/`_apply_reply_update`/`_reconcile`/`catchup`/`과거등록`은 추적과 무관 → **전부 유지**.

### 2-1. 전역 상태 (line 47-48)
```python
# 변경 전
flagged = set()
tracked = {}
# 변경 후 (tracked 줄 삭제)
flagged = set()
```

### 2-2. `on_ready` (line 75, 80, 82-84, 92)
- line 75: `global tracked, flagged` → `global flagged`
- line 80: `tracked = state.load_tracked()` **삭제**
- line 82: 로그 문구에서 tracked 제거
  ```python
  log.info(f"login: {client.user} | servers {len(client.guilds)} | 플래그 {len(flagged)}")
  ```
- line 83-84: `reminder_tick` 시작 블록 **삭제** (⚠️ **`resolve_loop` 시작 블록 85-86은 유지** — 혼동 주의)
  ```python
  # 삭제할 것:
  if not reminder_tick.is_running():
      reminder_tick.start()
  ```
- line 92: 시작 알림 문구에서 "추적 N건 복원" 제거
  ```python
  await alert(f"✅ 룩플봇(추출) 시작 (서버 {len(client.guilds)}) — 놓친 메시지 {n}건 따라잡음")
  ```

### 2-3. 이모지 부착 게이트 (line 165) — **C3 핵심**
```python
# 변경 전
if live and res.get("is_task") and msg.id not in flagged and msg.id not in tracked:
# 변경 후 (and msg.id not in tracked 삭제)
if live and res.get("is_task") and msg.id not in flagged:
```
> `flagged`만으로 "이미 1세트 부착함" 중복방지 그대로 보장.

### 2-4. `_clear_schedules` (line 207-223) — tracked 분기 제거 + clear_reaction 교체
```python
async def _clear_schedules(msg, untrack=False):
    """이 메시지의 모든 일정 행 삭제 + 건수 0 + (옵션) 플래그/이모지 정리."""
    prev = state.get_msg_item_count(msg.id)
    if prev > 0:
        keys = [f"{msg.id}#{i}" for i in range(prev)]
        await asyncio.to_thread(sheet.delete_rows, "일정", keys)
    state.set_msg_item_count(msg.id, 0)
    if untrack or msg.id in flagged:                 # tracked 항 제거
        flagged.discard(msg.id)
        state.del_flagged(msg.id)
        try:
            for e in config.PRIORITY:
                await msg.remove_reaction(e, client.user)   # clear_reaction→remove_reaction(self): 자기 것만, Manage 권한 불필요
        except Exception:
            pass
```
변경점: `tracked.pop`/`state.del_tracked` 2줄 삭제, 조건문에서 `msg.id in tracked` 삭제, `clear_reaction(e)` → `remove_reaction(e, client.user)`.
> `remove_reaction(self)`는 추출봇 자기 reaction만 제거 → 사람이 누른 reaction 보존(리마인더봇 추적 신호 유지). 권한도 최소화.

### 2-5. `on_message` (line 234-248) — 추적 해소 + _check_done 제거
```python
@client.event
async def on_message(msg):
    if msg.author.bot or msg.guild is None:
        return
    ref = msg.reference.message_id if msg.reference else None
    # 답장으로 일정 진행 갱신 → (소비여부, 완료여부)
    consumed = False
    if ref:
        consumed, _ = await _apply_reply_update(msg, ref)   # done 반환값 미사용 → _
    # (삭제) ref in tracked and done → resolve  ← 추적 해소는 리마인더봇 담당
    # (삭제) _check_done 호출  ← standalone "완료" 처리 드롭(§10-1)
    if not consumed:
        await process(msg, live=True)
    state.set_kv("last_seen", time.time())
```
삭제 라인: 235(`done = False`), 240-241(`if ref and ref in tracked and done: resolve`), 244-245(`if not consumed and await _check_done(msg)`).
> 완료 시 시트 갱신은 `_apply_reply_update`가 그대로 수행(line 273-290 유지). **답장 경로가 완료 시트 갱신의 주 경로.**

### 2-6. `_check_done` 정의 (line 308-327) — **완전 삭제**
- `tracked`를 데이터 소스로 쓰므로 제거 필수. ⚠️ **`re` import(line 6)는 삭제 금지** — line 555 `_is_past`(과거등록)가 사용. `brain.is_done`은 추출봇 내 다른 호출처 없음(무해).

### 2-7. `on_raw_reaction_add` (line 357-391) — **완전 삭제**
- 추적 시작 + ✅ 해소 → 리마인더봇으로 이동. 함수 전체 삭제.

### 2-8. `resolve` 정의 (line 394-403) — **완전 삭제**
- 호출처 3곳(241/323/366)이 모두 위에서 제거됨 → dangling 0.

### 2-9. `reminder_tick` (line 406-423) — **완전 삭제**
- `@tasks.loop(seconds=60)` 데코레이터까지 통째로. ⚠️ line 409의 `state.set_kv("last_seen", now)`는 `reminder_tick`이 켜져 있을 때만 갱신했음 — **last_seen 갱신은 `on_message`(line 250)·`on_raw_message_edit`(354)·`catchup`(117)이 이미 함** → 손실 없음. 단 "메시지가 한동안 없을 때 last_seen이 안 밀린다"는 차이는 있음(catchup 윈도가 길어질 뿐, 추출 정확성엔 무해. §9 테스트로 확인).

### 2-10. 슬래시 명령 (line 528-550)
- `추적목록`(528-534): **삭제**.
- `추적해제`(537-544): **삭제**.
- `상태`(547-550): tracked 문구만 제거.
  ```python
  @tree.command(name="상태", description="봇 상태")
  async def status_cmd(inter: discord.Interaction):
      await inter.response.send_message(
          f"🟢 추출봇 가동 중 | 서버 {len(client.guilds)} | 플래그 {len(flagged)}건", ephemeral=True)
  ```

### 2-11. `state.py` (선택) — tracked 잔재 정리
- `load_tracked`/`save_tracked`/`del_tracked` + `tracked` 테이블 생성(10-11) + `task` ALTER(14-17): 호출처 0 → **삭제 권장**(혼동 방지). 남겨도 무해하나, **`bot.py:80`의 `state.load_tracked()` 호출은 반드시 제거**(전역 tracked 부활 방지).
- 유지: `flagged` 테이블, `load/add/del_flagged`, `kv`, `get/set/del_msg_item_count`.

### 2-12. 추출봇 dangling 최종 grep 체크 (구현 후 필수)
```bash
# 아래 4개가 bot.py에서 0건이어야 함 (정의·참조 모두)
grep -nE '\btracked\b|reminder_tick|\bresolve\b|on_raw_reaction_add|_check_done' linkbot/bot.py
# resolve_loop / _resolve_busy / _review_reply 는 남아야 함(추적 무관) — 위 패턴에 안 걸림 확인
```

---

## 3. `reminder_bot.py` — 전체 설계 (함수별 코드)

> 파일: `D:\Study\DIscordBot\AutoLinkBot\reminder\reminder_bot.py`
> always-on 호스트에 `reminder/` 폴더만 복사 배포. 공유코드/공유DB 없음.

### 3-1. 헤더 / 인텐트 / 락
```python
# reminder/reminder_bot.py — 룩플 리마인더봇 (always-on / 본문 안 읽음)
#   추출봇이 메시지에 붙인 우선도 이모지(🔴🟡🔵)를 사람이 클릭 → 추적 시작
#   간격 지나면 재촉 / ✅ → 해소 / 답장 → 타이머 리셋. 본문은 절대 읽지 않음(reference 메타만).
import os, sys, time, socket, logging, unicodedata
import discord
from discord import app_commands
from discord.ext import tasks

HERE = os.path.dirname(os.path.abspath(__file__))
sys.path.insert(0, HERE)
import config_reminder as config
import state_reminder as state

logging.basicConfig(
    level=logging.INFO,
    format="%(asctime)s [%(levelname)s] %(message)s", datefmt="%H:%M:%S",
    handlers=[logging.FileHandler(os.path.join(HERE, "reminder.log"), encoding="utf-8", delay=True),
              logging.StreamHandler()])
log = logging.getLogger("reminder")

# 단일 인스턴스 락 (추출봇 47291과 다른 포트)
try:
    _LOCK = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    _LOCK.bind(("127.0.0.1", 47292))
    _LOCK.listen(1)
except OSError:
    log.warning("이미 다른 리마인더봇 인스턴스 실행 중 → 종료")
    sys.exit(0)

intents = discord.Intents.none()
intents.guilds = True            # get_channel / 길드 캐시 (없으면 전면 침묵 실패)
intents.guild_messages = True    # on_message 수신(메타: reference, author)
intents.guild_reactions = True   # on_raw_reaction_add 수신
intents.message_content = False  # ★ 본문 OFF = 프라이버시
client = discord.Client(intents=intents)
tree = app_commands.CommandTree(client)

tracked = {}   # mid -> {channel_id, interval, last, author_id, emoji}
```

### 3-2. 유틸 (alert / 이모지 정규화 / 플래그 식별) — **C4**
```python
async def alert(text):
    try:
        u = client.get_user(config.OWNER_ID) or await client.fetch_user(config.OWNER_ID)
        if u:
            await u.send(text)
    except Exception:
        log.exception("alert(DM) 실패")


def _norm_emoji(e):
    """variation selector(️) 제거 + NFC 정규화 → Discord 라운드트립 비교 안정화 (C4)."""
    return unicodedata.normalize("NFC", str(e)).replace("️", "")


_PRIORITY_NORM = {_norm_emoji(k): v for k, v in config.PRIORITY.items()}
_DONE_NORM = _norm_emoji(config.DONE_EMOJI)


async def _extractor_flagged(message):
    """★ 정식 플래그 식별: 이 메시지에 추출봇 계정이 PRIORITY 이모지 중 '하나라도' 달았는지.
       추출봇 add_reaction 부분실패(rate limit)로 일부만 달려도 추적되도록 '하나라도'로 완화(적대검증 이슈3).
       추출봇이 꺼져 있어도 reaction은 메시지에 남아있어 본문 없이 검증 가능."""
    want = set(_PRIORITY_NORM.keys())
    for r in message.reactions:
        if _norm_emoji(r.emoji) not in want:
            continue
        try:
            async for user in r.users():
                if user.id == config.EXTRACTOR_BOT_USER_ID:
                    return True
        except Exception:
            log.exception("reaction.users() 조회 실패")
    return False
```

### 3-3. `on_ready` — 복원 + 설정검증
```python
@client.event
async def on_ready():
    global tracked
    try:
        await tree.sync()
    except Exception:
        log.exception("명령어 sync 실패")
    tracked = state.load_tracked()
    log.info(f"login: {client.user} (id={client.user.id}) | "
             f"servers {len(client.guilds)} | 추적복원 {len(tracked)}")
    # 설정 함정 방어(적대검증 이슈7): EXTRACTOR_BOT_USER_ID를 자기 id로 잘못 넣으면 전면 미동작
    if config.EXTRACTOR_BOT_USER_ID == client.user.id:
        await alert("🚨 설정 오류: EXTRACTOR_BOT_USER_ID가 리마인더봇 자신의 ID로 설정됨 "
                    "→ 모든 추적이 무시됩니다. 추출봇의 User ID로 고치세요.")
        log.error("EXTRACTOR_BOT_USER_ID == self.id (전면 미동작)")
    if not reminder_tick.is_running():
        reminder_tick.start()
    await alert(f"✅ 리마인더봇 시작 — 추적 {len(tracked)}건 복원")
```

### 3-4. `on_raw_reaction_add` — 추적 시작 + ✅ 해소 (**C1**)
```python
@client.event
async def on_raw_reaction_add(payload):
    # (C1) 봇이 단 reaction 전면 차단: 추출봇의 🔴🟡🔵 부착이 추적을 자동 트리거하지 않도록.
    if payload.member and payload.member.bot:          # 길드 reaction엔 member 채워짐
        return
    if payload.user_id == config.EXTRACTOR_BOT_USER_ID:
        return
    if client.user and payload.user_id == client.user.id:
        return

    ch = client.get_channel(payload.channel_id)
    if ch is None or getattr(ch, "guild", None) is None:
        return
    emoji = _norm_emoji(payload.emoji)

    # ✅(DONE) → 추적 해소
    if emoji == _DONE_NORM:
        if payload.message_id in tracked:
            await resolve(payload.message_id, ch, "done-react")
        return

    # 우선도 이모지(🔴🟡🔵) 사람 클릭 → 추적 시작
    if emoji not in _PRIORITY_NORM:
        return
    if payload.message_id in tracked:            # 중복 추적 가드
        return

    try:
        m = await ch.fetch_message(payload.message_id)   # 본문 미사용, 메타/reaction만
    except Exception:
        return

    # ★ 정식 플래그 검증: 추출봇이 우선도 이모지를 단 메시지일 때만
    if not await _extractor_flagged(m):
        log.info(f"무시: 추출봇 플래그 아님 (mid={payload.message_id}, {emoji})")
        return

    t = {"channel_id": payload.channel_id,
         "interval": _PRIORITY_NORM[emoji] * 3600,
         "last": time.time(),
         "author_id": m.author.id,        # author는 message_content와 무관하게 읽힘
         "emoji": emoji}
    tracked[payload.message_id] = t
    state.save_tracked(payload.message_id, t)

    # 선택된 것 외 나머지 우선도 이모지 제거(클릭된 이모지는 '추적중' 시각표식으로 남김)
    for e in config.PRIORITY:
        if _norm_emoji(e) != emoji:
            try:
                await m.clear_reaction(e)    # 추출봇이 단 것 제거 → Manage Messages 권한 필요
            except Exception:
                pass                          # 권한 없으면 이모지만 잔존, 추적은 정상

    try:
        await m.reply(f"📌 추적 시작 {payload.emoji} — <@{m.author.id}> 이 작업 챙겨주세요!\n"
                      f"완료되면 **{config.DONE_EMOJI} 를 눌러주세요.** (답장은 '아직 진행 중' 신호로 봅니다)")
    except Exception:
        pass
    log.info(f"추적시작 mid={payload.message_id} {emoji} author={m.author.id}")
```

### 3-5. `on_message` — 답장은 타이머 리셋 (**C2**)
```python
@client.event
async def on_message(msg):
    # 본문 못 읽음: msg.content는 빈 문자열일 수 있음 → 절대 참조 안 함. reference 메타만 사용.
    if msg.author.bot or msg.guild is None:
        return
    ref = msg.reference.message_id if msg.reference else None
    if not ref or ref not in tracked:
        return
    # (C2) 본문을 못 읽어 완료/질문 구분 불가 → 답장은 '살아있다' 신호로만 보고 타이머 리셋.
    #      완전 해소는 ✅ 전용. (질문 답장 하나로 작업이 사라지는 회귀 방지)
    t = tracked[ref]
    t["last"] = time.time()
    state.save_tracked(ref, t)
    log.info(f"답장→타이머리셋 mid={ref}")
```
> 미결정 §10-2: "답장도 완전 해소"를 원하면 이 함수를 `resolve(ref, ...)`로 바꾸되, 작성자 본인 답장만(`msg.author.id == t['author_id']`)으로 제한 권장.

### 3-6. `resolve` — 해소
```python
async def resolve(mid, ch, why):
    tracked.pop(mid, None)
    state.del_tracked(mid)
    try:
        m = await ch.fetch_message(mid)
        await m.add_reaction("☑️")
        await m.reply("✅ 완료 처리됐어요. 수고하셨습니다!")
    except Exception:
        pass
    log.info(f"resolve({why}): {mid}")
```

### 3-7. `reminder_tick` — 재촉 + 유령추적 자동정리
```python
@tasks.loop(seconds=config.TICK_SECONDS)
async def reminder_tick():
    now = time.time()
    for mid, t in list(tracked.items()):
        if now - t["last"] < t["interval"]:
            continue
        ch = client.get_channel(t["channel_id"])
        if ch is None:
            continue
        try:
            m = await ch.fetch_message(mid)
        except Exception:
            # 메시지 삭제 등 → 추적 정리
            tracked.pop(mid, None)
            state.del_tracked(mid)
            continue
        # (적대검증 이슈6 완화) 추출봇 플래그 reaction이 사라졌으면(일정이 edit로 소멸 등) 추적 종료
        if not await _extractor_flagged(m):
            log.info(f"유령추적 정리(플래그 reaction 소멸): {mid}")
            tracked.pop(mid, None)
            state.del_tracked(mid)
            continue
        try:
            await m.reply(f"⏰ 리마인드 {t['emoji']} — <@{t['author_id']}> 아직 안 끝났어요!\n"
                          f"완료 시 **{config.DONE_EMOJI}** 를 눌러주세요.")
            t["last"] = now
            state.save_tracked(mid, t)
        except Exception:
            pass


@reminder_tick.before_loop
async def _before_tick():
    await client.wait_until_ready()
```
> 주의: 추출봇이 `remove_reaction(self)`로 자기 것만 제거하면(§2-4), 일정이 edit로 사라져도 **추출봇 플래그 reaction이 같이 사라지므로** 위 `_extractor_flagged` False 분기가 유령추적을 자동 정리. (추출봇이 reaction을 안 떼는 경우엔 ✅로 수동 해소 — §10-3.)

### 3-8. 슬래시 명령 (운영 편의)
```python
@tree.command(name="추적목록", description="현재 재촉 중인 작업")
async def list_cmd(inter: discord.Interaction):
    if not tracked:
        await inter.response.send_message("재촉 중인 작업 없음.", ephemeral=True)
        return
    lines = [f"- https://discord.com/channels/{inter.guild_id}/{t['channel_id']}/{mid} ({t['emoji']})"
             for mid, t in tracked.items()]
    await inter.response.send_message("📋 재촉 중:\n" + "\n".join(lines), ephemeral=True)


@tree.command(name="추적해제", description="추적 해제 (메시지 ID)")
async def untrack_cmd(inter: discord.Interaction, message_id: str):
    key = int(message_id) if message_id.isdigit() else None
    if key is not None and tracked.pop(key, None) is not None:
        state.del_tracked(key)
        await inter.response.send_message("해제했어요.", ephemeral=True)
    else:
        await inter.response.send_message("그 ID는 추적 중이 아니에요.", ephemeral=True)


@tree.command(name="리마인더상태", description="리마인더봇 상태")
async def status_cmd(inter: discord.Interaction):
    await inter.response.send_message(
        f"🟢 리마인더봇 가동 중 | 서버 {len(client.guilds)} | 추적 {len(tracked)}건", ephemeral=True)


if __name__ == "__main__":
    client.run(config.TOKEN)
```

---

## 4. `config_reminder.py` (gitignore 대상)

```python
# reminder/config_reminder.py — 리마인더봇 설정 (.gitignore 필수)
# 추출봇과 완전 분리된 별도 봇/토큰. 본문 인텐트 OFF.

TOKEN = "TOKEN_B_여기에"          # ← 리마인더봇 전용 봇 토큰(토큰B, 신규 발급)

# 모니터링/에러/설정경고 DM 받을 디스코드 유저 ID
OWNER_ID = 343290913172226049

# ★ 추출봇(linkbot)의 '봇 User ID' (Application ID 아님!).
#   현 추출봇 토큰 prefix 디코드값과 일치 확인됨. 토큰 재발급해도 User ID는 불변 → 그대로 사용.
#   확인법: 디스코드 개발자모드 ON → 추출봇 멤버 우클릭 → "ID 복사".
EXTRACTOR_BOT_USER_ID = 1519559729687429131

# 우선도 이모지 → 재촉 간격(시간). 추출봇 config.PRIORITY와 반드시 동일하게 유지.
PRIORITY = {"🔴": 12, "🟡": 24, "🔵": 72}
DONE_EMOJI = "✅"

# 재촉 루프 점검 주기(초)
TICK_SECONDS = 60
```

> `OWNER_ID`/`PRIORITY`/`DONE_EMOJI`는 추출봇 config.py와 같은 값. **두 PRIORITY가 어긋나면** 간격이 달라지고 이모지 비교가 깨질 수 있으니 동기 유지(이상적으론 한 곳에서 관리하나, 폴더 분리 배포 우선이라 복제).

---

## 5. 리마인더봇 state (`reminder/state_reminder.py`)

> 추출봇 `state.py`에서 `tracked`만 떼어낸 축소판. `task` 컬럼 없음(본문 못 읽음). `flagged`/`sched_n`/`last_seen` 불필요.

```python
# reminder/state_reminder.py — 리마인더봇 추적상태 영속(SQLite). 재시작해도 추적 유지.
import os, sqlite3

DB = os.path.join(os.path.dirname(os.path.abspath(__file__)), "state_reminder.db")


def _con():
    c = sqlite3.connect(DB, timeout=10)
    c.execute("CREATE TABLE IF NOT EXISTS tracked ("
              "mid INTEGER PRIMARY KEY, channel_id INTEGER, interval INTEGER, "
              "last REAL, author_id INTEGER, emoji TEXT)")
    return c


def load_tracked():
    c = _con()
    rows = c.execute("SELECT mid, channel_id, interval, last, author_id, emoji "
                     "FROM tracked").fetchall()
    c.close()
    return {r[0]: {"channel_id": r[1], "interval": r[2], "last": r[3],
                   "author_id": r[4], "emoji": r[5]} for r in rows}


def save_tracked(mid, t):
    c = _con()
    c.execute("INSERT OR REPLACE INTO tracked VALUES (?,?,?,?,?,?)",
              (mid, t["channel_id"], t["interval"], t["last"], t["author_id"], t["emoji"]))
    c.commit(); c.close()


def del_tracked(mid):
    c = _con(); c.execute("DELETE FROM tracked WHERE mid=?", (mid,)); c.commit(); c.close()
```
> 테이블 6컬럼 ↔ `save_tracked` 6값 일치 확인. (추출봇 원본은 7컬럼이었으나 `task`는 항상 NULL이었음 → 제거해도 무영향.)

---

## 6. 디스코드 2번째 봇 생성 · 초대 권한

### 6-1. 봇(애플리케이션) 생성
1. https://discord.com/developers/applications → **New Application** (이름 예: `룩플-리마인더`).
2. 좌측 **Bot** → **Add Bot** → **Reset Token**으로 토큰B 발급 → `config_reminder.py`의 `TOKEN`에 입력.
3. **Privileged Gateway Intents:**
   - MESSAGE CONTENT INTENT → **OFF** (★ 프라이버시 핵심)
   - SERVER MEMBERS INTENT → OFF (불필요)
   - PRESENCE INTENT → OFF (불필요)
4. 일반 인텐트(GUILDS / GUILD MESSAGES / GUILD MESSAGE REACTIONS)는 코드 `intents`로 활성 — 포털 토글 불필요(특권 인텐트만 포털 토글 대상).

### 6-2. 초대 (OAuth2 URL Generator)
- **Scopes:** `bot`, `applications.commands`
- **Bot Permissions (최소):**
  - View Channels
  - Read Message History (`fetch_message` / `reaction.users()`)
  - Send Messages (재촉/해소 댓글)
  - **Manage Messages** (추적 시작 시 추출봇이 단 나머지 우선도 이모지 `clear_reaction`)
  - (Add Reactions — ☑️ 부착용)
- 생성된 URL로 **같은 서버 2~3곳**에 초대.

### 6-3. 추출봇 권한 재점검 (변경에 따른 최소화)
- 추출봇은 `_clear_schedules`에서 `remove_reaction(self)`로 바뀌므로 **Manage Messages 불필요**.
- 추출봇 필요 권한: View Channels, Read Message History, Send Messages, **Add Reactions**.
- 인텐트: MESSAGE CONTENT **ON**(유지).

### 6-4. 토큰 보안 (필수)
- `config.py` line 4의 현 토큰은 **평문 노출 이력** → 추출봇 토큰A **즉시 재발급**. (User ID `1519559729687429131`은 불변이므로 `EXTRACTOR_BOT_USER_ID`는 재발급 후에도 그대로.)
- 토큰B는 신규 발급. `config.py`/`config_reminder.py` 모두 `.gitignore` 확인.

---

## 7. 호스트 배포 (always-on, 무료 옵션)

리마인더봇은 Ollama·시트 의존이 없고 CPU/메모리 매우 가벼움 → 저사양 always-on이면 충분.

| 옵션 | 장점 | 단점/주의 |
|---|---|---|
| **라즈베리파이(권장)** | 자가 호스팅, 완전 무료(전기만), 프라이버시 일관 | 초기 셋업. `systemd`로 자동재시작 권장 |
| **안드로이드 폰 + Termux** | 남는 폰 재활용, 무료 | 절전/네트워크 끊김 시 갭(reaction 큐잉 안 됨 → §8 수용리스크) |
| **Oracle Cloud Always Free** | 24/7 VM 무료(ARM), 안정 | 가입/카드등록, 해외 리전 레이턴시(무해) |
| **fly.io / Render 무료티어** | 간편 배포 | 무료티어 sleep 정책 주의(리마인더봇은 sleep 시 클릭 놓침 → 부적합 가능) |

권장 배포 절차(라즈베리파이/Termux 공통):
```bash
# 1) 폴더만 복사 (reminder/ 만; linkbot/은 복사 금지)
#    reminder_bot.py, state_reminder.py, config_reminder.py(토큰B 채움)
# 2) 의존성 (Ollama·gspread 등 불필요, discord.py만)
pip install -U "discord.py>=2.4"
# 3) 실행
python reminder_bot.py
```
자동재시작 (라즈베리파이 systemd 예):
```ini
# /etc/systemd/system/lookpl-reminder.service
[Unit]
Description=Lookpl Reminder Bot
After=network-online.target
[Service]
WorkingDirectory=/home/pi/reminder
ExecStart=/usr/bin/python3 /home/pi/reminder/reminder_bot.py
Restart=always
RestartSec=10
[Install]
WantedBy=multi-user.target
```
```bash
sudo systemctl enable --now lookpl-reminder
```

---

## 8. 구현 체크리스트

### 추출봇 (`linkbot/bot.py`)
- [ ] line 48 `tracked = {}` 삭제
- [ ] line 75 `global tracked, flagged` → `global flagged`
- [ ] line 80 `tracked = state.load_tracked()` 삭제
- [ ] line 82 로그 문구 tracked 제거
- [ ] line 83-84 `reminder_tick` 시작 블록 삭제 (85-86 `resolve_loop`는 **유지**)
- [ ] line 92 시작 알림 문구 tracked 제거
- [ ] line 165 `and msg.id not in tracked` 삭제
- [ ] `_clear_schedules`: tracked 분기 제거 + `clear_reaction`→`remove_reaction(self)`
- [ ] `on_message`: line 235/240-241/244-245 삭제
- [ ] `_check_done`(308-327) 삭제 — **`re` import는 유지**
- [ ] `on_raw_reaction_add`(357-391) 삭제
- [ ] `resolve`(394-403) 삭제
- [ ] `reminder_tick`(406-423) 삭제
- [ ] `추적목록`/`추적해제`(528-544) 삭제, `상태`(547-550) 문구 수정
- [ ] (선택) state.py tracked 테이블/함수 삭제
- [ ] **grep 검증**: `tracked|reminder_tick|\bresolve\b|on_raw_reaction_add|_check_done` 0건
- [ ] `python -c "import ast,sys; ast.parse(open('linkbot/bot.py',encoding='utf-8').read())"` 파싱 통과

### 리마인더봇 (신규 `reminder/`)
- [ ] `reminder_bot.py` 작성 (§3 전체)
- [ ] `state_reminder.py` 작성 (§5)
- [ ] `config_reminder.py` 작성 + 토큰B + EXTRACTOR_BOT_USER_ID 채움 (§4)
- [ ] `.gitignore`에 `config_reminder.py`, `*.db`, `*.log` 추가
- [ ] C1 가드(`payload.member.bot` + EXTRACTOR_BOT_USER_ID) 포함 확인
- [ ] C2 `on_message`가 resolve 아닌 타이머 리셋인지 확인
- [ ] C4 `_norm_emoji` 적용 확인

### 디스코드/배포
- [ ] 토큰A 재발급, 토큰B 신규 발급
- [ ] 리마인더봇 MESSAGE CONTENT INTENT **OFF** 확인
- [ ] 리마인더봇 초대(Manage Messages 포함), 2~3 서버
- [ ] always-on 호스트에 `reminder/`만 배포 + 자동재시작

---

## 9. 테스트 (수동 시나리오)

> 두 봇을 같은 테스트 서버에 올리고 진행. 추출봇은 PC, 리마인더봇은 (테스트 단계엔) 같은 PC 다른 포트로 동시 실행 가능(락 47291/47292 분리).

1. **정상 추적 흐름**
   - 작업 메시지 작성 → 추출봇이 🔴🟡🔵 부착되는지.
   - 🔴 클릭 → 리마인더봇이 "추적 시작" 댓글 + 🟡🔵 사라지고 🔴만 남는지.
   - state_reminder.db에 1건 등록 확인(`/추적목록`).

2. **C1 자동 헛추적 차단**
   - 새 작업 메시지 → 추출봇이 🔴🟡🔵 부착하는 그 순간, 리마인더봇이 **추적을 시작하지 않아야** 함(사람 클릭 전엔 tracked 0). 로그에 "추적시작" 없어야 정상.

3. **C2 답장 과잉해소 방지**
   - 추적 중 메시지에 제3자가 "이거 언제까지예요?" 답장 → **추적 유지 + 타이머만 리셋**(해소/☑️ 없어야 함). `/추적목록`에 그대로.
   - 같은 메시지에 ✅ 클릭 → 그제서야 해소(☑️ + 완료 댓글).

4. **재촉**
   - 테스트용으로 `PRIORITY`를 분 단위로 임시 축소(예: `{"🔴": 0.02}` ≈ 72초) → 간격 후 재촉 댓글 오는지, `last` 갱신되는지.

5. **재시작 복원**
   - 리마인더봇 종료 → 재실행 → `추적복원 N` 로그 + `/추적목록` 유지 확인.

6. **C4 이모지 비교**
   - 추출봇이 단 🔴를 클릭했을 때 "추출봇 플래그 아님" 무시 로그가 **안 떠야** 정상(정규화 동작 확인).

7. **유령추적 정리**
   - 추적 중 일정 메시지를 edit로 비일정화 → 추출봇이 자기 🔴 제거 → 다음 `reminder_tick`에서 리마인더봇이 "유령추적 정리" 로그 + 추적 해제.

8. **추출봇 회귀 없음**
   - 일반 메시지/일정/정산/확인필요/답장 진행갱신/과거등록/`상태` 명령이 추적 제거 후에도 정상 동작(특히 `on_message`가 NameError 없이 도는지 — C3).

---

## 10. 미결정 (사용자 확인 요망)

1. **standalone "완료" 키워드 처리 (드롭 확정 여부).**
   현재 스펙은 `_check_done` 제거 = "완료"라고 그냥 적으면 시트 미갱신. 완료는 **답장으로** 안내(추적시작/재촉 댓글에 반영됨). 메모리 방향과 일치하나, 기존 사용자가 standalone "완료"를 계속 쓰면 **조용히 시트 누락**. → 드롭 확정 OK인지, 아니면 추출봇에 "완료는 답장으로 달아주세요" 경량 안내 핸들러만 남길지.

2. **답장의 의미 (타이머 리셋 vs 완전 해소).**
   본 스펙은 C2 때문에 **답장=타이머 리셋, 완전 해소는 ✅ 전용**으로 확정. "답장도 완료로 인정"을 원하면 §3-5를 `resolve`로 바꾸되 **작성자 본인 답장만**으로 제한 권장(질문 답장 오인 방지). 어느 쪽?

3. **유령추적 — 추출봇 reaction 제거 방식과의 결합.**
   §2-4에서 추출봇이 `remove_reaction(self)`로 자기 것만 떼면, 리마인더봇 `reminder_tick`의 `_extractor_flagged` False 분기가 유령추적을 자동 정리(권장 동작). 단 이는 **재촉 간격이 도래해야** 정리됨(최대 간격만큼 지연). 즉시 정리가 필요하면 추출봇→리마인더봇 신호 채널이 필요(현재 없음, MVP는 지연 수용). 수용 OK인지.

4. **PRIORITY 동기화.**
   추출봇 `config.py`와 리마인더봇 `config_reminder.py`에 PRIORITY가 **이중 정의**됨(폴더 분리 배포 때문). 한쪽만 바꾸면 어긋남. 공유 설정 파일로 단일화할지, 복제 유지 + 주석 경고로 둘지.

5. **재시작 순간 놓친 클릭.**
   always-on이라도 재시작/네트워크 갭 동안의 reaction-add는 Discord가 큐잉 안 함 → 놓침. MVP는 "놓치면 사용자 재클릭"으로 수용. catchup(history 스캔) 보강은 enhancement로 분리. 수용 OK인지.

---

### 관련 파일 (절대경로)
- 추출봇: `D:\Study\DIscordBot\AutoLinkBot\linkbot\bot.py`, `D:\Study\DIscordBot\AutoLinkBot\linkbot\state.py`, `D:\Study\DIscordBot\AutoLinkBot\linkbot\config.py`
- 리마인더봇(신규): `D:\Study\DIscordBot\AutoLinkBot\reminder\reminder_bot.py`, `D:\Study\DIscordBot\AutoLinkBot\reminder\state_reminder.py`, `D:\Study\DIscordBot\AutoLinkBot\reminder\config_reminder.py`
