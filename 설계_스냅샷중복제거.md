# 설계: 스냅샷 교체 — "전체일정" 재게시 중복 행 제거 (최종 통합 스펙)

> 대상 파일(절대경로)
> - `D:\Study\DIscordBot\AutoLinkBot\linkbot\brain.py` (수정)
> - `D:\Study\DIscordBot\AutoLinkBot\linkbot\state.py` (수정)
> - `D:\Study\DIscordBot\AutoLinkBot\linkbot\bot.py` (수정)
> - `D:\Study\DIscordBot\AutoLinkBot\linkbot\sheet.py` (**무변경**)
> - `D:\Study\DIscordBot\AutoLinkBot\extract\AppsScript.gs` (**무변경**)
> - 선행 설계 불변식: `D:\Study\DIscordBot\AutoLinkBot\설계_답장수정다중일정.md` (키 `{mid}#i` 규칙·merge/upsert 계약·ok:0 검사 — 전부 유지)
>
> 이 문서는 세 하위 설계([match]/[flow]/[state])와 적대검증 3건(총 40건, 중복 제거 후 유효 22건)을 **하나의 구현 가능한 스펙**으로 통합한다. 하위 설계 간 모순([flow]↔[state]의 matched push 모드, 완료 행 후보 제외, 신규 키 인덱스, F 규칙, 심볼표)은 전부 본 문서에서 단일 확정했다. 개별 하위 설계와 본 문서가 다르면 **본 문서가 우선**한다.

---

## 1) 확정 설계 요약

### 1-1. 아키텍처: 레지스트리 방식 확정 (GAS 무변경)

- 행 키 `{mid}#i`는 생성 후 **불변**. rekey(행 소유권을 최신 스냅샷 메시지로 이동) **기각** — GAS action 추가·마이그레이션·크래시 시 키 중복/유실 검증용 read 경로가 연쇄로 필요해지는 반면, 얻는 것(답장이 최신 메시지로 향함)은 `snap_msgs` 라우팅으로 동등 달성.
- 채널(작업자)별 **스냅샷 레지스트리**를 `linkbot/state.db`(SQLite)에 둔다: `snap_reg`(행 목록) + `snap_meta`(순서 게이트) + `snap_msgs`(라우팅 마킹) + `sheet_outbox`(완료/삭제 push 재시도 대기열).
- 시트 연산은 기존 계약만 소비: `push(upsert)`(기존행 비공백만 덮기+미존재 생성), `push(merge)`(미존재행 skip), `delete_rows`, `clearSchedule()`(수동 1회). **AppsScript.gs·sheet.py 한 줄도 안 바꾼다.**

### 1-2. 스냅샷 감지 (최종 규칙)

```
스냅샷 = state.is_snap_msg(msg.id)                # 한번 스냅샷이면 영구 스냅샷 (sticky)
       OR ( brain.SNAP_HINT_RE가 본문에 매치      # "전체일정"이 줄 단독 헤더로 존재
            AND 추출 items >= 2 )
```

- `SNAP_HINT_RE = re.compile(r"^\s*[\[\(#*•\-~<【]*\s*전체\s*일정\s*[\]\)>:#*~】]*\s*$", re.M)` — **줄 전체가 키워드(±장식)일 때만**. "전체일정에 추가해주세요"/"전체일정 회의 잡을게요" 같은 문장 속 언급은 미매치(오탐 차단, 검증 V2-10/V3-7).
- sticky 규칙: 한 번 스냅샷으로 diff된 메시지는 edit/catchup 재생에서 재추출이 1건이어도 **스냅샷 경로 유지**(LLM 비결정성으로 인한 재분류 금지). 반대 방향(일반 메시지 → edit로 스냅샷 승격)은 허용 — 신규 키 HW 할당(§1-6)이 자기 행과의 키 충돌을 구조적으로 막는다.
- 감지 즉시(**diff 시작 전**) `snap_msgs`에 선등록 — stale/Ollama 중단으로 diff가 안 끝나도 그 메시지에 온 답장은 레지스트리 라우팅을 탄다(답장 누수→중복 행 차단, 검증 V2-14).
- 오탐 잔여 리스크는 §1-4의 missing 가드(언급 0건 → missing 생략) + 자동완료 알림 + 재등장 부활(§1-4)이 흡수. **작성자 가드는 기각**(§7).

### 1-3. 매칭 엔진 (2단, brain.py)

- **1단**: `canon()` 정규화 완전일치 — 후보·신규 양쪽 모두 **유일**할 때만 인정. canon 충돌(괄호 쌍둥이 등)·불일치는 2단으로.
- **2단**: `SAME_TASK_PROMPT`로 exaone3.5:7.8b(temperature 0, 기존 `ollama()` 재사용)에 후보 **원문** 제시 → 번호 하나 또는 `새작업`. 오출력/범위밖/다중숫자 → **새 행**(잘못 합치기 금지). 기한은 프롬프트에 제시하지 않는다(날짜 연장이 오판 유발).
- **전송 실패**(ollama 2회 재시도 후에도)는 예외 전파 → **그 스냅샷 diff 전체 중단**(부분 반영 금지, 대량 신규행 방지). 의미 재시도는 없음(temp 0에서 무의미).
- 전체 매칭은 반드시 `match_snapshot()`(1:1 선점 계약)으로 — 단건 `match_task()` 루프 금지(이중 클레임 방지).
- **매칭 후보는 완료 행 포함** (`reg_channel_items(ch, active_only=False)`) — [match]의 `active_rows`/[flow] S4 모순을 여기서 확정. 완료 행이 후보에 없으면 부활이 불가능하고 "완료행+신규행" 중복이 영구 누적된다(검증 3건 공통 최상위 이슈).

### 1-4. diff 의미론 (matched / new / missing-b안 / 재활성)

| 분류 | 정의 | 레지스트리 | 시트 |
|---|---|---|---|
| matched | 매칭 성공 | task/date 갱신, `managed=1` 승격, missing=0, `last_seen_snap=S` | `upsert` — 필드 규칙 §1-5 |
| new | 미매칭 + (날짜 있음 or 진행줄 아님) | `{S}#hw+k`로 INSERT | `upsert` 신규행 |
| 진행줄 미매칭 | 날짜 없음 + (진행내용 있음 or 상태=완료) + 미매칭 | 없음 | **행 미생성**, 로그만 (F 확정, [flow] S6 채택) |
| missing | `managed=1`·`진행중`·이번 스냅샷 미언급 | `missing_count+1` (KST 날짜 기준 **하루 1회**) | 불변 |
| 자동완료(b안) | missing이 2 도달 | `상태=완료, completed_by='snap:{S}'` | outbox 경유 `merge {상태:완료}` + **채널 알림** |

**재활성(부활) 정책 — 확정** (matched인데 레지스트리 status가 '완료'일 때):

```
completed_by가 'snap:…' (빠짐 자동완료)  → 무조건 진행중 부활
    근거: "빠져서 완료 처리"했는데 재등장 = 완료 판정의 반증. 오탐/추출실패 자동완료의 자가치유 백스톱.
completed_by가 'reply:…' 또는 ''        → 신정보(날짜 변경 or 진행 문구 존재) 있을 때만 부활.
    없으면 상태=완료 유지(터치만: missing 리셋·task/date 갱신·completed_by 보존).
    근거: 작업자가 완료 답장 후에도 옛 줄을 리스트에 계속 복붙하는 패턴에서 완료 뒤집힘 방지.
스냅샷 라인 자체가 완료 선언(상태=완료 추출)          → 완료 그대로 (completed_by는 기존값 보존).
```

**missing 가드 (오완료 방어 3중)**:
1. 하루 1회: `last_missing_date != 스냅샷 KST 날짜`일 때만 +1 (같은 날 재게시 이중 카운트 방지; KST는 `msg.created_at+9h` — 재생 시점 무관, 멱등).
2. 언급 0건 가드: `len(matched)==0`이고 채널 활성 managed 행 ≥3이면 **missing 단계(+자동완료) 생략** — 스냅샷 오탐(무관 리스트)의 폭발반경 차단. (비율 가드 50~60%는 **기각** — 정상적 대량 축소 시 은퇴가 영구 교착됨, §7.)
3. 본문 절단 가드: 본문 길이 > `SNAP_BODY_MAX`(1200)면 missing 생략(꼬리 항목 반복 오완료 방지).
4. 자동완료 발생 시 채널에 알림 게시("『X』 전체일정에서 2일 연속 빠져 완료 처리. 진행 중이면 이 메시지에 답장") — 알림 메시지도 `snap_msgs(kind='alert')` 등록 → 그 답장이 레지스트리로 라우팅되어 부활 가능.

### 1-5. 시트 push 계약 (모순 확정: [state]의 upsert 채택 + 필드 제한)

**matched 행은 `upsert`** ([flow] §9-4 "merge, PM 삭제 존중" **폐기**). 단 필드를 제한해 PM 수동편집을 보존한다:

```
matched upsert fields = { 작업내용, 담당자, 링크 }
                      + 날짜        (스냅샷 라인에 날짜 있을 때만)
                      + 진행내용    (스냅샷 라인에 진행 문구 있을 때만)
                      + 상태        (전이 발생 시에만: 부활 → '진행중' / 라인 완료선언 → '완료')
```

- 전이 없는 평상시엔 상태를 **보내지 않음** → GAS upsert의 "기존행 비공백만 덮기" 계약으로 PM이 수동 입력한 상태(쉬는중/대기중/답변필요)가 매일 덮이지 않는다(검증 V1-1 절충).
- PM이 행을 수동 삭제해도 스냅샷이 계속 선언하는 한 upsert가 행을 **재생성**(자가치유: push 실패/크래시 후에도 다음 스냅샷에서 시트가 레지스트리로 수렴). 운영 규칙: **PM은 행 삭제 대신 상태=완료로 마감**, 영구 정리는 clearSchedule+force(§4).
- 재생성된 행은 상태 칸이 빈칸일 수 있음(다음 전이/답장에서 채워짐) — 한계로 문서화(§8).
- **자동완료 merge·edit 축소 delete는 1회성이라 자가치유가 없다** → `sheet_outbox`에 같은 트랜잭션으로 적재하고, push 직후 + `resolve_loop` 매 사이클에 드레인(재시도). 응답이 `err:`가 아니면(ok:0 포함 — 행이 이미 없음=목적 달성) outbox에서 제거. (검증 V1-6/V3-6)

### 1-6. 멱등·순서 (레이스/재시작/edit 안전장치)

1. **채널별 `asyncio.Lock`** — "후보 조회 → 매칭(LLM) → `reg_apply_snapshot` 커밋 → push" 전 구간과, 스냅샷 답장 라우팅, 비스냅샷 일정 편입(enroll)을 같은 락으로 직렬화. on_message/catchup/backfill/edit 전 진입점 공용. (읽기→커밋 비원자 구간의 이중 신규행 레이스 차단, 검증 V2-5/V3-4)
2. **순서 게이트**: 채널별 `snap_meta.last_snap_mid`(snowflake 단조증가). `S < last` → **stale**: diff 전체 무시(신규행도 안 만듦). `S == last` → **replay**: `prev_*` 1세대 undo로 되감고 재계산(증분이 아닌 재계산 → missing 오증가 불가). `S > last` → new.
3. **신규 키 HW(high-water) 할당** — "#0..#(n-1) 연속" 불변식을 스냅샷에 한해 폐기:
   `hw = max( sched_n:{S}, reg_items_by_src(S)의 최대 인덱스+1, 0 )`, 신규 k건에 `{S}#hw..{S}#hw+k-1` 부여. `reg_apply_snapshot`은 `matched ∩ new = ∅`를 검사해 위반 시 예외(diff 중단). (replay/스냅샷 승격에서 자기 행 하이재킹 차단 — 검증 3건 공통 최상위 이슈)
4. **replay 되감기 규칙**: `missing/status/last_missing_date`를 `prev_*`로 복원하되 `completed_by LIKE 'reply:%'`는 상태 보호. `last_seen_snap`은 복원하지 않는다(§2-B 참고 — missing 조건에서 `last_seen_snap` 비교를 제거했으므로 무해). replay의 `deleted_new`(사라진 자기 신규행)에서 **status='완료' 행은 제외**(답장 완료 기록 보존, 검증 V2-7/V3-10).
5. **커밋 → push 순서**: 레지스트리가 진실원본. push 실패 시 재시도하지 않아도 matched/new는 다음 스냅샷 upsert로, 완료/삭제는 outbox로 수렴.
6. catchup은 코드 무변경 — 채널 내 `oldest_first=True` 재생이 이미 순서를 보장하고, 스냅샷 처리(stale/replay 게이트 포함)는 process 내부에 위임된다. `last_seen` 커서와 레지스트리 커서는 상호 독립.

### 1-7. 처리 파이프라인 (전체 흐름)

```
on_message / catchup / edit / backfill
 └ ref 있음? → _apply_reply_update
      ├ ref ∈ snap_msgs? → 본문이 SNAP_HINT면 미소비(새 스냅샷으로) / 아니면 _route_snapshot_reply
      └ 아니면 기존 n판정 경로 (불변) + 레지스트리 완료 훅
 └ process → analyze
      ├ 일정 & _is_snapshot → _apply_snapshot   [채널 락]
      │    snap_msgs 선등록 → merge_snapshot_items(진행줄 선흡수)
      │    → 후보(완료 포함) 조회 → match_snapshot (1단 canon → 2단 LLM)
      │    → 재활성 정책·F 규칙 적용 → new 키 HW 할당 → missing 가드 판정
      │    → reg_apply_snapshot(트랜잭션: rewind/matched/missing/자동완료/INSERT/outbox/meta)
      │    → stale면 종료 / matched+new upsert push → sched_n=HW' → outbox 드레인 → 자동완료 알림
      │    (이모지 미부착 — 의도된 변화)
      ├ 일정 & 비스냅샷 → 기존 _reconcile_schedules (불변) + reg 편입(managed=0, canon 중복 가드)
      └ 정산/확인필요 → 불변
resolve_loop: 기존 + (일정 등록 시) canon 선병합·편입 + outbox 드레인
```

---

## 2) 파일별 정확한 변경점

### 2-A. `linkbot\brain.py`

기존 심볼 무변경: `ollama()`(재시도 2회 그대로), `_canon_task`/`parse_deadline_lines`(메시지 내부 dedup 의미론 불변), `DEADLINE_PROMPT`, `PROGRESS_PROMPT`, `extract_progress`, `is_done`/`detect_status`, `norm_date`.

**심볼표(확정 — [flow]의 `snap_canon`/`SNAP_MATCH_PROMPT`/`match_snapshot_item` 명칭 폐기)**: `CANON_VERSION`, `canon()`, `SNAP_HINT_RE`, `SNAP_BODY_MAX`, `SAME_TASK_PROMPT`, `parse_same_task()`, `match_task()`, `match_snapshot()`, `merge_snapshot_items()`, `REPLY_ROUTE_PROMPT`, `route_reply()` + `analyze()` 2곳 수정.

#### (A-1) canon 정규화 (파일 상단 `import unicodedata` 추가)

```python
# ── 스냅샷 매칭: canon 정규화 ────────────────────────────────
CANON_VERSION = 1    # 규칙 변경 시 +1 → 기동 시 reg_recanon으로 전량 재계산

_PAREN_RE = re.compile(r"[(\[{（【〔「『［].*?[)\]}）】〕」』］]")
_NIM_STRIP_RE = re.compile(r"([가-힣a-z0-9_]{1,12})님")
_TRAIL = ("까지", "부터", "작업", "예정", "진행", "중")


def canon(s):
    """스냅샷 레지스트리 매칭용 작업명 정규화. (_canon_task와 별개)"""
    t = unicodedata.normalize("NFKC", (s or "")).lower().strip()
    if not t:
        return ""
    t2 = _PAREN_RE.sub("", t)                    # (개인일정) [6/28~7/15] 등 괄호주석 제거
    if len(re.sub(r"\s", "", t2)) >= 2:          # 전부 괄호였으면 되돌림 (빈 canon 방지)
        t = t2
    t = _NIM_STRIP_RE.sub(r"\1", t)              # 카조에님 → 카조에 (이름은 보존)
    t = re.sub(r"[^0-9a-z가-힣.~/]", "", t)       # 공백·문장부호 제거
    t = re.sub(r"(?<![0-9])[.~/]|[.~/](?![0-9])", "", t)  # 숫자 사이 아닌 . ~ / 제거
    changed = True
    while changed:                               # 꼬리 필러: "모델링작업중"→"모델링"
        changed = False
        for suf in _TRAIL:
            if t.endswith(suf) and len(t) - len(suf) >= 2:
                t = t[:-len(suf)]
                changed = True
    return t
```

규칙 순서·근거·과잉정규화 방지 쌍(이름 보존, 숫자·숫자사이 `.~/` 보존, "도움" 미삭제, 길이 2 가드)은 [match] §1 그대로 채택. 부분포함(containment) 휴리스틱 불채택.

#### (A-2) 스냅샷 감지 상수

```python
SNAP_HINT_RE = re.compile(r"^\s*[\[\(#*•\-~<【]*\s*전체\s*일정\s*[\]\)>:#*~】]*\s*$", re.M)
SNAP_BODY_MAX = 1200      # 스냅샷 본문 절단 한도 (bot이 절단 여부 판정에도 사용)
```

#### (A-3) 2단 판정 프롬프트 + 파서 ([match] 원안 그대로)

```python
SAME_TASK_PROMPT = (
    "한 작업자의 '기존 작업 목록'과 '새로 보고된 작업' 하나가 있다.\n"
    "새 작업이 기존 목록 중 하나와 같은 작업인지 판정해라.\n"
    "같은 작업 = 표현·어순·꾸밈말만 다르고 실제로는 동일한 일.\n"
    "대상 인물이 다르거나, 부위·단계·차수·계절 등 실체가 다르면 다른 작업이다.\n"
    "출력 규칙(반드시):\n"
    "  - 같은 작업이 있으면 그 번호 하나만 출력. 예: 2\n"
    "  - 없으면 정확히 `새작업` 한 단어만 출력.\n"
    "  - 설명·문장 금지. 확실하지 않으면 `새작업`.\n\n"
    "예시1)\n기존 작업 목록:\n1. 카조에님 오리지널 의상\n2. 권민님 작캠용 바디 수정 도움\n"
    "새로 보고된 작업: 카조에님 오리지날 의상 제작\n-> 1\n\n"
    "예시2)\n기존 작업 목록:\n1. 다시바님 오리지널 헤드\n"
    "새로 보고된 작업: 다시바님 오리지널 헤어\n-> 새작업\n\n"
    "예시3)\n기존 작업 목록:\n1. 권민님 바디 수정 도움\n2. 카조에님 모델링\n"
    "새로 보고된 작업: 권민님 작캠용 바디 수정\n-> 1\n\n"
    "예시4)\n기존 작업 목록:\n1. 뿌요님 여름 의상\n"
    "새로 보고된 작업: 뿌요님 겨울 의상\n-> 새작업\n\n"
    "예시5)\n기존 작업 목록:\n1. 마태자님 1차 검수\n"
    "새로 보고된 작업: 마태자님 2차 검수\n-> 새작업\n\n"
    "기존 작업 목록:\n{cands}\n새로 보고된 작업: {task}\n->\n")

_NEW_TASK_TOKENS = ("새작업", "새로운작업", "신규작업", "신규", "없음", "new")
_ANS_DECOR_RE = re.compile(r"->|정답|후보|번째|번|[\s.():：\-,]+")


def parse_same_task(out, n):
    """출력 → 1..n | 'new' | None(해석불능). 'new'/None 모두 호출부에서 새 행."""
    if not out:
        return None
    line = ""
    for ln in out.strip().splitlines():
        if ln.strip():
            line = ln.strip()
            break
    if not line:
        return None
    flat = re.sub(r"\s", "", line).lower()
    if not re.search(r"\d", flat):
        if any(flat.startswith(tok) for tok in _NEW_TASK_TOKENS):
            return "new"
        return None
    core = _ANS_DECOR_RE.sub("", line).strip()
    if core.isdigit():
        k = int(core)
        if 1 <= k <= n:
            return k
    return None
```

#### (A-4) 매칭 진입점 ([match] 원안, 시그니처 동일)

```python
def match_task(candidates, new_task):
    """candidates: [{'canon','task'}] 순서 유지. 반환 인덱스|None(새 행).
    ollama 전송 실패는 전파 — 호출부가 스냅샷 diff 전체 중단."""
    if not candidates:
        return None
    nc = canon(new_task)
    if nc:
        hits = [i for i, c in enumerate(candidates)
                if c.get("canon") and c["canon"] == nc]
        if len(hits) == 1:
            return hits[0]                     # 1단: 유일 완전일치만
    cand_lines = "\n".join(
        f"{i + 1}. {(c.get('task') or '')[:60]}" for i, c in enumerate(candidates))
    out = ollama(SAME_TASK_PROMPT.format(cands=cand_lines, task=(new_task or '')[:60]))
    r = parse_same_task(out, len(candidates))
    if isinstance(r, int):
        return r - 1
    return None


def match_snapshot(candidates, new_tasks):
    """1:1 선점 매칭. 반환: [cand_index|None] (new_tasks와 같은 길이). 예외는 전파."""
    result = [None] * len(new_tasks)
    claimed = set()
    canons = [canon(t) for t in new_tasks]
    by_canon = {}
    for i, c in enumerate(candidates):
        by_canon.setdefault(c.get("canon") or "", []).append(i)
    dup_new = {c for c in canons if c and canons.count(c) > 1}
    for j, nc in enumerate(canons):            # 1단: 양쪽 유일할 때만 선점
        if not nc or nc in dup_new:
            continue
        hits = [i for i in by_canon.get(nc, []) if i not in claimed]
        if len(hits) == 1:
            result[j] = hits[0]
            claimed.add(hits[0])
    for j in range(len(new_tasks)):            # 2단: 남은 항목 × 남은 후보 (메시지 순 = 결정적)
        if result[j] is not None:
            continue
        remain = [i for i in range(len(candidates)) if i not in claimed]
        if not remain:
            break
        r = match_task([candidates[i] for i in remain], new_tasks[j])
        if r is not None:
            result[j] = remain[r]
            claimed.add(remain[r])
    return result
```

#### (A-5) 스냅샷 내부 병합 — **[match] 원안에서 수정** (무조건 canon 병합 → 진행줄 흡수 한정)

적대검증 V1-3/V2-2: 원안은 "바디 수정(상의)"/"바디 수정(하의)"처럼 canon이 충돌하는 **날짜 있는 별개 작업을 diff 전에 소멸**시켰다. 병합은 F 목적(날짜 없는 진행줄 흡수)에만 한정한다.

```python
def _is_progline(it):
    """날짜 없음 + (진행 문구 or 완료) = 진행 보고 줄."""
    return (not it.get("날짜")) and (bool(it.get("진행내용")) or it.get("상태") == "완료")


def merge_snapshot_items(items):
    """canon 동일 그룹에서 '진행줄'만 날짜 있는 항목에 흡수. 날짜 있는 canon 쌍둥이는 별개 유지.
    진행줄끼리(둘 다 날짜 없음)는 첫 항목에 합침."""
    out = []
    by_canon = {}
    for it in items:
        it = dict(it)
        k = canon(it.get("작업내용", ""))
        group = by_canon.get(k, []) if k else []
        target = None
        if group:
            if _is_progline(it):
                target = group[0]                       # 진행줄 → 같은 canon의 앞 항목에 흡수
            else:
                proglines = [g for g in group if _is_progline(g)]
                if proglines:
                    target = proglines[0]               # 앞서 나온 진행줄에 날짜 항목을 얹음
        if target is None:
            out.append(it)
            if k:
                by_canon.setdefault(k, []).append(it)
            continue
        if not target.get("날짜") and it.get("날짜"):
            target["날짜"] = it["날짜"]
        if it.get("진행내용") and not target.get("진행내용"):
            target["진행내용"] = it["진행내용"]
        if "완료" in (target.get("상태"), it.get("상태")):
            target["상태"] = "완료"
        elif it.get("날짜"):
            target["상태"] = it.get("상태") or target.get("상태")
    return out
```

#### (A-6) 답장 라우팅 프롬프트 (스냅샷 답장 전용, B 지원)

```python
REPLY_ROUTE_PROMPT = (
    "한 작업자의 '작업 목록'과, 그 작업자가 보낸 '답장'이 있다.\n"
    "답장이 어느 작업 하나에 대한 것이면 그 번호 하나만 출력.\n"
    "목록 전체에 대한 것이면(다/전부/모두/싹 등) 정확히 `전체`만 출력.\n"
    "어느 작업인지 알 수 없으면 정확히 `모름`만 출력. 설명·문장 금지.\n\n"
    "예시1)\n작업 목록:\n1. 카조에님 오리지널 의상\n2. 다시바님 오리지널 헤드\n"
    "답장: 의상 절반 정도 했어요\n-> 1\n\n"
    "예시2)\n작업 목록:\n1. 카조에님 오리지널 의상\n2. 다시바님 오리지널 헤드\n"
    "답장: 다 끝냈습니다\n-> 전체\n\n"
    "예시3)\n작업 목록:\n1. 카조에님 오리지널 의상\n2. 다시바님 오리지널 헤드\n"
    "답장: 내일 병원 다녀올게요\n-> 모름\n\n"
    "작업 목록:\n{cands}\n답장: {m}\n->\n")

_ALL_TOKENS = ("전체", "모두", "전부", "다", "all")


def route_reply(candidates, text):
    """스냅샷 답장 → ('one', idx) | ('all', None) | ('none', None). 전송 실패는 전파."""
    if not candidates:
        return ("none", None)
    if len(candidates) == 1:
        return ("one", 0)                      # 후보 1개면 LLM 생략
    cand_lines = "\n".join(
        f"{i + 1}. {(c.get('task') or '')[:60]}" for i, c in enumerate(candidates))
    out = ollama(REPLY_ROUTE_PROMPT.format(cands=cand_lines, m=(text or '')[:200]))
    r = parse_same_task(out, len(candidates))
    if isinstance(r, int):
        return ("one", r - 1)
    line = (out or "").strip().splitlines()[0].strip() if (out or "").strip() else ""
    flat = re.sub(r"\s", "", line).lower()
    if any(flat.startswith(t) for t in _ALL_TOKENS):
        return ("all", None)
    return ("none", None)
```

#### (A-7) `analyze()` 수정 2곳 (그 외 불변)

```python
def analyze(content, channel_name, author, mentions, context):
    snap = bool(SNAP_HINT_RE.search(content or ""))                  # [수정1] 힌트 선판정
    text = (content or "")[:SNAP_BODY_MAX if snap else 400]          # [수정1] 400 → 조건부 1200
    if len(text) < 3:
        return None
    # ... 정산 분기 불변 ...
    if has(text, config.DATE_HINTS) or snap:                         # [수정1] 스냅샷 헤더도 일정 신호
        out = ollama(DEADLINE_PROMPT.format(m=text))
        # ... rows/worker/작업자 불명확 분기 불변 ...
        if len(rows) > (25 if snap else 10):                         # [수정2] 스냅샷 상한 25
            return review(content, who, client, f"일정 과다추출({len(rows)}건) — 확인 요망")
        # ... items 조립·return 불변 ...
```

- 스냅샷인데 25건 초과 → 확인필요 회송(부분 diff 금지). diff가 안 돌므로 missing도 안 돈다(안전 방향). 검증 V1-11/V2-9.
- `or snap`: DATE_HINTS에 안 걸리는 리스트(날짜 표기가 특이한 경우)도 스냅샷 헤더만으로 분석 진입.

### 2-B. `linkbot\state.py`

기존 kv/tracked/flagged/`sched_n` API 무변경. **[flow]의 `load_snap`/`save_snap`/`reset_snap`/`set_snap_msg` 명칭 폐기**, [state]의 `reg_*` 채택(아래가 최종). 신규 함수는 bot에서 전부 `asyncio.to_thread`로 호출.

#### (B-1) `_con()`에 테이블 4개 추가

```python
    c.execute("""CREATE TABLE IF NOT EXISTS snap_reg (
        channel_id        INTEGER NOT NULL,
        row_key           TEXT    NOT NULL,      -- 시트 _key "mid#i" — 생성 후 불변
        src_mid           INTEGER NOT NULL,
        canon             TEXT    NOT NULL,      -- 매칭 1단 키 (enroll 재등록 시엔 재계산 허용)
        task_text         TEXT    NOT NULL,      -- 최근 원문 (2단 LLM 제시용)
        date_text         TEXT    NOT NULL DEFAULT '',
        status            TEXT    NOT NULL DEFAULT '진행중',   -- '진행중'|'완료'
        managed           INTEGER NOT NULL DEFAULT 0,          -- 1=missing 카운트 대상
        missing_count     INTEGER NOT NULL DEFAULT 0,
        last_missing_date TEXT    NOT NULL DEFAULT '',         -- KST YYYY-MM-DD 하루 1회 가드
        last_seen_snap    INTEGER NOT NULL DEFAULT 0,
        completed_by      TEXT    NOT NULL DEFAULT '',         -- 'snap:<mid>'|'reply:<mid>'|''
        prev_missing      INTEGER NOT NULL DEFAULT 0,
        prev_status       TEXT    NOT NULL DEFAULT '',
        prev_missing_date TEXT    NOT NULL DEFAULT '',
        updated_at        REAL    NOT NULL,
        PRIMARY KEY (channel_id, row_key))""")
    c.execute("CREATE INDEX IF NOT EXISTS ix_snapreg_canon ON snap_reg(channel_id, canon)")
    c.execute("CREATE INDEX IF NOT EXISTS ix_snapreg_src ON snap_reg(src_mid)")
    c.execute("""CREATE TABLE IF NOT EXISTS snap_meta (
        channel_id INTEGER PRIMARY KEY, last_snap_mid INTEGER NOT NULL DEFAULT 0,
        last_snap_ts REAL NOT NULL DEFAULT 0, updated_at REAL NOT NULL)""")
    c.execute("""CREATE TABLE IF NOT EXISTS snap_msgs (
        mid INTEGER PRIMARY KEY, channel_id INTEGER NOT NULL, ts REAL NOT NULL,
        kind TEXT NOT NULL DEFAULT 'snap')""")          -- 'snap'|'alert'(자동완료 알림)
    c.execute("""CREATE TABLE IF NOT EXISTS sheet_outbox (
        id INTEGER PRIMARY KEY AUTOINCREMENT, kind TEXT NOT NULL,   -- 'done'|'del'
        tab TEXT NOT NULL, row_key TEXT NOT NULL, created REAL NOT NULL,
        UNIQUE(kind, tab, row_key))""")
```

#### (B-2) 조회·마킹·편입 API

```python
_REG_COLS = ("channel_id,row_key,src_mid,canon,task_text,date_text,status,managed,"
             "missing_count,last_missing_date,last_seen_snap,completed_by,"
             "prev_missing,prev_status,prev_missing_date,updated_at")

def _row2d(r):
    return dict(zip(_REG_COLS.split(","), r))

def reg_channel_items(channel_id, active_only=False):
    """매칭 후보. 기본 = 완료 포함(재활성 정책이 완료 행을 봐야 함 — §1-4).
    정렬은 불변 키(src_mid,row_key) — replay 시 2단 프롬프트 후보 번호 재현성(검증 V3-11)."""
    c = _con()
    q = f"SELECT {_REG_COLS} FROM snap_reg WHERE channel_id=?"
    if active_only:
        q += " AND status='진행중'"
    rows = c.execute(q + " ORDER BY src_mid, row_key", (channel_id,)).fetchall()
    c.close()
    return [_row2d(r) for r in rows]

def reg_find_by_canon_all(channel_id, canon, active_only=True):
    """canon 완전일치 전건 반환(fetchone 금지 — 쌍둥이 임의 병합 방지, 검증 V1-3c/V3-9)."""
    c = _con()
    q = f"SELECT {_REG_COLS} FROM snap_reg WHERE channel_id=? AND canon=?"
    if active_only:
        q += " AND status='진행중'"
    rows = c.execute(q, (channel_id, canon)).fetchall()
    c.close()
    return [_row2d(r) for r in rows]

def reg_items_by_src(src_mid):
    c = _con()
    rows = c.execute(f"SELECT {_REG_COLS} FROM snap_reg WHERE src_mid=?", (int(src_mid),)).fetchall()
    c.close()
    return [_row2d(r) for r in rows]

def reg_meta(channel_id):
    c = _con()
    r = c.execute("SELECT last_snap_mid, last_snap_ts FROM snap_meta WHERE channel_id=?",
                  (channel_id,)).fetchone()
    c.close()
    return (r[0], r[1]) if r else (0, 0.0)

def is_snap_msg(mid):
    c = _con()
    r = c.execute("SELECT 1 FROM snap_msgs WHERE mid=? AND kind='snap'", (int(mid),)).fetchone()
    c.close()
    return r is not None

def snap_msg_channel(mid):
    """답장 라우팅용 — kind 불문(스냅샷+자동완료 알림 둘 다 레지스트리로 라우팅)."""
    c = _con()
    r = c.execute("SELECT channel_id FROM snap_msgs WHERE mid=?", (int(mid),)).fetchone()
    c.close()
    return r[0] if r else None

def snap_msg_add(mid, channel_id, kind="snap"):
    c = _con()
    c.execute("INSERT OR REPLACE INTO snap_msgs VALUES (?,?,?,?)",
              (int(mid), channel_id, time.time(), kind))
    c.commit(); c.close()

def reg_enroll(channel_id, row_key, canon, task_text, date_text="", status="진행중", managed=0):
    """비스냅샷 경로 편입. 재등록(edit)이면 canon도 재계산값으로 갱신(검증 V2-8) — missing류는 보존."""
    now = time.time()
    c = _con()
    c.execute("""INSERT INTO snap_reg (channel_id,row_key,src_mid,canon,task_text,date_text,status,
                   managed,missing_count,last_missing_date,last_seen_snap,completed_by,
                   prev_missing,prev_status,prev_missing_date,updated_at)
                 VALUES (?,?,?,?,?,?,?,?,0,'',0,'',0,'','',?)
                 ON CONFLICT(channel_id,row_key) DO UPDATE SET
                   canon=excluded.canon, task_text=excluded.task_text,
                   date_text=excluded.date_text, status=excluded.status,
                   updated_at=excluded.updated_at""",
              (channel_id, row_key, int(str(row_key).split("#")[0]), canon, task_text,
               date_text, status, int(managed), now))
    c.commit(); c.close()

def reg_delete(channel_id, row_keys):
    if not row_keys:
        return
    c = _con()
    c.executemany("DELETE FROM snap_reg WHERE channel_id=? AND row_key=?",
                  [(channel_id, k) for k in row_keys])
    c.commit(); c.close()

def reg_set_status(channel_id, row_keys, status, source):
    """답장 완료/부활 훅. status='완료'→completed_by=source, '진행중'→completed_by='' (부활)."""
    now = time.time()
    c = _con()
    c.executemany("""UPDATE snap_reg SET status=?, completed_by=?, updated_at=?
                     WHERE channel_id=? AND row_key=?""",
                  [(status, source if status == "완료" else "", now, channel_id, k)
                   for k in row_keys])
    c.commit(); c.close()

def reg_reset_channel(channel_id):
    """force 재구축용 초기화."""
    c = _con()
    c.execute("DELETE FROM snap_reg WHERE channel_id=?", (channel_id,))
    c.execute("DELETE FROM snap_meta WHERE channel_id=?", (channel_id,))
    c.execute("DELETE FROM snap_msgs WHERE channel_id=?", (channel_id,))
    c.commit(); c.close()

def reg_recanon(canon_fn, ver):
    """canon 버전업 시 전량 재계산 (on_ready에서 kv 'canon_ver' 불일치 시 1회)."""
    c = _con()
    rows = c.execute("SELECT channel_id,row_key,task_text FROM snap_reg").fetchall()
    for ch, rk, tt in rows:
        c.execute("UPDATE snap_reg SET canon=? WHERE channel_id=? AND row_key=?",
                  (canon_fn(tt), ch, rk))
    c.commit(); c.close()
    set_kv("canon_ver", ver)

def outbox_pending(limit=50):
    c = _con()
    rows = c.execute("SELECT id, kind, tab, row_key FROM sheet_outbox ORDER BY id LIMIT ?",
                     (limit,)).fetchall()
    c.close()
    return [{"id": r[0], "kind": r[1], "tab": r[2], "row_key": r[3]} for r in rows]

def outbox_del(ids):
    if not ids:
        return
    c = _con()
    c.executemany("DELETE FROM sheet_outbox WHERE id=?", [(i,) for i in ids])
    c.commit(); c.close()
```

#### (B-3) 핵심: `reg_apply_snapshot` — 단일 트랜잭션 diff 적용 ([state] 원안에서 5곳 수정)

수정점: (1) `matched∩new` 충돌 검사 추가, (2) missing 조건에서 `last_seen_snap !=` 제거 → `NOT IN (matched∪new)`로 교체(검증 V1-8/V2-6), (3) `deleted_new`에서 완료 행 제외(V2-7), (4) 신규 INSERT를 `OR REPLACE` → 일반 INSERT(충돌=예외, V1-5), (5) 자동완료/삭제를 같은 트랜잭션에서 outbox 적재(V1-6), (6) `allow_missing` 파라미터(§1-4 가드).

```python
def reg_apply_snapshot(channel_id, snap_mid, snap_kst_date, matched, new_items, allow_missing=True):
    """matched=[{row_key,task_text,date_text,status}] (재활성 정책 적용 후 최종값 — bot이 계산)
    new_items=[{row_key,canon,task_text,date_text,status}] (row_key는 bot이 HW 인덱스로 부여)
    반환 {"mode":'new'|'replay'|'stale', "auto_completed":[...], "deleted_new":[...]}"""
    snap_mid = int(snap_mid)
    now = time.time()
    matched_keys = {m["row_key"] for m in matched}
    new_keys = {n["row_key"] for n in new_items}
    if matched_keys & new_keys:
        raise ValueError(f"snapshot key collision: {matched_keys & new_keys}")
    c = _con()
    try:
        c.execute("BEGIN IMMEDIATE")
        r = c.execute("SELECT last_snap_mid FROM snap_meta WHERE channel_id=?",
                      (channel_id,)).fetchone()
        last = r[0] if r else 0
        if snap_mid < last:
            c.execute("ROLLBACK"); c.close()
            return {"mode": "stale", "auto_completed": [], "deleted_new": []}
        mode = "new" if snap_mid > last else "replay"
        deleted_new = []
        if mode == "new":
            c.execute("""UPDATE snap_reg SET prev_missing=missing_count, prev_status=status,
                         prev_missing_date=last_missing_date WHERE channel_id=?""", (channel_id,))
        else:   # replay: 되감기 (답장 완료 보호) 후 재계산
            c.execute("""UPDATE snap_reg SET missing_count=prev_missing,
                         status=CASE WHEN completed_by LIKE 'reply:%' THEN status ELSE prev_status END,
                         last_missing_date=prev_missing_date,
                         completed_by=CASE WHEN completed_by=? THEN '' ELSE completed_by END
                         WHERE channel_id=? AND prev_status != ''""",
                      (f"snap:{snap_mid}", channel_id))
            olds = c.execute("SELECT row_key, status FROM snap_reg WHERE channel_id=? AND src_mid=?",
                             (channel_id, snap_mid)).fetchall()
            deleted_new = [k for k, st in olds
                           if k not in new_keys and k not in matched_keys and st != "완료"]
            if deleted_new:
                c.executemany("DELETE FROM snap_reg WHERE channel_id=? AND row_key=?",
                              [(channel_id, k) for k in deleted_new])
        for m in matched:
            c.execute("""UPDATE snap_reg SET task_text=?, date_text=?, status=?, managed=1,
                         missing_count=0, last_seen_snap=?, updated_at=?,
                         completed_by=CASE WHEN ?='진행중' THEN '' ELSE completed_by END
                         WHERE channel_id=? AND row_key=?""",
                      (m["task_text"], m["date_text"], m["status"], snap_mid, now,
                       m["status"], channel_id, m["row_key"]))
        auto_completed = []
        if allow_missing:
            keep = matched_keys | new_keys
            ph = ",".join("?" * len(keep)) if keep else "''"
            c.execute(f"""UPDATE snap_reg SET missing_count=missing_count+1,
                          last_missing_date=?, updated_at=?
                          WHERE channel_id=? AND managed=1 AND status='진행중'
                            AND last_missing_date != ? AND row_key NOT IN ({ph})""",
                      (snap_kst_date, now, channel_id, snap_kst_date, *keep))
            rows = c.execute("""SELECT row_key FROM snap_reg WHERE channel_id=? AND managed=1
                                AND status='진행중' AND missing_count>=2""", (channel_id,)).fetchall()
            auto_completed = [x[0] for x in rows]
            if auto_completed:
                c.executemany("""UPDATE snap_reg SET status='완료', completed_by=?, updated_at=?
                                 WHERE channel_id=? AND row_key=?""",
                              [(f"snap:{snap_mid}", now, channel_id, k) for k in auto_completed])
        for n in new_items:
            c.execute("""INSERT INTO snap_reg (channel_id,row_key,src_mid,canon,task_text,date_text,
                          status,managed,missing_count,last_missing_date,last_seen_snap,completed_by,
                          prev_missing,prev_status,prev_missing_date,updated_at)
                         VALUES (?,?,?,?,?,?,?,1,0,'',?,'',0,?,'',?)""",
                      (channel_id, n["row_key"], snap_mid, n["canon"], n["task_text"],
                       n["date_text"], n["status"], snap_mid, n["status"], now))
        for k in auto_completed:
            c.execute("INSERT OR IGNORE INTO sheet_outbox (kind,tab,row_key,created) VALUES ('done','일정',?,?)", (k, now))
        for k in deleted_new:
            c.execute("INSERT OR IGNORE INTO sheet_outbox (kind,tab,row_key,created) VALUES ('del','일정',?,?)", (k, now))
        c.execute("""INSERT INTO snap_meta VALUES (?,?,?,?)
                     ON CONFLICT(channel_id) DO UPDATE SET last_snap_mid=excluded.last_snap_mid,
                       last_snap_ts=excluded.last_snap_ts, updated_at=excluded.updated_at""",
                  (channel_id, snap_mid, now, now))
        c.execute("INSERT OR REPLACE INTO snap_msgs VALUES (?,?,?,'snap')",
                  (snap_mid, channel_id, now))
        c.execute("COMMIT")
    except Exception:
        try:
            c.execute("ROLLBACK")
        finally:
            c.close()
        raise
    c.close()
    return {"mode": mode, "auto_completed": auto_completed, "deleted_new": deleted_new}
```

#### (B-4) 스냅샷 철회 (edit로 추출 0건 시 — C 경로)

```python
def reg_revoke_snapshot(channel_id, snap_mid):
    """최신 스냅샷 edit-철회: prev_* 복원 + 자기 신규행 제거(**완료 행은 보존** — V3-10).
    snap_meta는 유지(더 오래된 스냅샷 역주입 차단). snap_msgs도 유지(잔여 답장 라우팅).
    반환: 시트에서 지울 row_key 목록(완료 제외)."""
    snap_mid = int(snap_mid)
    c = _con()
    c.execute("BEGIN IMMEDIATE")
    c.execute("""UPDATE snap_reg SET missing_count=prev_missing,
                 status=CASE WHEN completed_by LIKE 'reply:%' THEN status ELSE prev_status END,
                 last_missing_date=prev_missing_date,
                 completed_by=CASE WHEN completed_by=? THEN '' ELSE completed_by END
                 WHERE channel_id=? AND prev_status != ''""", (f"snap:{snap_mid}", channel_id))
    olds = c.execute("SELECT row_key, status FROM snap_reg WHERE channel_id=? AND src_mid=?",
                     (channel_id, snap_mid)).fetchall()
    keys = [k for k, st in olds if st != "완료"]
    if keys:
        c.executemany("DELETE FROM snap_reg WHERE channel_id=? AND row_key=?",
                      [(channel_id, k) for k in keys])
    c.execute("COMMIT"); c.close()
    return keys
```

`import time`을 state.py 상단에 추가. 레지스트리 불변식(최종):
1. `row_key` 불변·`canon`은 matched 갱신 시 불변 / **enroll 재등록 시 재계산**(V2-8 반영으로 [state] 불변식2 완화).
2. `last_snap_mid` 단조증가만 허용. `prev_*`는 new 모드 진입 시에만 캡처(1세대 undo).
3. 스냅샷 메시지의 `sched_n:{S}` = **신규 인덱스 high-water mark**(다음 할당 시작점). "#0..#(n-1) 연속" 불변식은 비스냅샷 메시지에만 존속. 스냅샷 소유 행의 진실은 `reg_items_by_src(S)`.
4. 레지스트리는 리스트 구조(canon 중복 공존 허용). `task_text` 원문 보존. 상한 200행/채널(초과 시 완료·오래된 순 정리 — `reg_prune`, 구현 선택).

### 2-C. `linkbot\bot.py`

무변경: `on_message`, `on_raw_message_edit`, `catchup`(코드), `_review_reply`, 정산 경로, `alert`/`alert_err`/`clear_err`, 단일 인스턴스 락.

#### (C-1) 공용 헬퍼 (모듈 상단, `flagged` 선언부 근처)

```python
_chan_locks = defaultdict(asyncio.Lock)     # 채널별 직렬화 (§1-6-1)

def _kst_date(msg):
    return (msg.created_at + timedelta(hours=9)).strftime("%Y-%m-%d")

def _is_snapshot(msg_id, content, items):
    if state.is_snap_msg(msg_id):
        return True                          # sticky (재추출 흔들려도 스냅샷 유지)
    return bool(brain.SNAP_HINT_RE.search(content or "")) and len(items) >= 2

def _snap_hw(mid):
    """스냅샷 신규 키 시작 인덱스 (재사용 금지 단조 할당)."""
    idx = [int(r["row_key"].rsplit("#", 1)[1]) + 1 for r in state.reg_items_by_src(mid)]
    return max([state.get_msg_item_count(mid), 0] + idx)

async def _drain_outbox():
    ops = await asyncio.to_thread(state.outbox_pending)
    done_ids = []
    for op in ops:
        if op["kind"] == "done":
            r = await asyncio.to_thread(sheet.push, op["tab"],
                    [{"key": op["row_key"], "fields": {"상태": "완료"}}], "merge")
        else:   # 'del'
            r = await asyncio.to_thread(sheet.delete_rows, op["tab"], [op["row_key"]])
        if not (isinstance(r, str) and r.startswith("err")):
            done_ids.append(op["id"])        # ok:0 포함 = 행 이미 없음 = 목적 달성
    await asyncio.to_thread(state.outbox_del, done_ids)
```

#### (C-2) 신규 `_apply_snapshot(msg, items, link)` — 스냅샷 diff 본체

```python
async def _apply_snapshot(msg, items, link):
    ch_id = msg.channel.id
    async with _chan_locks[ch_id]:
        await asyncio.to_thread(state.snap_msg_add, msg.id, ch_id, "snap")   # 선등록 (V2-14)
        items = brain.merge_snapshot_items(items)
        cands = await asyncio.to_thread(state.reg_channel_items, ch_id)      # 완료 포함
        try:
            idx = await asyncio.to_thread(
                brain.match_snapshot,
                [{"canon": c["canon"], "task": c["task_text"]} for c in cands],
                [it["작업내용"] for it in items])
        except Exception:
            log.exception("스냅샷 매칭 실패")
            await alert_err("ollama", "⚠️ 스냅샷 매칭 실패 — Ollama 확인 필요", threshold=2)
            return          # diff 전체 중단 (부분 반영 금지)
        clear_err("ollama")

        matched, new_raw = [], []
        for j, it in enumerate(items):
            i = idx[j]
            if i is None:
                if brain._is_progline(it):
                    log.info(f"스냅샷 진행줄 미매칭 → 행 미생성: {it['작업내용'][:30]}")
                    continue                                   # F: 새 행 금지
                new_raw.append(it)
                continue
            c = cands[i]
            st = it.get("상태") or "진행중"
            if c["status"] == "완료" and st != "완료":         # 재활성 정책 (§1-4)
                revive = (c["completed_by"].startswith("snap:")
                          or bool(it.get("진행내용"))
                          or bool(it.get("날짜") and it["날짜"] != c["date_text"]))
                st = "진행중" if revive else "완료"
            matched.append({
                "row_key": c["row_key"], "task_text": it["작업내용"],
                "date_text": it.get("날짜") or c["date_text"], "status": st,
                "_prev_status": c["status"], "_prog": it.get("진행내용", ""),
                "_date_new": it.get("날짜") or "", "_owner": it.get("담당자", "")})

        hw = _snap_hw(msg.id)
        new_items = []
        for k, it in enumerate(new_raw):
            new_items.append({
                "row_key": f"{msg.id}#{hw + k}", "canon": brain.canon(it["작업내용"]),
                "task_text": it["작업내용"], "date_text": it.get("날짜") or "",
                "status": it.get("상태") or "진행중", "_item": it})

        active_managed = sum(1 for c in cands if c["managed"] and c["status"] == "진행중")
        truncated = len(msg.content or "") > brain.SNAP_BODY_MAX
        allow_missing = (not truncated) and not (len(matched) == 0 and active_managed >= 3)

        try:
            res = await asyncio.to_thread(
                state.reg_apply_snapshot, ch_id, msg.id, _kst_date(msg),
                [{k2: m[k2] for k2 in ("row_key", "task_text", "date_text", "status")} for m in matched],
                [{k2: n[k2] for k2 in ("row_key", "canon", "task_text", "date_text", "status")} for n in new_items],
                allow_missing)
        except Exception:
            log.exception("reg_apply_snapshot 실패")
            return
        if res["mode"] == "stale":
            log.info(f"스냅샷 stale skip: {msg.id}")
            return

        push = []
        for m in matched:                    # §1-5 필드 규칙
            f = {"작업내용": m["task_text"], "담당자": m["_owner"], "링크": link}
            if m["_date_new"]:
                f["날짜"] = m["_date_new"]
            if m["_prog"]:
                f["진행내용"] = m["_prog"]
            if m["status"] != m["_prev_status"]:
                f["상태"] = m["status"]      # 전이 시에만 (PM 수동 상태 보존)
            push.append({"key": m["row_key"], "fields": f})
        for n in new_items:
            f = dict(n["_item"]); f["링크"] = link
            push.append({"key": n["row_key"], "fields": f})
        if push:
            r = await asyncio.to_thread(sheet.push, "일정", push, "upsert")
            if isinstance(r, str) and r.startswith("err"):
                await alert_err("push", f"⚠️ 시트 전송 실패: {r[:100]}")
                # 레지스트리는 커밋됨 → 다음 스냅샷 upsert가 자가치유 (§1-5)
        state.set_msg_item_count(msg.id, hw + len(new_items))   # = 새 HW

        await _drain_outbox()                # auto_completed merge / deleted_new delete
        if res["auto_completed"]:
            names = [c["task_text"] for c in cands if c["row_key"] in set(res["auto_completed"])]
            try:
                m2 = await msg.channel.send(
                    "📋 전체일정에서 2일 연속 빠져 완료 처리했어요: "
                    + ", ".join(f"『{t[:24]}』" for t in names[:5])
                    + "\n아직 진행 중이면 이 메시지에 답장해 주세요.")
                await asyncio.to_thread(state.snap_msg_add, m2.id, ch_id, "alert")
            except Exception:
                log.exception("자동완료 알림 실패")
        log.info(f"-> 스냅샷 {res['mode']}: 매칭 {len(matched)} / 신규 {len(new_items)} / "
                 f"자동완료 {len(res['auto_completed'])} / 정리 {len(res['deleted_new'])}")
```

#### (C-3) `process()` 일정 분기 수정 (bot.py:162-179)

```python
    if tab == "일정":
        items = res.get("items", [])
        if not items:
            if is_edit:
                await _clear_schedules(msg)
            return False
        if _is_snapshot(msg.id, msg.content, items):
            await _apply_snapshot(msg, items, link)
            return True                      # 이모지 미부착(매일 리스트 스팸 방지 — 의도된 변화)
        async with _chan_locks[msg.channel.id]:
            await _reconcile_schedules(msg, items, link)
            _reg_add_rows(msg, items)        # 비스냅샷 편입 (C-4)
        # 이모지 블록 기존 그대로 ...
```

#### (C-4) 비스냅샷 편입 `_reg_add_rows` + `_reconcile_schedules`/`_clear_schedules` 훅

```python
def _reg_add_rows(msg, items):
    """일반 메시지가 만든 행을 레지스트리에 편입(managed=0). 이후 스냅샷이 언급하면 matched로 입양.
    동일 canon 활성 행(다른 메시지 소유)이 이미 있으면 편입 생략 — 쌍둥이로 1단 유일성 파괴 방지(V3-13)."""
    for i, it in enumerate(items):
        cn = brain.canon(it.get("작업내용", ""))
        if cn:
            hits = state.reg_find_by_canon_all(msg.channel.id, cn)
            if any(not h["row_key"].startswith(f"{msg.id}#") for h in hits):
                log.info(f"reg 편입 생략(canon 중복): {it.get('작업내용','')[:30]}")
                continue
        state.reg_enroll(msg.channel.id, f"{msg.id}#{i}", cn, it.get("작업내용", ""),
                         it.get("날짜", "") or "", it.get("상태", "진행중"), managed=0)
```

- `_reconcile_schedules` 끝의 초과분 삭제 블록에 1줄 추가: `state.reg_delete(msg.channel.id, over)` (시트-레지스트리 동기).
- `_clear_schedules` 수정: 맨 앞에 스냅샷 분기 —
```python
    if state.is_snap_msg(msg.id):            # 스냅샷 철회 (edit 0건/비일정 전이)
        keys = await asyncio.to_thread(state.reg_revoke_snapshot, msg.channel.id, msg.id)
        if keys:
            await asyncio.to_thread(sheet.delete_rows, "일정", keys)
        state.set_msg_item_count(msg.id, 0)
        return
    # 이하 기존 range(prev) 삭제 경로 + state.reg_delete(msg.channel.id, keys) 1줄 추가
```

#### (C-5) `_apply_reply_update` 수정 (bot.py:247) — 스냅샷 라우팅 선점

함수 맨 앞(`n = state.get_msg_item_count(ref_id)` **앞**)에 추가:

```python
    snap_ch = state.snap_msg_channel(ref_id)
    if snap_ch is not None:
        if brain.SNAP_HINT_RE.search(reply_msg.content or ""):
            return (False, False)            # 답장 본문이 새 스냅샷 → process가 diff (V2-12)
        return await _route_snapshot_reply(reply_msg, ref_id, snap_ch)
```

기존 경로(불변)에 레지스트리 완료 훅 2줄(V1-9):
- `n == 1` 성공 merge 뒤: `state.reg_set_status(reply_msg.channel.id, [f"{ref_id}#0"], fields["상태"], f"reply:{reply_msg.id}")` (레지스트리에 없는 키면 no-op).
- `n >= 2` done 일괄 merge 뒤: `state.reg_set_status(reply_msg.channel.id, [f"{ref_id}#{i}" for i in range(n)], "완료", f"reply:{reply_msg.id}")`.

#### (C-6) 신규 `_route_snapshot_reply(msg, ref_id, ch_id)` — B 최종

```python
async def _route_snapshot_reply(msg, ref_id, ch_id):
    try:
        upd = await asyncio.to_thread(brain.extract_progress, msg.content)
    except Exception:
        await alert_err("ollama", "⚠️ 진행 추출 실패 — Ollama 확인 필요", threshold=2)
        return (False, False)
    clear_err("ollama")
    if not upd or not upd.get("진행내용"):
        return (False, False)                # ㅇㅋ류 → 미소비 (기존 관례)
    prog = upd["진행내용"]
    done = brain.detect_status(prog) == "완료"
    link = jump(msg.guild.id, msg.channel.id, msg.id)
    async with _chan_locks[ch_id]:
        cands = await asyncio.to_thread(state.reg_channel_items, ch_id)      # 완료 포함(부활 지원)
        if not cands:
            return (False, False)
        try:
            scope, i = await asyncio.to_thread(
                brain.route_reply, [{"task": c["task_text"]} for c in cands], msg.content)
        except Exception:
            await alert_err("ollama", "⚠️ 답장 라우팅 실패 — Ollama 확인 필요", threshold=2)
            return (False, False)
        if scope == "one":
            c = cands[i]
            st = "완료" if done else "진행중"
            fields = {"진행내용": prog, "상태": st}
            if upd.get("날짜"):
                fields["날짜"] = upd["날짜"]
            r = await asyncio.to_thread(sheet.push, "일정",
                                        [{"key": c["row_key"], "fields": fields}], "merge")
            if isinstance(r, str) and r.endswith(":0"):
                await _review_reply(msg, ref_id, "[수동] 답장 대상 일정 행 없음 — 시트 확인", link)
                return (True, done)
            await asyncio.to_thread(state.reg_set_status, ch_id, [c["row_key"]], st,
                                    f"reply:{msg.id}")     # 완료 기록/부활 (replay 보호 근거)
            return (True, done)
        if scope == "all" and done:
            tgt = [c for c in cands if c["status"] == "진행중"
                   and (c["last_seen_snap"] == ref_id or c["src_mid"] == ref_id)]
            if not tgt:
                tgt = [c for c in cands if c["status"] == "진행중" and c["managed"]]
            keys = [c["row_key"] for c in tgt]
            if keys:
                await asyncio.to_thread(sheet.push, "일정",
                        [{"key": k, "fields": {"상태": "완료"}} for k in keys], "merge")
                await asyncio.to_thread(state.reg_set_status, ch_id, keys, "완료",
                                        f"reply:{msg.id}")
            return (True, True)
        await _review_reply(msg, ref_id, "[수동] 스냅샷 답장 모호 — 시트에서 직접 갱신", link)
        return (True, False)
```

#### (C-7) `_is_past` 수정 (bot.py:439) — 범위는 **끝 날짜** 기준 (검증 V3-5)

```python
def _is_past(date_str, today):
    s = (date_str or "")
    if "~" in s:
        s = s.split("~")[-1].strip()         # "2026-06-28 ~ 2026-07-15" → 끝 날짜로 판정
    m = re.match(r"(\d{4})-(\d{2})-(\d{2})", s)
    if not m:
        return False
    try:
        return datetime(int(m.group(1)), int(m.group(2)), int(m.group(3))).date() < today
    except ValueError:
        return False
```

#### (C-8) `backfill_cmd` 수정 (bot.py:463)

1. **skip 조건 확장**: `if not force and (state.get_msg_item_count(msg.id) > 0 or state.is_snap_msg(msg.id)):` — 신규행 0건 스냅샷도 재분석 없이 skip.
2. **stale 스냅샷 cheap-skip** (LLM 예산 보호, V3-12): analyze 호출 전에
   `if not force and brain.SNAP_HINT_RE.search(msg.content or "") and msg.id < state.reg_meta(ch.id)[0]: dup += 1; continue`
3. **force 채널 절차** (V2-11 반영 — [state] §4-2-6의 "force 없이 이어서" **폐기**): 채널 루프 진입 직후
   ```python
   if force:
       done_at = float(state.get_kv(f"bf_done:{ch.id}", 0) or 0)
       if time.time() - done_at < 21600:      # 6h 내 완료 채널 → 통째 skip (이어하기)
           continue
       state.reg_reset_channel(ch.id)         # 리셋은 채널 재생 직전, 채널 단위
   ```
   채널을 상한 없이 끝까지 재생했으면 `state.set_kv(f"bf_done:{ch.id}", time.time())`. 상한 도달 채널은 마커 없이 중단.
4. **처리 위임**: 일정 판정 후
   ```python
   items = res.get("items", [])
   if _is_snapshot(msg.id, msg.content, items):
       await _apply_snapshot(msg, items, jump(g.id, ch.id, msg.id))   # 과거날짜 필터 면제 (V3-5)
       added += len(items)
       continue
   fut = [it for it in items if not _is_past(it.get("날짜"), today)]
   # 재생 경로 canon 중복 스킵(다운타임 재생 이중 생성 방지, V1-10):
   fut = [it for it in fut if not _canon_dup_foreign(ch.id, msg.id, it)]
   ...기존 _reconcile_schedules 호출... + _reg_add_rows(msg, fut)
   ```
   ```python
   def _canon_dup_foreign(ch_id, mid, it):
       cn = brain.canon(it.get("작업내용", ""))
       if not cn:
           return False
       hits = state.reg_find_by_canon_all(ch_id, cn)
       return any(not h["row_key"].startswith(f"{mid}#") for h in hits)
   ```
5. tail 문구 교체: `" (상한 도달 — 같은 명령(force 포함)으로 재실행하면 완료된 채널은 건너뛰고 이어서)"`. followup에 `스냅샷 diff 건수` 카운트 추가(선택).
6. catchup(라이브 재생)의 비스냅샷 경로는 process 경유라 4의 `_canon_dup_foreign` 스킵이 자동 적용되지 않는다 — process의 `_reg_add_rows`가 편입만 생략(행 생성은 현행 유지). 재생 이중 생성의 주 방어는 기존 멱등(같은 키 upsert)이며, 스냅샷이 먼저 만든 행과의 중복은 다음 스냅샷 diff에서 canon 쌍둥이 → 2단 LLM → 한쪽만 matched → 남은 쪽은 managed=0으로 잔존(시트 정리는 PM/force) — 한계로 §8 기록.

#### (C-9) `resolve_loop` 일정 등록 블록 수정 (bot.py:398-410) — E 최종

```python
            if tab == "일정":
                ch_id = _channel_from_link(link)         # 신규 헬퍼 (아래)
                push_items, merged = [], 0
                for i, f in enumerate(payload):
                    f = dict(f); f["담당자"] = worker; f["링크"] = link
                    cn = brain.canon(f.get("작업내용", ""))
                    hits = state.reg_find_by_canon_all(ch_id, cn) if (ch_id and cn) else []
                    if len(hits) == 1:                   # 유일 일치만 병합 (2건 이상 → 새 행, V3-9)
                        r1 = await asyncio.to_thread(sheet.push, "일정",
                                [{"key": hits[0]["row_key"], "fields": f}], "upsert")
                        if isinstance(r1, str) and r1.startswith("ok"):
                            merged += 1
                            continue
                    push_items.append({"key": None, "fields": f})     # 키는 아래서 연번 부여
                for i, it in enumerate(push_items):
                    it["key"] = f"{base}#{i}"
                r = await asyncio.to_thread(sheet.push, "일정", push_items, "upsert") if push_items else "ok:0"
                ok = (not push_items) or (isinstance(r, str) and r.startswith("ok") and not r.endswith(":0"))
                if ok:
                    if base.isdigit():
                        state.set_msg_item_count(int(base), len(push_items))
                    if ch_id:
                        for it in push_items:            # 신규분 편입 → 이후 스냅샷과 매칭 (E)
                            state.reg_enroll(ch_id, it["key"], brain.canon(it["fields"]["작업내용"]),
                                             it["fields"]["작업내용"], it["fields"].get("날짜", "") or "",
                                             it["fields"].get("상태", "진행중"), managed=0)
                    else:
                        log.warning(f"resolve: 링크 파싱 실패 → 레지스트리 미편입 {base}")
                    delete_keys.append(key)
                    log.info(f"resolve(등록->일정 {len(push_items)}건/병합 {merged}): {base} / {worker}")
                else:
                    await alert_err("confirm_push", f"⚠️ 등록 실패(삭제 보류): {str(r)[:100]}")
```

```python
_LINK_RE = re.compile(r"discord\.com/channels/(\d+)/(\d+)/(\d+)")

def _channel_from_link(link):
    m = _LINK_RE.search(link or "")
    return int(m.group(2)) if m else None
```

또한 `resolve_loop` 본문 시작부(fetch 전)에 `await _drain_outbox()` 1줄 추가(outbox 주기 재시도).

#### (C-10) `on_ready` 추가 (canon 버전 마이그레이션)

```python
    if str(state.get_kv("canon_ver", "")) != str(brain.CANON_VERSION):
        await asyncio.to_thread(state.reg_recanon, brain.canon, brain.CANON_VERSION)
```

### 2-D. `linkbot\sheet.py` — **무변경**

### 2-E. `extract\AppsScript.gs` — **무변경 확정**

| 필요 연산 | 사용 계약 (현행 그대로) |
|---|---|
| matched 갱신·자가치유 재생성 / 신규행 | `doPost mode=upsert` — 기존행 비공백만 덮기 + 미존재 생성 (:44-56) |
| 자동완료 / 답장 완료 | `doPost mode=merge` — 미존재행 skip=`ok:0` (:45) → outbox는 ok:0도 완료로 간주 |
| edit 축소·replay 정리 | `doDelete` — `_key` 완전일치 (:135) |
| 확인필요 read-back | `doGet` 화이트리스트 그대로(일정 read 불필요 — 진실원본=레지스트리) |
| force 초기화 | `clearSchedule()` 수동 1회 (:277) |

---

## 3) 상호작용 A~F 해소 규칙 (최종본)

### A. item_count 의미론
- **비스냅샷 메시지: 완전 불변** — `sched_n` = 항목 수, `#0..#(n-1)` 연속, 답장 n판정·edit reconcile·초과분 삭제 전부 기존 코드 그대로.
- **스냅샷 메시지: `sched_n:{S}` = 신규 인덱스 high-water mark**(첫 적용 시 = 신규행 수와 동일). 용도는 (a) 신규 키 할당 시작점 `_snap_hw`, (b) backfill "이미등록" skip(>0). 답장 n판정에는 도달하지 않는다 — `snap_msg_channel(ref)` 조회가 **n 판정보다 먼저** 라우팅을 선점(n==0인 스냅샷도 답장이 일반 process로 새지 않음). 스냅샷 소유 행의 정확한 집합은 언제나 `reg_items_by_src(S)`.
- edit reconcile: 스냅샷은 `_reconcile_schedules`를 타지 않으므로(스냅샷 분기가 먼저) 연속키·초과삭제 가정과 충돌 없음.

### B. 답장 라우팅 (`_route_snapshot_reply`)
- ref가 `snap_msgs`(kind 불문: 스냅샷·자동완료 알림)에 있으면 스냅샷 라우팅.
- 답장 본문이 SNAP_HINT면 **미소비** → process가 새 스냅샷으로 diff("어제 스냅샷에 답장으로 오늘 리스트" 패턴, V2-12).
- 진행 문구 추출(기존 `extract_progress`) → `route_reply`(exaone)로 대상 결정:
  - **one**: 해당 행 merge(진행내용+상태+날짜) + `reg_set_status`(완료면 `reply:{mid}` 기록 / 진행이면 부활). `ok:0`이면 `[수동]` 회송.
  - **all + 완료**: 그 스냅샷이 언급한 행(`last_seen_snap==ref or src_mid==ref`) 중 진행중 → 일괄 완료(비면 채널 활성 managed 전체로 폴백).
  - **all + 미완료 / none(모호)**: `[수동]` 확인필요 회송 (기존 관례 유지).
  - 진행 문구 없음(ㅇㅋ류): 미소비.
- **비스냅샷 답장(불변 경로)**에도 완료/갱신 성공 시 `reg_set_status` 훅 — 레지스트리가 완료를 알아야 다음날 스냅샷 matched가 완료를 뒤집지 않는다(재활성 정책의 입력, V1-9).

### C. 수정(edit)
- `on_raw_message_edit` 코드 무변경 → process(is_edit=True) 내부 분기:
  - **sticky**: `is_snap_msg` 참이면 항목 1건이어도 스냅샷 경로 → `_apply_snapshot`이 replay 모드로 되감기+재계산(missing 오증가·이중완료 구조적으로 불가). 신규 항목은 HW 인덱스(자기 행 하이재킹 불가).
  - 추출 0건/비일정 전이 → `_clear_schedules`의 스냅샷 분기 → `reg_revoke_snapshot`(완료 행 보존) + 반환 키 delete.
  - **과거 스냅샷(S < last_snap_mid) edit** → reg_apply_snapshot이 stale 반환 → 전체 무시. 운영 안내: "지난 리스트 수정은 반영 안 됨 — 최신 리스트를 새로 올리세요".
  - 일반 메시지를 edit로 스냅샷화 → 허용(승격). 기존 자기 행은 후보로서 matched(자기 키 유지), 추가분만 HW 신규.
- 한계(V2-6): 새 메시지 없이 **같은 스냅샷을 매일 edit로만 갱신**하는 작업자는 snap_mid 불변 → replay 반복 → missing이 1을 넘지 못해 b안 은퇴가 진행되지 않음. 코드 대응 없이 운영 안내("매일 새 메시지로 게시")로 확정. §8-3.

### D. catchup / 과거등록
- catchup 코드 무변경. 채널 내 `oldest_first` 재생 + stale/replay 게이트 + 하루 1회 missing 가드(KST=created_at 기준이라 재생 시점 무관) = 멱등. 라이브와의 경합은 채널 락 + stale 게이트가 방어(라이브가 먼저 커밋되면 과거분은 stale skip — 신규행도 안 만듦).
- 분류 고정: `is_snap_msg` 참 → 재생 시 무조건 스냅샷 경로. `sched_n>0`이고 snap_msgs에 없음 → 일반 경로 유지. 재분류는 edit에서만.
- backfill: skip 조건 확장(sched_n>0 OR is_snap_msg), stale 스냅샷 cheap-skip, 스냅샷은 과거날짜 필터 면제(§2-C-8), `_is_past`는 범위 끝 날짜 기준.
- force: 채널 단위 `reg_reset_channel` → oldest_first 재생으로 레지스트리·시트 동시 재구축(과거 중복 자동 붕괴, 히스토리 중간에 빠진 작업은 b안 재현). 상한 도달 시 **force로 재실행**(완료 채널은 `bf_done` 마커로 skip — "force 없이 이어서"는 pre-clear 마커 때문에 전량 skip되는 결함이 있어 폐기, V2-11).

### E. resolve_loop 편입
- 원본링크에서 channel_id 파싱 → push 전 canon **유일** 일치 시 기존 행에 upsert 병합(새 행 금지), 2건 이상/불일치/파싱 실패 → 새 행(+실패 시 편입 생략 로그).
- 신규분 push 성공 후 `reg_enroll(managed=0)` — 이후 스냅샷이 언급하는 순간 matched가 managed=1로 승격. `[수동]` 가드·`ok:0` 검사·삭제 보류 계약 불변.

### F. 진행 보고 줄 ([match]·[flow] 모순 단일화)
1. 스냅샷 내부 선흡수: `merge_snapshot_items` — canon 동일 그룹에서 **진행줄(날짜 없음+진행/완료)만** 날짜 항목에 흡수. 날짜 있는 canon 쌍둥이는 별개 유지(2단 LLM행).
2. 남은 진행줄은 레지스트리 매칭에 그대로 참여 → matched면 그 행의 진행내용/상태만 갱신(날짜는 라인에 없으므로 레지스트리 값 유지, 시트에도 날짜 미전송=보존), **missing 집계상 "언급"으로 카운트**(matched에 포함되므로 자연 성립).
3. 미매칭 진행줄 → **행 미생성 + 로그** ([flow] S6 채택, [match]의 "별건 행" 폐기 — 날짜 없는 행 스팸 방지).
4. 날짜 없고 진행 문구도 없는 항목은 일반 항목 취급(미매칭 시 새 행 허용 — 기존 비스냅샷 동작과 동일).

---

## 4) 운영 절차

### 4-1. 배포
1. 동시 배포 가능(GAS 무변경 → 순서 제약 없음): `state.py` → `brain.py` → `bot.py` 한 릴리스로 재기동. SQLite 테이블은 `_con()`이 idempotent CREATE. 마이그레이션 스크립트 불요.
2. 재기동 시 on_ready가 `canon_ver`를 확인해 필요 시 `reg_recanon` 1회(첫 배포 땐 레지스트리가 비어 있어 no-op).
3. `pip` 신규 의존성 없음(표준 라이브러리 `unicodedata`뿐). 신규 모델 없음(exaone3.5:7.8b 재활용 — 확정 결정 3).

### 4-2. 초기 리셋 재등록 (배포 직후 1회 권장 — 기존 시트 중복 정리, 확정 결정 4)
1. (권장) 조용한 시간대 + PM에게 "시트 수동편집 잠시 중단" 공지.
2. GAS 에디터에서 `clearSchedule()` 1회 실행(헤더·서식 유지 전삭제).
3. 디스코드 `/과거등록 category:<대상> days:60 force:True`.
   - 채널별로: `bf_done` 마커 6h 이내면 skip(이어하기) → 아니면 `reg_reset_channel` → oldest_first 재생(스냅샷은 diff, 비스냅샷은 기존 등록+편입).
   - 150콜 상한 도달 → 같은 명령(**force 포함**) 재실행 = 완료 채널 skip하고 이어서.
4. 검증: followup 카운트 + 시트 육안 대조(작업자별 행 수 = 마지막 스냅샷 항목 수 + 미스냅샷 단발 행).
5. 미실행 시에도 동작은 함 — 단 첫 스냅샷이 레지스트리 공백 때문에 전 항목을 신규행으로 만들어 기존 중복 위에 한 겹 더 쌓임(이후 증식은 없음).

### 4-3. 운영 규칙 (PM/작업자 안내)
- 작업자: 전체 리스트에는 "전체일정" 머리글 줄 유지. 갱신은 **매일 새 메시지**로(edit로만 갱신하면 빠진 항목 자동완료가 진행되지 않음). 같은 날 정정은 edit로.
- PM: 행 정리는 **삭제 대신 상태=완료**(삭제해도 스냅샷이 선언하는 한 재생성됨). 영구 정리는 clearSchedule+force.
- 자동완료 알림이 채널에 뜨면: 오처리일 경우 그 알림에 답장("아직 진행중이에요") → 해당 행 부활.

---

## 5) 구현 체크리스트

**Phase 0 — state.py**
- [ ] `import time` + `_con()` 테이블 4개(snap_reg/snap_meta/snap_msgs(kind)/sheet_outbox(UNIQUE)).
- [ ] 조회/마킹/편입 API 12종(§2-B-2) — `reg_channel_items`는 완료 포함·`ORDER BY src_mid,row_key`, `reg_find_by_canon_all`은 fetchall.
- [ ] `reg_apply_snapshot`(키 충돌 검사·missing NOT IN matched∪new·deleted_new 완료 제외·plain INSERT·outbox 동일 트랜잭션·allow_missing).
- [ ] `reg_revoke_snapshot`(완료 보존) / `reg_recanon` / `outbox_pending`/`outbox_del`.

**Phase 1 — brain.py**
- [ ] `import unicodedata`, `CANON_VERSION`/`canon()`, `SNAP_HINT_RE`/`SNAP_BODY_MAX`.
- [ ] `SAME_TASK_PROMPT`/`parse_same_task`/`match_task`/`match_snapshot`.
- [ ] `_is_progline`/`merge_snapshot_items`(진행줄 흡수 한정판).
- [ ] `REPLY_ROUTE_PROMPT`/`route_reply`(후보 1개 LLM 생략 포함).
- [ ] `analyze` 2곳: 본문 컷 400→조건부 1200(+`or snap` 신호), 상한 10→조건부 25.

**Phase 2 — bot.py**
- [ ] `_chan_locks`/`_kst_date`/`_is_snapshot`/`_snap_hw`/`_drain_outbox`/`_canon_dup_foreign`/`_channel_from_link`/`_LINK_RE`.
- [ ] `_apply_snapshot`(C-2) — 락·선등록·재활성 정책·F 규칙·missing 가드·push 필드 규칙·자동완료 알림(kind='alert').
- [ ] `process` 일정 분기(C-3): 스냅샷 분기 + 비스냅샷 락/`_reg_add_rows` + 스냅샷 이모지 미부착.
- [ ] `_reconcile_schedules` 초과삭제에 `reg_delete` 1줄 / `_clear_schedules` 스냅샷 revoke 분기 + `reg_delete` 1줄.
- [ ] `_apply_reply_update` 선두 스냅샷 라우팅 + 기존 경로 `reg_set_status` 훅 2곳.
- [ ] `_route_snapshot_reply`(C-6).
- [ ] `_is_past` 범위 끝 날짜(C-7).
- [ ] `backfill_cmd`(C-8): skip 확장·cheap-skip·force 채널 마커/리셋·스냅샷 위임(과거필터 면제)·canon dup 스킵·tail 문구.
- [ ] `resolve_loop`(C-9): canon 유일 병합·enroll·`_drain_outbox` + `on_ready` canon_ver(C-10).

**Phase 3 — 검증**: §6 단위 + e2e. 라이브 프롬프트 회귀는 `OLLAMA_LIVE=1`로 별도.

---

## 6) 테스트

### 6-1. 단위 (`tests\conftest.py` + `tests\test_match.py` — [match] §4 원안 채택 + 아래 수정/추가)

conftest: `sys.path.insert(0, <repo>/linkbot)`. [match]의 canon 동등/구분/엣지, `parse_same_task` 전 케이스, `match_task` 1단/쌍둥이/오출력/전송실패 전파/빈 후보, `match_snapshot` 1:1 선점 케이스 그대로. **수정**: `test_merge_snapshot_items`는 새 의미론으로 교체 —

```python
def test_merge_snapshot_items():
    items = [
        {"날짜": "", "작업내용": "카조에님 모델링 작업", "진행내용": "진행중", "상태": "진행중"},
        {"날짜": "2026-07-15", "작업내용": "카조에님 모델링", "진행내용": "", "상태": "진행중"},
        {"날짜": "2026-07-30", "작업내용": "다시바님 헤어", "진행내용": "", "상태": "진행중"},
    ]
    out = brain.merge_snapshot_items(items)
    assert len(out) == 2 and out[0]["날짜"] == "2026-07-15" and out[0]["진행내용"] == "진행중"

def test_merge_keeps_dated_twins():           # V1-3/V2-2 회귀
    items = [
        {"날짜": "2026-07-05", "작업내용": "바디 수정(상의)", "진행내용": "", "상태": "진행중"},
        {"날짜": "2026-07-10", "작업내용": "바디 수정(하의)", "진행내용": "", "상태": "진행중"},
    ]
    assert len(brain.merge_snapshot_items(items)) == 2   # canon 충돌해도 병합 금지
```

추가 단위(신규 `tests\test_snap_state.py`, 임시 DB로 state 직접 검증):
- `reg_apply_snapshot`: new→replay 멱등(같은 입력 2회 → missing/status 동일), stale 반환, matched∩new 예외, deleted_new 완료 제외, allow_missing=False 시 missing 불변, 하루 1회 가드(같은 kst_date 2회 → +1), outbox 적재/UNIQUE.
- 부활 회귀: snap: 완료 행 matched(status=진행중 전달) → completed_by 비워짐 / reply: 완료 행 status='완료' 전달 → completed_by 보존.
- `reg_revoke_snapshot`: 완료 행 잔존 + 반환 키에 미포함.
- `_snap_hw` 동작: 첫 적용 0, sched_n=3이면 3, reg에 #5 있으면 6.

### 6-2. e2e 시나리오 (수동/스테이징)

| # | 시나리오 | 기대 |
|---|---|---|
| S1 | 실례 메시지(전체일정+3건) 첫 게시 | 신규 3행 `{M}#0..#2`, 레지스트리 3항목(managed=1), sched_n=3, 이모지 없음 |
| S2 | 익일 동일 리스트 재게시(M2) | **새 행 0** — 3건 matched upsert(링크 갱신), 시트 행수 불변, 상태 필드 미전송 |
| S3 | 날짜 연장+항목 1 추가 | 기존 행 날짜만 갱신, 신규 1행 `{M2}#0` |
| S4 | 항목 X 2일 연속 미등장 → 3일째 재등장 | 1일차 m=1(시트 불변) → 2일차 상태=완료 merge+채널 알림 → 3일째 **기존 행 부활**(진행중, 새 행 0) |
| S5 | "권민님 작캠용 바디 수정 도움"→"권민님 바디수정" | 1단 불일치 → 2단 매칭 → 기존 행 갱신. LLM 불응 시 새 행(오병합 없음) |
| S6 | "[카조에님 모델링 진행중]"+리스트 | 매칭 행 진행내용/상태만 갱신·날짜 불변·새 행 0. 미매칭 진행줄은 행 미생성 로그 |
| S7 | 오늘 스냅샷 edit: 항목 제거/추가 | 제거: 자기행이면 삭제(완료행은 보존), 타행이면 m=1(재edit에도 m 불변). 추가: `#hw`부터 — 기존 행 하이재킹 없음 |
| S8 | 스냅샷 답장 "의상 절반"/"다 끝냈습니다"/"ㅇㅋ"/오늘 리스트 전문 | one→해당 행 / all+완료→그 스냅샷 행 일괄 완료(+reg) / 미소비 / 미소비→새 스냅샷 diff |
| S9 | S2 처리 직후 크래시 → 재기동 catchup | replay 되감기-재계산, 행 중복 0, missing 오증가 0 |
| S10 | clearSchedule → `/과거등록 force` (+중간 상한 도달 → force 재실행) | 채널별 리셋 후 시간순 재구축, 완료 채널 skip 이어하기, 최종 시트=마지막 스냅샷 상태. 6/28 시작(진행중) 작업 **유실 없음**(과거필터 면제) |
| S11 | 확인필요 등록 행이 다음날 스냅샷에 등장 | canon 유일이면 병합/아니면 새 행 후 matched 입양(managed=1). 스냅샷에 안 나오는 resolve 행(managed=0)은 오완료 없음 |
| S12 | 답장 완료 후 다음날 같은 줄 복붙 | 새 행 0 + **완료 유지**(신정보 없음). 날짜 연장해서 올리면 부활 |
| S13 | 같은 날 정정 재게시(연속 2메시지) | 채널 락 직렬화 → 두 번째가 첫 번째 결과 위에 diff(이중 신규행 0). 첫 번째는 이후 stale |
| S14 | PM 행 수동 삭제 후 스냅샷 계속 선언 | 행 재생성(자가치유). 상태 칸 빈칸 가능(§8) |
| S15 | 오탐성 리스트("전체일정" 헤더줄 없이 문장 언급) | 스냅샷 미판정 → 기존 동작. 헤더 있는 무관 리스트(매칭 0건) → matched/new만 반영, missing 생략 로그 |
| S16 | 자동완료 push 실패(GAS 다운) 후 복구 | outbox 잔존 → resolve_loop 드레인에서 완료 merge 재시도 성공 |
| S17 | 비스냅샷 회귀: 단건 일정/답장 n==1/n≥2 완료/[수동] 회송/정산/edit 축소 | 전부 기존 동작 그대로(+reg 훅으로 인한 시트 변화 없음) |

### 6-3. 라이브 프롬프트 회귀 (`OLLAMA_LIVE=1 pytest`)
[match] §4의 4케이스 + route_reply 3케이스(one/전체/모름). "헤드,헤어 묶음행 vs 헤어 단독"은 회색 케이스 — 초기엔 관찰만, 팀 방향대로 few-shot 추가로 고정(튜닝 포인트).

---

## 7) 적대검증 반영 매트릭스

출처 표기: V1=중복재발·유실 검증, V2=item_count·답장·수정·catchup 검증, V3=부분리스트·감지·운영 검증. 동일 이슈는 병합.

| # | 이슈 (출처) | 심각도 | 판정 | 반영 |
|---|---|---|---|---|
| 1 | matched push 모드 [flow]merge ↔ [state]upsert 정면 모순 (V1-1/V2-4/V3-3) | 높음 | 반영·확정 | **upsert + 필드 제한**(상태는 전이 시에만) §1-5. [flow] §9-4 폐기, 운영 규칙 "PM은 삭제 대신 완료" |
| 2 | 완료 행 후보 제외 → 부활 불가·완료+신규 중복 영구 누적, S4 자기모순 (V1-2/V2-3/V3-1) | 높음 | 반영 | 후보=완료 포함(`reg_channel_items` 기본), 재활성 정책(snap:무조건/reply:신정보) §1-4, S4·S12 테스트 |
| 3 | `merge_snapshot_items` 무조건 canon 병합 → 날짜 있는 별개 작업 소멸 (V1-3a/V2-2) | 높음 | 반영 | 진행줄 흡수 한정판으로 교체 §2-A-5, `test_merge_keeps_dated_twins` |
| 4 | 완료로 쌍둥이 한쪽 소거 시 1단 "유일" 오성립 → 오병합 (V1-3b) | 높음 | 반영(부분) | 후보에 완료 포함(#2)으로 쌍둥이가 후보에 남아 1단 유일성 유지 → 2단행. 잔여 리스크는 §8-6 |
| 5 | `reg_find_by_canon` fetchone → 쌍둥이 임의 병합 (V1-3c/V3-9) | 높음 | 반영 | `reg_find_by_canon_all`(fetchall), 2건 이상 → no-hit(새 행) — resolve·enroll 공용 |
| 6 | 추출 상관 실패(같은 줄 반복 누락) → 이틀 만에 침묵 자동완료 (V1-4/V3-8) | 높음 | 반영 | 절단 가드 + 자동완료 **채널 알림**(+알림 답장으로 부활) §1-4. 급감 비율 가드는 기각(아래 R2) |
| 7 | replay 신규 키 `#0..` 재부여 → 자기 행 하이재킹/시트 덮임 (V1-5/V2-1/V3-2) | 높음 | 반영 | HW 단조 할당 `_snap_hw` + `matched∩new` 예외 + plain INSERT §1-6-3, S7 테스트 |
| 8 | auto_completed/deleted_new push 유실 → 영구 비수렴 (V1-6/V3-6) | 중간 | 반영 | `sheet_outbox` 동일 트랜잭션 적재 + push 직후·resolve_loop 드레인 §1-5, S16 |
| 9 | 스냅샷 오탐(부분리스트/PM 질문) → 채널 전 행 missing (V1-7/V2-10/V3-7) | 중간 | 반영 | SNAP_HINT **줄 단독 헤더** 강화 + 언급 0건 missing 생략 가드 + 알림·부활 백스톱 §1-2/§1-4 |
| 10 | replay가 `last_seen_snap` 미복원 → edit 제거 항목 missing 미증가(S7 위반) (V1-8/V2-6) | 중간 | 반영 | missing 조건을 `NOT IN (matched∪new)`로 교체(lsn 비교 제거) §2-B-3 |
| 11 | 비스냅샷 답장 완료가 레지스트리 미기록 → 다음날 완료 뒤집힘 (V1-9) | 중간 | 반영 | 기존 답장 경로에 `reg_set_status` 훅 2곳 §2-C-5 + 재활성 정책이 reply: 보호 |
| 12 | 일반 메시지 canon 쌍둥이 편입 → 1단 파괴·영구 진행중 중복 (V1-10/V3-13) | 중간 | 반영(부분) | live=enroll 생략, backfill/재생=항목 스킵 §2-C-4/C-8. 시트 잔여 중복은 §8-5 한계 |
| 13 | 11건+ 리스트가 스냅샷 우회 → 매일 확인필요 스팸·diff 정지 (V1-11/V2-9) | 중간 | 반영 | 스냅샷 힌트 시 상한 25·본문 1200 §2-A-7. 25 초과는 확인필요(부분 diff 금지) |
| 14 | F 규칙 [match]"별건 행" ↔ [flow]"미생성" 불합치 (V1-12/V2-13일부) | 낮음 | 반영·확정 | §3-F: 레지스트리 포함 매칭 + 미매칭 진행줄 미생성 + matched=언급 카운트 |
| 15 | 채널 직렬화 부재 — 정정 재게시 이중 신규행·완료 lost update·catchup 경합 (V1-13/V2-5/V3-4) | 높음 | 반영 | 채널별 `asyncio.Lock`(diff·답장·enroll 전 구간) §1-6-1, S13 |
| 16 | replay `deleted_new`가 답장 완료 행 하드 삭제 (V2-7/V3-10) | 중간 | 반영 | deleted_new·revoke 모두 완료 행 제외 §2-B-3/B-4 |
| 17 | `reg_enroll` ON CONFLICT canon 미갱신 → edit 후 오매칭 (V2-8) | 중간 | 반영 | `canon=excluded.canon` §2-B-2, 불변식 완화 |
| 18 | force 상한 후 "force 없이 이어서"가 pre-clear 마커로 전량 skip → 복구 불능 (V2-11) | 중간 | 반영 | 채널 완료 마커 `bf_done:{ch}` + force 재실행 이어하기로 절차 교체 §2-C-8/§4-2 |
| 19 | 어제 스냅샷에 답장으로 오늘 리스트 → 진행 갱신 오소비 (V2-12) | 중간 | 반영 | 답장 라우팅 전 SNAP_HINT 선검사 → 미소비 §2-C-5 |
| 20 | 세 설계 심볼표 불일치 (V2-13) | 중간 | 반영 | §2-A/§2-B 단일 심볼표 확정([flow] 명칭 폐기 명시) |
| 21 | stale/중단 스냅샷 snap_msgs 미등재 → 답장 누수 중복 행 (V2-14) | 낮음 | 반영 | 감지 즉시 선등록(diff 전) §1-2/§2-C-2 |
| 22 | `_is_past` 범위 시작일 판정 → force 재구축서 진행중 작업 유실 (V3-5) | 높음 | 반영 | 끝 날짜 기준 수정 + 스냅샷 신규는 과거필터 면제 §2-C-7/C-8 |
| 23 | 후보 정렬 updated_at → replay 프롬프트 비재현 (V3-11) | 낮음 | 반영 | `ORDER BY src_mid,row_key` §2-B-2 |
| 24 | 비force backfill이 stale 스냅샷에 LLM 소모 (V3-12) | 낮음 | 반영 | cheap-skip §2-C-8-2 |

### 기각/보류 (사유)

| # | 제안 | 판정 | 사유 |
|---|---|---|---|
| R1 | 작성자 가드(직전 스냅샷 작성자/워커명 일치 시만 스냅샷) (V1-7) | 기각 | 결정 1 "채널=작업자의 상태 선언"과 충돌(PM 대리 게시·닉 변경 시 미탐). 헤더줄 규칙+언급0 가드+알림·부활 백스톱이 오탐 피해를 흡수하며, 오탐산 신규행도 b안으로 이틀 내 자가 회수 |
| R2 | missing 급감 비율 가드(matched+new < 50~60% → 보류) (V1-4/V3-7) | 기각 | 정상적 대량 축소(10→3건 마일스톤 종료)에서 은퇴가 **영구 교착** — 빠진 7건이 영원히 진행중. 언급 0건 가드(명백 오탐만 차단)+하루1회+2일 요건+알림으로 대체 |
| R3 | `prev_seen_snap` 칼럼 추가 (V1-8 수정안 중 하나) | 기각 | missing 조건 교체(#10)로 불요. 컬럼·복원 로직 증가만 남음 |
| R4 | matched SQL에 "reply mid > snap mid" 시간 가드 (V2-5b) | 기각 | 채널 락이 읽기~커밋을 직렬화해 lost update 원천 차단. SQL 가드는 mid 비교 의미가 모호(같은 채널 총순서는 락이 보장) |
| R5 | GAS rekey 액션(행 소유권 이동) — 대안 비교 의무 | 기각·확정 | GAS 배포+키 마이그레이션+실패 시 키 중복/유실 검증용 doGet 확장 연쇄. 이득(답장의 자연 라우팅)은 snap_msgs로 동등 달성. §1-1 |
| R6 | matched 상태 전이를 별도 merge 왕복으로 전송 (V1-1 수정안 일부) | 변형 채택 | 같은 upsert 페이로드에 전이 시에만 상태 포함 — GAS "비공백만 덮기"로 동일 효과, 왕복 1회 절약 |
| R7 | catchup limit 200→500 상향 | 보류 | 스냅샷은 상태수렴적이라 놓쳐도 다음 라이브에서 수렴(오염 없음). 다운타임 >200건/채널이 관측되면 상향 |
| R8 | doGet 일정 화이트리스트 개방(레지스트리↔시트 감사) | 보류 | matched-upsert 자가치유+outbox로 감사 필요성 낮음. GAS 무변경 원칙 우선 |
| R9 | managed=0 행 만료 규칙(60일 미언급 → 완료) (V1-10) | 보류 | enroll canon 가드(#12)로 쌍둥이 유입이 차단돼 시급성 낮음. 레지스트리 200행 상한 정리에 위임 |
| R10 | D 청소루프(기존 시트 중복 자동 정리) | 범위 밖 | 확정 결정 4 그대로 — clearSchedule+force가 운영 경로 |

---

## 8) 미결정 / 주의

1. **자가치유로 재생성된 행의 상태 칸 빈칸** — PM 삭제·push 유실 후 matched-upsert 재생성 시 상태 미전송(전이 없음)이라 빈칸. 다음 전이/답장에서 채워짐. 자주 거슬리면 "재생성 감지 시 상태 포함" 개선은 doGet 확장(R8)과 묶어 별건.
2. **`config.DATE_HINTS` 커버리지 확인** — 실례 리스트의 "[6/28~7/15]"가 DATE_HINTS에 걸리는지 배포 전 확인. §2-A-7의 `or snap`이 백스톱이지만, 스냅샷 아닌 일반 날짜 리스트는 여전히 DATE_HINTS 의존(기존 동작).
3. **daily-edit 작업자 패턴** — 같은 메시지를 매일 edit로 갱신하면 replay만 반복되어 b안 은퇴 불가(§3-C). 운영 안내로 대응, 코드 대응은 관측 후 결정.
4. **다운타임 중 스냅샷 edit** — Discord가 이벤트를 재전송하지 않아 복구 불가(catchup은 저장된 분류로 최종 본문 재생). 기존 설계 §8-4와 동급 한계.
5. **비스냅샷 라이브 재선언의 시트 중복** — 작업자가 리스트 밖에서 기존 작업을 새 메시지로 또 선언하면(답장 아님) 시트에는 여전히 새 행(현행 유지). 레지스트리 오염만 차단(#12). 안내: 갱신은 답장 또는 전체일정으로.
6. **canon 쌍둥이 양쪽이 모두 레지스트리에서 사라진 뒤 재등장** — 이론상 1단 유일 오성립 가능성이 잔존하나, 완료 행 후보 포함(#2)+200행 상한 내 보존으로 실운영 확률 극소. 관측 시 canon_full(괄호 보존 부가 키) 도입 검토.
7. **스냅샷 25건 초과 작업자** — 매일 확인필요 회송 스팸 가능(극단 케이스). 발생 시 상한 재조정 또는 리스트 분할 안내.
8. **`_chan_locks` 메모리** — defaultdict 무한 성장은 채널 수 규모(수십)라 무시 가능.
9. **동시 배포 전 데이터 없음 전제** — 레지스트리는 배포 후 첫 메시지부터 축적. 초기 force 재구축(§4-2) 전까지는 첫 스냅샷이 기존 시트 행과 매칭되지 않음(레지스트리 공백)을 팀에 공지.
10. **라이브 프롬프트 회귀의 회색 케이스**("헤드,헤어" 묶음 vs "헤어" 단독) — 정답이 운영 정책 의존. 초기 관찰 후 few-shot으로 고정.
