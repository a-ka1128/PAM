# 룩플/PAM 봇 — 코드 점검 & 개선 로드맵

> 점검일 2026-07-31 · 대상 `linkbot/` + `reminderbot/` + `extract/AppsScript.gs` (총 ~4,600줄)
> 방법: 정확성 / 성능 / 정합성 / 보안 / 운영성 5개 축으로 실제 코드·실데이터·가동 프로세스를 직접 확인.
> 이 문서는 "무엇을 고쳤다"가 아니라 **"앞으로 무엇을 해야 하는가"** 를 판단해 적은 것.

---

## 0. 한 줄 요약

추출 정확도는 이제 충분히 좋다(골든 89/89). **남은 위험은 정확도가 아니라 "운영"과 "구조"에 있다.**
가장 큰 문제는 코드가 아니라 **리마인더봇이 꺼져 있다는 사실**이고, 그다음이 *실패했을 때 조용히 어긋나는* 몇 군데다.

---

## 1. 🔴 지금 당장

> **정정(사용자 확인)**: 리마인더봇은 **VM에서 가동 중**이다. 이 PC에 프로세스가 없다는 관찰만으로
> "죽어 있다"고 본 것은 틀렸다. Apps Script도 **재배포 완료**(정산 탭 읽기 확인). 아래는 그 전제로 갱신한 내용.

### 1-1. VM 리마인더봇이 **구버전 코드**로 돌고 있다 — 최우선

`reminder_bot.py`의 💰 미수금 나이추적은 이 저장소에만 있고, VM은 아직 `git pull` 전이다.
즉 **오늘 만든 기능이 VM에서는 존재하지 않는다.**

**할 일**
1. VM에서 `git pull` + 리마인더봇 재시작.
2. 그 전에 아래 1-2(금액 파싱 버그)가 포함된 커밋까지 받아야 한다 — 안 그러면 합계가 틀린 채로 DM이 간다.

### 1-2. 읽기 토큰이 아직 **적용되지 않았다** (실측)

배포는 됐지만 스크립트 속성 `READ_TOKEN`이 비어 있다. 틀린 토큰(`WRONG-TOKEN-TEST`)으로 요청해도
**156행이 그대로 응답된다.**

```
틀린 토큰 요청 결과: ok=True rows=156   → 🔓 누구나 열람 가능
```

배포 URL만 알면 의뢰자명·작업내용·원본 메시지 링크가 전부 읽힌다. **3단계를 모두 해야 닫힌다**
([운영가이드.md](운영가이드.md) 10-1): ① 스크립트 속성 `READ_TOKEN` 설정 → ② `linkbot/config.py`와
**VM의** `reminderbot/config_reminder.py` 양쪽에 같은 값 → ③ 재배포.
⚠️ ①만 하고 ②를 빼먹으면 두 봇 모두 시트를 못 읽는다. **셋을 한 번에.**

> 교훈: 배포와 활성화는 다른 단계다. "배포했다"만으로 보안 기능이 켜졌다고 볼 수 없어서 실제로 찔러봤다.

---

## 1-3. 💰 미수금 섹션에 있던 금액 버그 (발견 즉시 수정)

정산 탭이 읽히자마자 실데이터로 돌려보니 **합계가 140원**으로 나왔다. 원인 둘:

| 문제 | 원인 | 결과 |
|---|---|---|
| 금액 합계가 깨짐 | `doGet`이 `getDisplayValues()`로 읽어 **셀 표시형식 그대로**("500,000") 옴 | `int()` 전부 실패 → 합계 140원 |
| 미정산 건수 과다 | 정산 탭의 **완전 빈 행 10개**를 미정산으로 셈 | 19건으로 표시(실제 9건) |

수정 후: **미정산 9건 / 합계 911,115원**. 손으로 적은 "20만원" 같은 단위도 처리(안 하면 20으로 읽혀 1만배 어긋남).
빈 행 10개는 시트에서 정리(23→13행).

> 이건 **정산 검증이 얇다**(3-4)는 지적의 실증 사례다. 기능을 만든 당일에 실데이터를 한 번만 돌려봤어도
> 잡혔을 버그였고, 실제로 그렇게 잡았다. **새 집계 기능은 반드시 실데이터로 1회 검증할 것.**

---

## 2. 🟠 구조적 빈틈 (조용히 어긋나는 곳)

### 2-1. 시트 전송 실패 시 레지스트리만 남는다 — 비스냅샷 경로

`_reconcile_schedules`([linkbot/bot.py:289](linkbot/bot.py:289))는 시트 push가 실패하면 `alert` 후 **그냥 return** 한다.
그런데 호출부는 반환값을 안 보고 곧바로 `_reg_add_rows`를 실행한다:

```python
await _reconcile_schedules(msg, fut, link)   # 내부에서 실패해도 조용히 return
_reg_add_rows(msg, fut)                      # ← 무조건 실행됨
```

**결과**: 레지스트리에는 있는데 시트에는 없는 행이 생긴다. 이후 스냅샷이 그 행을 "이미 있음(matched)"으로
처리하므로 **영영 시트에 안 나타난다.** 스냅샷 경로는 "레지스트리 커밋 → 다음 스냅샷이 자가치유"라고
주석까지 달아뒀는데([bot.py:437](linkbot/bot.py:437)), 비스냅샷 경로엔 그 자가치유가 없다.

**제안**: `_reconcile_schedules`가 성공 여부를 반환하고, 실패면 `_reg_add_rows`를 건너뛴다. 또는 outbox에
재시도 항목으로 넣는다(이미 `sheet_outbox` 테이블이 있으니 재사용).

### 2-2. 중복 판정으로 버려진 일정이 흔적 없이 사라진다

라이브 dedup은 중복으로 판정한 항목을 **조용히 드롭**하고 로그 한 줄만 남긴다([bot.py:258](linkbot/bot.py:258)).
매처가 틀리면(오늘 실제로 오병합 3종을 발견했다) **작업자가 보고한 일정이 그냥 증발한다.**

**제안**: 드롭된 항목을 최소한 되찾을 수 있게 한다. 셋 중 하나면 충분하다.
- (가벼움) 로그에 "무엇이 무엇과 병합됐는지" 쌍으로 남기기 — 지금은 건수만 남는다
- (중간) `state`에 dedup 이벤트 테이블 추가 → `/중복이력` 명령으로 조회
- (확실) 애매한 경우(유사도 중간대)만 `확인필요`로 회송

**"조용한 삭제"는 이 봇의 철학(사람 승인 게이트)과 어긋난다.** 최소한 (가벼움)은 해야 한다.

### 2-3. 메시지를 수정하면 억제됐던 중복이 되살아난다

dedup은 신규 메시지에만 적용된다(`if not is_edit`). 그래서 A 메시지의 항목이 중복으로 걸러진 뒤
**그 메시지를 편집하면** 걸러졌던 항목이 새 행으로 등록된다. 사용자 입장에선 "고쳤더니 중복이 늘었다".

**제안**: edit 경로에도 dedup을 적용하되 **자기 메시지 소유 행은 제외**(이미 `_canon_dup_foreign`이
`row_key.startswith(f"{mid}#")`로 자기 것을 걸러내므로 그대로 써도 된다). 지금 `is_edit` 예외를 둔 이유가
"자기 행 reconcile 유지"인데, 그 목적은 이미 함수 내부에서 달성돼 있다.

---

## 3. 🟡 정확도 — 더 깎을 것

### 3-1. 골든셋이 다시 포화될 것이다

지금 89/89. 이 숫자는 "완벽하다"가 아니라 **"이 세트로는 더 못 잰다"** 는 뜻이다. 오늘도 세트를
확장하자마자 실제 버그 2개(미래형 완료 오판, 편측 범위)가 튀어나왔다.

**운영 규칙으로 굳힐 것**: 시트에서 이상한 행을 볼 때마다 **그 메시지를 `golden/local_deadline.jsonl`에
한 줄 추가**한다. 주 1회 5분이면 충분하고, 이게 정확도의 유일한 복리 자산이다.

### 3-2. 판단(match_task) 임계값이 경험적으로 촘촘하다

지금 매처에는 임계가 세 개 있다: 포함 길이비 `0.4`, LLM 판정 후 길이비 `0.35`, 유사도 `0.34`.
`0.33(오탐) / 0.44(정탐)` 처럼 **관측 2점 사이에 끼워 넣은 값**이라 데이터가 늘면 흔들릴 수 있다.

**제안**: 임계를 더 만들지 말고, 지금 값들을 `local_same_task.jsonl`(16건)로 **계속 검증**한다.
새 오판이 나오면 임계를 옮기기 전에 **케이스를 먼저 추가**할 것. 임계 튜닝은 케이스가 30건쯤 모인 뒤에.

### 3-3. thinking은 이미 기각됐지만, 재평가 조건은 남겨둔다

측정 결과 `Structured Output(format)`과 thinking은 **양립 불가**(스키마를 걸면 사고 길이 0).
스키마 없이 돌리면 21/22·195초로, 결정적 백스톱(22/22·35초)보다 나쁘다.

**재평가 트리거**: 모델을 바꿀 때만. 그때 `config.THINK_JUDGE=True`로 한 번만 재측정하면 된다(경로는 이미 있음).

### 3-4. 아직 안 건드린 정확도 영역: **정산**

이번 점검은 일정 위주였다. 정산 추출(`SETTLE_PROMPT`)은 골든 11건뿐이고, `norm_amount`는
"백만원 → 1000000" 같은 한글 수사만 다룬다. 실제 메시지엔 `"5만 + 3만"`, `"1인당 4개 총 20"`,
`"부가세 별도"` 같은 게 섞인다. **돈은 틀리면 가장 아픈 데이터인데 검증이 가장 얇다.**

**제안**: `collector.db`에서 `MONEY_HINTS` 통과 메시지를 뽑아 정산 골든을 20~30건으로 늘린다.
(오늘 일정에 쓴 수확 방식을 그대로 재사용하면 된다.)

---

## 4. ⚙️ 성능

### 4-1. 라이브 dedup이 항목당 ~2.5초를 더 쓴다 (실측)

후보 30개 기준 측정: 신규 판정 2.6초 / 중복 판정 2.4초. 5개짜리 메시지면 **추출 + 12초**가 추가된다.
`_OLLAMA_LOCK`으로 직렬화되므로 여러 채널이 동시에 활동하면 더 밀린다.

> 오늘 이 호출이 **이벤트 루프를 통째로 블로킹**하던 버그는 수정했다(`asyncio.to_thread`로 이동).
> 그전에는 봇이 그 시간 동안 하트비트도 못 보냈다.

**제안 — 값싼 사전 게이트**: LLM을 부르기 전에 후보 전체와 bigram 유사도를 계산해서, **어떤 후보와도
겹침이 미미하면 즉시 "신규"로 확정**한다. 완전히 새로운 작업(대부분의 경우)이 LLM을 건너뛰므로
체감 지연이 크게 준다. 순수 파이썬이라 비용 0이고, `_core_sim_ok`가 이미 같은 계산을 갖고 있다.
⚠️ 매칭 행동을 바꾸므로 반드시 `local_same_task.jsonl`로 전후 비교할 것.

### 4-2. 후보 상한 30은 채널이 커지면 부족하다

`DUP_MATCH_CAND_MAX = 30`은 프롬프트 비대를 막지만, 진행중 작업이 30개를 넘는 채널에서는
**오래된 후보가 잘려나가 중복을 못 잡는다**(현재 최다 담당자 22건이라 아직 여유는 있음).
4-1의 사전 게이트를 넣으면 후보를 늘려도 비용이 안 늘어난다.

---

## 5. 🔒 보안 · 프라이버시

### 5-1. 시트 읽기 토큰 — 배포는 됐으나 **미활성** (1-2 참조)

실측으로 확인함: 틀린 토큰으로도 156행이 응답된다. 스크립트 속성 `READ_TOKEN`을 설정하고
**양쪽 config(로컬 + VM)**에 같은 값을 넣어야 닫힌다.

### 5-2. `OWNER_ID`(실제 디스코드 ID)가 example 설정에 커밋돼 있다

`config.example.py`, `reminder/config.example.py`, `reminderbot/config_reminder.example.py` 세 곳에
`OWNER_ID = 343290913172226049`가 실값으로 들어 있다. 치명적이진 않지만(디스코드 ID는 준공개)
**example 파일에 실제 개인 식별자를 두는 건 원칙적으로 틀렸다.** 플레이스홀더로 바꿀 것.

### 5-3. 마이그레이션 함수가 비원자적 (기존 지적 유지)

`migrateScheduleAddClient`는 `sh.clear()` 후 행별로 다시 쓴다. GAS 6분 제한/쿼터 오류로 중간에
죽으면 **일정 탭이 통째로 날아간다.** 1회성 함수라 급하진 않지만, 다시 돌릴 일이 생기면
**실행 전 시트 복제 백업**을 반드시 할 것.

---

## 6. 🧪 테스트 · 관측

### 6-1. 테스트가 없는 모듈

| 모듈 | 테스트 | 비고 |
|---|---|---|
| `linkbot/brain.py` | 97건 하드단언 + 골든 89 | 충분 |
| `linkbot/state.py` | 20건 | 적당 |
| `linkbot/bot.py` | 간접만 | 흐름 로직 미검증 |
| `linkbot/sheet.py` | **0** | 네트워크 계층 |
| `reminderbot/*` | **0** | 다이제스트·재촉 전부 미검증 |

리마인더봇은 **코드도 안 돌고 테스트도 없다.** 최소한 `build_digest`는 순수 집계라
모의 rows로 단위 테스트하기 쉽다(오늘 수동으로 해봤고 잘 된다 — 그걸 파일로 굳히면 된다).

### 6-2. 로그 로테이션이 없다

`linkbot.log`가 121KB이고 계속 커진다. `RotatingFileHandler`(예: 5MB × 3개)로 바꾸는 건 두 줄이다.
지금은 문제 없지만 몇 달 방치하면 tail/grep이 느려지고 디스크를 먹는다.

### 6-3. "봇이 살아있는가"를 알 방법이 약하다

오늘 리마인더봇이 죽어 있는 걸 **프로세스를 직접 조회해서야** 알았다.
**제안**: 다이제스트 DM 맨 아래에 `추출봇 🟢 / 재촉봇 🟢 (마지막 활동 hh:mm)` 한 줄을 붙인다.
매일 아침 DM 한 통이 곧 헬스체크가 된다. (통합 런처 상태창과 함께라면 이중 안전망.)

---

## 7. 💡 앞으로 추가하면 좋을 것

우선순위 순. 위 1~6을 정리한 뒤 손대는 걸 권한다.

1. **통합 런처(추출+재촉)** — 1-1의 해결책이자 재발 방지. 가장 값싸고 효과가 크다.
2. **정산 골든셋 확장** — 돈 데이터의 검증 공백을 메운다(3-4).
3. **dedup 사전 게이트** — 체감 지연을 줄이면서 후보 상한도 풀 수 있다(4-1, 4-2).
4. **주간 다이제스트** — 지금은 일간뿐. 주 단위로 "이번 주 완료 N건 / 지연 N건 / 담당자별 부하"를
   보내면 PM 관점의 추세가 보인다. 시트 read-only + 순수 집계라 LLM 0회.
5. **`/일정조회` 슬래시 명령** — 지금 조회 수단이 시트뿐이다. 디스코드에서 "내 일정",
   "OO님 건" 정도만 봐도 팀원 문의가 줄어든다. 역시 LLM 0회.
6. **감사 로그(⑨)** — "이 행이 왜 이렇게 됐지?"에 답할 수 있게. `reg_apply_snapshot` 트랜잭션 안에
   이벤트 INSERT 한 줄이면 원자성은 공짜다. 2-2의 dedup 이력과 같은 테이블을 쓰면 일석이조.
7. **별칭 사전(⑧)** — PM이 "이 둘은 같은 작업"이라고 승인한 쌍을 결정적 자산으로 축적.
   단 `canon()`에 넣지 말 것(1단 유일성이 깨져 조용한 과병합). 2단 진입 직전 정규화로만.

### 하지 말아야 할 것 (판단 근거를 남김)

- **모델 교체** — qwen3:14b가 실측 우위였고, 교체는 프롬프트·파서·골든 전면 재튜닝 = 최대 회귀면.
  지금 병목은 모델이 아니라 위의 구조 문제다.
- **thinking 상시 활성** — 측정으로 기각됨(3-3).
- **자동 상태 변경 부활** — 2026-07-03에 끈 결정. 신뢰도가 낮으면 `확인필요` 회송까지만.
- **임계값 추가 튜닝** — 케이스가 모이기 전엔 과적합이다(3-2).

---

## 8. 권장 실행 순서

```
1) VM에서 git pull + 리마인더봇 재시작     ← 미수금 기능·금액버그 수정이 VM에 없음
2) READ_TOKEN 3단계 설정(속성+양쪽 config+재배포)  ← 지금 시트가 열려 있음. 사람 손 필요.
3) 2-1 시트실패 정합성 · 2-2 dedup 이력   ← 조용히 어긋나는 곳 봉합
4) 정산 골든 확장 · 로그 로테이션          ← 검증 공백 + 위생 (1-3이 실증한 영역)
5) dedup 사전 게이트 · 주간 다이제스트     ← 체감 개선
```

> VM 운영이 끼면서 **"배포 대상이 둘"** 이 됐다(로컬 PC = 추출봇, VM = 리마인더봇).
> 앞으로 `reminderbot/`을 고칠 때마다 VM 반영을 함께 챙겨야 한다 — 이번처럼 조용히 어긋난다.

**관통 원칙 세 가지**
1. **검증된 불변식은 교체하지 말고 얹어라** — `canon` 1단 매칭, 백스톱 임계, replay 재현성.
2. **골든셋 없이 매칭 코어를 손대지 마라** — 오늘 하드단언이 회귀를 두 번 잡았다.
3. **조용한 실패를 만들지 마라** — 드롭·삭제·병합은 반드시 흔적을 남긴다.

---

*작성: 2026-07-31 · 근거는 전부 이 저장소의 코드·실데이터·가동 프로세스 실측.*
