# =========================================================
# config.example.py  →  config.py 로 복사해서 채우세요 (config.py 는 gitignore됨)
#   ※ 이건 수집봇과 별개인 "리마인더 봇" 전용 설정입니다 (별도 토큰!).
# =========================================================

# 리마인더 봇 토큰 (수집봇과 다른 새 애플리케이션의 토큰)
TOKEN = "여기에_리마인더봇_토큰"

# 봇을 조작할 사람(PM/본인)의 디스코드 유저 ID
OWNER_ID = 343290913172226049

# 우선순위 이모지 → (내부라벨, 리마인드 간격(시간), 한글명)
PRIORITY = {
    "🔴": ("high",   1,  "긴급"),
    "🟡": ("medium", 3,  "보통"),
    "🔵": ("low",    12, "여유"),
}

# 완료(추적 종료) 처리할 이모지
DONE_EMOJI = "✅"

# 리마인드 방식: "dm"(대상에게 개인 DM) 또는 "channel"(원본 채널에 멘션 핑)
ALERT = "channel"

# 활동 로그를 띄울 채널 ID (0 = 끔). 설정하면 추적시작/완료/재촉을 그 채널에 기록.
LOG_CHANNEL_ID = 0
