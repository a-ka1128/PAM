# 🛠️ 룩플봇 운영 가이드 (관리자용)

> 팀원용 간단 사용법은 `사용법.md`. 이 문서는 **운영자(아카)용** 기술 문서예요.

---

## 1. 개요
**룩플봇(linkbot)** = 디스코드 메시지에서 *작업·마감·정산*을 자동 추출 → 구글 시트에 정리 + 우선도 이모지로 작업 재촉.
- **전부 로컬에서** 동작 (로컬 LLM) → 메시지 본문이 외부로 안 나감 (프라이버시)
- 봇 1개(PAM#2883), 서버 = LooKP_Studio

## 2. 전체 구조
```
디스코드 메시지 (모든 채널)
        ↓
   linkbot (bot.py)
   ├─ 사전필터(날짜/금액 키워드) → exaone(로컬 LLM)로 추출
   ├─ 분류 → [일정] / [정산] / [확인필요]
   ├─ 구글시트 webhook(Apps Script)으로 upsert
   └─ 작업/마감 메시지엔 🔴🟡🔵 부착 → 클릭 시 추적·재촉
```
- **실행 체인:** 시작프로그램 → `runner.py`(워치독) → `bot.py`
- **LLM:** Ollama + `exaone3.5:7.8b` (GPU)
- **출력:** 구글 시트 (아카 소유 복사본)

## 3. 동작 흐름

### 추출 → 시트
1. 메시지 올라옴 (모든 채널 감시)
2. **사전필터** — 날짜/금액/작업 키워드 있는 것만 LLM 처리 (잡담 스킵 → GPU 절약)
3. **exaone + few-shot** 추출 → 정규화 (날짜→`YYYY-MM-DD`, 금액→숫자, 한글금액 `백만원`→1000000)
4. **주체 판별** — 작업자 = 채널 접두어(`OO-일정정리`) 또는 @멘션 / 의뢰자 = `"OO님"`(앞뒤 메시지까지 탐색)
5. **분류** — 작업자 명확+마감 → `일정` / 돈 얘기 → `정산` / 애매(작업자 불명·추출실패) → `확인필요`
6. **시트 upsert** — 메시지 링크가 key → 같은 메시지 재처리해도 중복 없이 갱신

### 재촉 (리마인더)
1. `일정`(작업) 메시지에 봇이 **🔴🟡🔵 자동 부착**
2. 누가 하나 클릭 → 우선도 + 추적 시작 — **🔴 12시간 / 🟡 24시간(1일) / 🔵 72시간(3일)** 마다 알림
3. **✅ 반응** 또는 **답장** → 완료 처리(알림 멈춤)

## 4. 파일 구성 (`AutoLinkBot/linkbot/`)
| 파일 | 역할 |
|---|---|
| `bot.py` | 메인 봇 (이벤트·추출 호출·시트 전송·재촉·명령어 + 단일락·상태영속·따라잡기·모니터링) |
| `brain.py` | 추출 두뇌 (exaone 프롬프트·few-shot·정규화·작업자/의뢰자 판별) |
| `sheet.py` | 시트 webhook 전송 |
| `state.py` | 상태 영속 (`state.db` SQLite: 추적/플래그/마지막확인) |
| `config.py` | 설정 (토큰·웹훅·OWNER_ID·모델·키워드·이모지 간격) — **gitignore됨** |
| `runner.py` | 워치독 (bot.py 죽으면 5초 후 자동 재시작) |
| `test_brain.py` | 추출 회귀 테스트 (14개) |
| `linkbot.log` / `state.db` | 로그 / 상태 DB |

시트 스크립트는 `AutoLinkBot/extract/AppsScript.gs` (구글 Apps Script에 붙여넣은 코드).

## 5. 구글 시트 (자동 관리 3탭)
| 탭 | 컬럼 | 비고 |
|---|---|---|
| **일정** | 날짜·작업·담당자·상태·D-day·원본 | 날짜순 자동 정렬, 상태 5종 드롭다운 |
| **정산** | 받는이·항목·금액·상태·원본 | 상태: 미정산/회사에서 미정산/완료 |
| **확인필요** | 내용·추정작업자·추정의뢰자·사유·원본·처리 | 봇이 애매한 것 = **PM 검수 큐** |
- **작업자별 보드 = PM 수동** (봇이 안 건드림)
- 탭은 첫 데이터 들어올 때 자동 생성 + 서식/드롭다운/색/링크
- Apps Script 함수: `doPost`(자동) / `fixSettleStatus`·`fixAll`·`resortSchedule`(에디터에서 수동 1회용)

## 6. 운영

### 상태 확인
- **디스코드 명령어:** `/상태` · `/추적목록` · `/추적해제`
- **로그:** `linkbot\linkbot.log`
- **LLM:** `ollama ps` (exaone 로드 확인)
- **모니터링 DM:** 봇 시작 / Ollama 다운 / 시트 전송 실패 시 → 아카에게 자동 DM

### 시작 / 정지
- **자동:** 컴 켜고 로그인하면 자동 실행 (시작프로그램 등록됨)
- **수동 시작:** 시작프로그램의 `LooKP_LinkBot` 바로가기 더블클릭, 또는 재부팅
- **정지:** 작업관리자 → `pythonw` 전부 종료 (워치독까지)
- **영구 정지:** 시작프로그램에서 `LooKP_LinkBot` 바로가기 삭제

### Ollama
- 보통 자동 실행(트레이 아이콘). 꺼졌으면 Ollama 앱 실행 or `ollama serve`

## 7. 설정 (`linkbot/config.py`)
| 항목 | 설명 |
|---|---|
| `TOKEN` | 봇 토큰 (노출 시 Discord 개발자포털 Reset → 여기 교체) |
| `WEBHOOK_URL` | 시트 Apps Script 웹앱 URL |
| `OWNER_ID` | 알림 DM 받을 디스코드 유저 ID |
| `MODEL` | `exaone3.5:7.8b` |
| `PRIORITY` | `{🔴:12, 🟡:24, 🔵:72}` (시간) |
| `DATE_HINTS` / `MONEY_HINTS` | 사전필터 키워드 |
| `NIM_BLOCKLIST` | "OO님"에서 사람 아닌 보통명사 (고객/선생 등) |

## 8. 문제 해결
| 증상 | 확인 / 해결 |
|---|---|
| 봇 응답 없음 | 작업관리자에 `pythonw` 있나 → 없으면 시작프로그램 바로가기 더블클릭 / 로그 확인 |
| "Ollama 응답 안함" DM | `ollama ps` → 꺼졌으면 Ollama 재실행 |
| 추출이 안 됨 | 메시지에 날짜/금액 키워드 있나 / Ollama 떴나 / 로그의 `-> ` 줄 |
| 시트 안 들어감 | 웹훅 URL·배포 권한("모든 사용자"인지) / 로그의 "시트 전송 실패" |
| `401 LoginFailure` | 토큰 무효 → config.py 토큰 확인 / 재발급 |
| 봇이 여러 개? | **단일 락이 자동 차단** (port 47291) — 걱정 안 해도 됨 |

## 9. 신뢰성 장치 (Tier 1)
- **상태 영속**(SQLite) — 재시작해도 추적 안 날아감
- **따라잡기** — 봇 꺼진 동안 온 메시지 보충
- **단일 인스턴스 락** — 중복 봇 차단
- **keep_alive 30분** — 모델 안 식음 (콜드로드 타임아웃 방지) + 타임아웃 120초
- **워치독 + 시작프로그램** — 자동 실행 / 크래시 자동 복구
- **모니터링 DM** — 문제 시 알림

## 10. 프라이버시
모든 추출·분석은 **로컬 exaone**에서만. 메시지 본문은 외부로 안 나감. 구글 시트(아카 소유)에만 *추출된 항목*이 올라감.

---
*최종 업데이트: 2026-06-27 — 1번(정산 드롭다운·토큰) + 신뢰성(영속실행·워치독·락) 완료 시점*
