# 설계: 일정 답장(reply)·수정(edit) 갱신 + 다중일정(0..N) 분류

> 대상 파일(절대경로)
> - `D:\Study\DIscordBot\AutoLinkBot\linkbot\brain.py`
> - `D:\Study\DIscordBot\AutoLinkBot\linkbot\bot.py`
> - `D:\Study\DIscordBot\AutoLinkBot\linkbot\sheet.py`
> - `D:\Study\DIscordBot\AutoLinkBot\linkbot\state.py`
> - `D:\Study\DIscordBot\AutoLinkBot\extract\AppsScript.gs`
>
> 이 문서는 세 영역 설계(brain/bot/gas)와 4건의 적대검증을 **하나의 구현 가능한 스펙**으로 통합한다. 적대검증의 유효 이슈는 모두 반영했고, 무효/안전 항목은 §7에 기각 사유와 함께 기록한다.

---

## 1) 확정 설계 요약

### 1-1. 키 규칙 (모든 충돌의 근원 → 단일 규칙으로 못박음)

**확정: 모든 탭, 모든 push의 key는 `f"{msg.id}#{index}"`. 단일 항목도 `#0`.**

- 일정: 다중이면 `#0, #1, …`. 단일도 `#0`.
- 정산/확인필요: 항상 `#0` (다중 분류 대상 아님, 그러나 키 네임스페이스 통일을 위해 `#0` 강제).
- 근거: 적대검증 [이슈3·5]가 지적한 "raw `msg.id` vs `#0` 공존 → 중복 행"을 원천 차단. "정산/확인필요는 기존 단일 경로 유지(raw key)"라는 예외를 **완전히 제거**한다.
- GAS `map[it.key]`는 문자열 완전일치이므로(`AppsScript.gs:36,42`), 기존 시트의 raw `_key`(`"123"`)는 마이그레이션으로 전부 `"123#0"`로 정규화한다(§3). 일정·정산·확인필요 **세 탭 모두** 정규화 대상.

### 1-2. 반환 계약 (brain.analyze) — 0건 = None 단일화

```
None                                                   # 비일정/신호없음/추출 0건
{"tab":"정산",     "fields":{...}}                      # 단건 (기존 형태 유지)
{"tab":"확인필요", "fields":{...}}                      # 단건 (기존 형태 유지)
{"tab":"일정", "is_task":True, "items":[ {fields}, … ]} # items 길이 >= 1 (다중)
```

- **확정: 0건은 항상 `None`.** brain은 절대 `{"items":[]}`를 반환하지 않는다(적대검증 [이슈10] 해소). edit로 "기존 행을 0건으로 정리"하는 책임은 **호출부(bot)** 가 `res is None`을 보고 명시적으로 수행한다.
- 정산/확인필요는 `fields` 단건 유지(회귀 0). 일정만 `items` 리스트.

### 1-3. 답장(reply) [A]

작업자가 원본 일정 메시지에 디스코드 답장 → 봇이 원본 행의 진행내용(+날짜/상태) **부분 갱신**.

- **원본 일정 건수 N 판정은 재추출이 아니라 `state`에 저장된 item_count로 한다** (적대검증 [이슈3·6] 해소: 재추출은 비결정적이라 N 오판 → 틀린 행 갱신).
- N 해소 규칙(확정):
  - `N == 1`: 원본 `{ref}#0` 행을 진행내용/날짜/상태만 부분병합.
  - `N >= 2` (모호): **"완료" 키워드가 감지되면 원본 전 인덱스(`#0..#N-1`) 상태를 일괄 `완료`로** 갱신(완료는 메시지 단위라 행 모호성 없음). 그 외(부분 진행 문구)는 **확인필요 탭으로 1건 회송**(PM 판단). ← **이것이 "답장×다중 모호" 확정 해소안.**
  - `N == 0` (원본이 일정으로 등록된 적 없음, 예: 확인필요로 빠졌던 경우): **확인필요로 회송**(유령행 생성 금지, [이슈5] 해소).
- 답장 메시지 자체의 신규 추출과 충돌 방지: **답장이 진행 갱신/완료/회송 중 하나로 "소비"되면 그 답장 메시지는 신규 일정 추출(`process`)을 건너뛴다** (적대검증 동시성 [이슈4]/[이슈8] 해소).

### 1-4. 수정(edit) [B]

원본 메시지 edit → 재추출 → index reconcile.

- `on_raw_message_edit`로 수신(캐시 미존재 메시지도 처리). `fetch_message` → `process(msg, live=False, is_edit=True)`.
- reconcile: `#0..#(N-1)` 업서트 + **이전 item_count 초과분(`#N..#(prev-1)`) 삭제**.
  - 삭제 범위는 **state에 저장된 직전 item_count**로 정확히 계산(적대검증 [이슈1·3·4] 해소: "후보키 일괄삭제" 폐기. 후보키는 멀티추출 비결정성·상한 초과로 정상행 삭제/고아 잔존을 유발).
- **edit는 전체 덮어쓰기**(진행내용 포함). 답장(부분병합)과 구분 (적대검증 [이슈2·5] 해소: edit 축소 시 인덱스 시프트로 진행내용이 다른 작업에 오승계되는 것 방지. edit는 "정정"이므로 사용자가 새로 쓴 내용이 진실).
- edit로 일정 0건이 되면: 그 msg.id의 모든 일정 행 삭제 + **추적 중이면 추적 해제·이모지 정리** (적대검증 [이슈8/이슈9] 해소).
- 탭 전이(일정↔정산↔확인필요): edit 결과 탭이 바뀌면 **이전 일정 행을 먼저 정리** (적대검증 동시성 [이슈9] 해소).

### 1-5. 다중일정 [C]

한 메시지 0..N건 추출. 과분할 방어:

- 멀티라인 프롬프트 + few-shot으로 분리/단일유지/잡담=없음 학습.
- `parse_deadline_lines`에서 dedup 키는 **`(작업명, 기한)` 튜플** (적대검증 [이슈11] 해소: 작업명만이면 다른 단계 작업이 합쳐짐).
- 작업명이 순수 날짜/기호로만 구성된 줄 폐기 (날짜 나열 잡담 환각 방어).
- **보수 모드(초기 운영): 한 메시지에서 5건 초과 추출 시 통째로 확인필요 회송** (폭주/환각 방어, 적대검증 [이슈7/이슈8] 해소). 데이터 쌓이면 상한 완화.

### 1-6. 리마인더 이모지·우선도·추적

메시지 단위 1세트 유지. `flagged`/`tracked`는 `msg.id` 정수 키(행 key `#index`와 별개 네임스페이스 → 무충돌). 이모지는 `live=True`이고 일정이 1건 이상일 때만 1회 부착.

### 1-7. 일정 탭 스키마

```
날짜 / 작업내용 / 진행내용 / 담당자 / 상태 / D-day / 원본
```
- "작업" → "작업내용" rename, "진행내용" 신규.
- 헤더 라벨은 **`D-day` 유지**(전 수식·indexOf 의존). 화면 "디데이" 표기는 채택 안 함(§8).
- 진행내용 초기값(b안): 첫 메시지에 진행 문구 있으면 초기값, 없으면 빈칸(키 자체를 안 보냄).

### 1-8. 부분병합 vs 전체덮기 (GAS 동작 분기)

GAS `doPost`가 **모드 플래그**로 분기:
- `mode:"merge"` (답장): 제공된 비어있지 않은 칼럼만 덮음. **기존 행 없으면 신규행 생성 거부**(noop, 유령행 방지).
- `mode:"upsert"` (신규/수정, 기본): 제공 칼럼은 덮고 미제공은 신규행이면 빈칸 초기화·기존행이면 보존. 단 **D-day/원본/_key/_sort/정렬 보존**.

---

## 2) 파일별 정확한 변경점

### 2-A. `brain.py`

#### (A-1) `DEADLINE_PROMPT` 교체 (멀티라인 + 진행 칼럼 + few-shot)

`brain.py:12-18`의 `DEADLINE_PROMPT`를 아래로 교체:

```python
DEADLINE_PROMPT = (
    "다음 디스코드 메시지에서 '작업 일정'을 모두 뽑아.\n"
    "한 메시지에 일정이 여러 개일 수 있다. 서로 다른 작업이면 각각 한 줄로 분리해라.\n"
    "각 줄 형식(정확히): `작업내용 | 기한 | 진행`\n"
    "  - 작업내용: 무엇을 하는지 (필수)\n"
    "  - 기한: 마감일/날짜. 없으면 빈칸\n"
    "  - 진행: 진행상황·완료여부 문구가 있으면. 없으면 빈칸\n"
    "규칙:\n"
    "  - 같은 작업을 쪼개지 마라. '모델링'과 '의상'처럼 분명히 다른 작업만 분리.\n"
    "  - 일정이 하나도 없으면 `없음` 한 단어만.\n"
    "  - 라벨·설명·번호·머리글 금지. 데이터 줄만 출력.\n\n"
    "예시1) 메시지: \"빈즈님 캐릭터 모델링 11/20까지요\"\n->\n캐릭터 모델링 | 11/20 | \n\n"
    "예시2) 메시지: \"마태자님 모델링 7/20까지, 의상 7/25까지\"\n->\n모델링 | 7/20 | \n의상 | 7/25 | \n\n"
    "예시3) 메시지: \"텍스처 작업 다음주 월요일까지 / 리깅은 이번주 금요일\"\n->\n텍스처 작업 | 다음주 월요일 | \n리깅 | 이번주 금요일 | \n\n"
    "예시4) 메시지: \"모델링 7/20까지였는데 절반 정도 끝냈어요\"\n->\n모델링 | 7/20 | 절반 정도 진행\n\n"
    "예시5) 메시지: \"캐릭터 모델링 완료했습니다\"\n->\n캐릭터 모델링 |  | 완료\n\n"
    "예시6) 메시지: \"다들 점심 드셨어요? 날씨 좋네요\"\n->\n없음\n\n"
    "메시지: \"{m}\"\n->\n")
```

#### (A-2) 신규 상수·헬퍼 추가

`brain.py:173`의 `LABELS` 정의 뒤(또는 `analyze` 앞)에 추가:

```python
# 완료로 간주할 진행 문구
DONE_HINTS = ["완료", "끝냈", "끝났", "마무리", "마쳤", "done", "완성"]


def _canon_task(s):
    return re.sub(r"\s+", "", (s or "")).lower()


def detect_status(progress):
    """진행내용 문구 → 일정 상태. 완료 감지 시 '완료', 아니면 '진행중'."""
    t = (progress or "").replace(" ", "")
    return "완료" if any(k in t for k in DONE_HINTS) else "진행중"


# 작업명이 순수 날짜/기호로만 구성됐는지 (잡담 환각 방어)
_DATEONLY_RE = re.compile(r"^[\d\s/.\-월일주요()]+$")


def parse_deadline_lines(out):
    """LLM 멀티라인 출력 → [{'작업내용','기한','진행'}, ...].
    잘못된/라벨/빈/날짜만 줄은 폐기. (작업명,기한) 중복 제거."""
    if not out:
        return []
    if out.strip().startswith("없음"):
        return []
    rows, seen = [], set()
    for raw in out.splitlines():
        line = raw.strip()
        if not line or "|" not in line:
            continue
        p = [x.strip() for x in line.split("|")] + ["", "", ""]
        task, due, prog = p[0], p[1], p[2]
        if not task or task in LABELS:
            continue
        if _DATEONLY_RE.match(task):            # 작업명이 날짜/기호뿐 = 환각
            continue
        ck = (_canon_task(task), _canon_task(due))   # (작업명,기한) 튜플 dedup
        if ck in seen:
            continue
        seen.add(ck)
        rows.append({"작업내용": task, "기한": due, "진행": prog})
    return rows


# 답장 진행문구 전용 경량 추출 프롬프트
PROGRESS_PROMPT = (
    "다음은 어떤 작업에 대한 '진행 상황 답장'이다. 진행내용과 날짜를 뽑아.\n"
    "형식(정확히): `진행 | 날짜`  (날짜 없으면 빈칸). 진행 문구가 전혀 없으면 `없음`.\n\n"
    "예시1) \"모델링 절반 정도 했어요\" -> 모델링 절반 진행 | \n"
    "예시2) \"7/22까지 미루겠습니다\" -> 기한 연기 | 7/22\n"
    "예시3) \"다 끝냈습니다\" -> 완료 | \n"
    "예시4) \"ㅇㅋ 감사합니다\" -> 없음\n\n"
    "메시지: \"{m}\"\n->\n")


def extract_progress(text):
    """답장/진행 답장 → {'진행내용': str, '날짜': str|None} or None.
    빈 컨텍스트·정산 분기 없이 진행만 경량 추출."""
    t = (text or "")[:300]
    if len(t) < 2:
        return None
    out = ollama(PROGRESS_PROMPT.format(m=t))
    if not out or out.strip().startswith("없음") or "|" not in out:
        return None
    p = [x.strip() for x in out.split("|")] + ["", ""]
    prog = p[0]
    if not prog or prog in LABELS:
        return None
    date = norm_date(p[1]) if p[1] else None
    return {"진행내용": prog, "날짜": date or None}
```

#### (A-3) `analyze()` 일정 분기 멀티화

`brain.py:199-213` (일정 분기 + 마지막 `return None`)을 아래로 교체. **정산 분기(187-197)는 불변.**

```python
    # 일정/작업 (멀티화)
    if has(text, config.DATE_HINTS):
        out = ollama(DEADLINE_PROMPT.format(m=text))
        rows = parse_deadline_lines(out)          # 0..N건
        if not rows:
            return None                           # 신호는 있었으나 0건 → None

        who = mentions[0] if mentions else author
        client = find_client(content, context)
        worker = worker_from_channel(channel_name) or (mentions[0] if mentions else None)

        # 작업자 불명확 = 메시지 단위 → 통째 확인필요 (행 안 쪼갬)
        if not worker:
            return review(content, who, client, "작업자 불명확")

        # 보수 모드: 5건 초과는 과분할/환각 의심 → 확인필요 회송
        if len(rows) > 5:
            return review(content, who, client, f"일정 과다추출({len(rows)}건) — 확인 요망")

        items = []
        for r in rows:
            items.append({
                "날짜": norm_date(r["기한"]),
                "작업내용": r["작업내용"],
                "진행내용": r["진행"],            # 빈 문자열일 수 있음 (bot이 빈값이면 키 생략)
                "담당자": worker,
                "상태": detect_status(r["진행"]),
            })
        return {"tab": "일정", "is_task": True, "items": items}

    return None
```

> 주의: `LABELS`(173행)에 `"진행"`을 추가하지 않는다(진행 칼럼 값은 자유 텍스트). 라벨 echo 방어는 작업명에만 적용.

---

### 2-B. `state.py`

> 현재 `state.py`는 SQLite KV(`get_kv`/`set_kv`) + tracked/flagged 영속을 제공한다(코드 호출부 기준). 메시지별 item_count 저장 API를 추가한다. KV를 재사용하되 키 충돌을 피하기 위해 전용 헬퍼를 둔다.

추가 함수(기존 KV 테이블 재사용):

```python
def set_msg_item_count(mid, n):
    set_kv(f"sched_n:{int(mid)}", int(n))


def get_msg_item_count(mid):
    v = get_kv(f"sched_n:{int(mid)}")
    try:
        return int(v)
    except (TypeError, ValueError):
        return 0


def del_msg_item_count(mid):
    # KV 삭제 API가 있으면 사용, 없으면 0으로 set
    try:
        del_kv(f"sched_n:{int(mid)}")     # del_kv 없으면 아래 set_kv로 대체
    except (AttributeError, NameError):
        set_kv(f"sched_n:{int(mid)}", 0)
```

> 구현 시 `state.py` 실제 KV 스키마를 확인해 `set_kv`가 정수/문자열을 받는지 맞추고, 삭제 API 유무에 따라 `del_msg_item_count`를 정리한다. 핵심은 **`(mid -> 직전 일정 건수)`** 영속.

---

### 2-C. `bot.py`

#### (C-1) 상수

`bot.py:48` 부근에 추가:

```python
SETTLE_TABS = ("정산", "확인필요")   # 단건 탭
```

#### (C-2) `process()` 재설계 — `bot.py:118-149` 전체 교체

```python
async def process(msg, live=True, is_edit=False):
    if msg.guild is None or msg.author.bot:
        return False
    if msg.channel.id in getattr(config, "EXCLUDED_CHANNEL_IDS", []):
        return False

    ctx = list(ctx_buf[msg.channel.id])
    if live:                                  # edit/catchup은 컨텍스트 오염 방지
        ctx_buf[msg.channel.id].append(msg.content or "")
    mentions = [m.display_name for m in msg.mentions if not m.bot]

    try:
        res = await asyncio.to_thread(
            brain.analyze, msg.content, msg.channel.name, msg.author.display_name, mentions, ctx)
    except Exception:
        log.exception("analyze 실패")
        await alert_err("ollama", "⚠️ 추출 실패 — Ollama(로컬 AI)가 응답하지 않는 것 같아요. 켜져 있는지 확인해 주세요.")
        return False

    link = jump(msg.guild.id, msg.channel.id, msg.id)

    # 결과 없음
    if not res:
        if is_edit:
            await _clear_schedules(msg)        # edit로 일정 사라짐 → 행+추적 정리
        return False

    tab = res["tab"]

    # 탭 전이: edit로 일정→비일정 되면 이전 일정 행 정리
    if is_edit and tab != "일정":
        await _clear_schedules(msg, untrack=True)

    # 일정 (다중)
    if tab == "일정":
        items = res.get("items", [])
        if not items:
            if is_edit:
                await _clear_schedules(msg)
            return False
        await _reconcile_schedules(msg, items, link)
        # 이모지: live + 1건 이상 + 미추적/미플래그일 때만 1세트
        if live and res.get("is_task") and msg.id not in flagged and msg.id not in tracked:
            try:
                for e in config.PRIORITY:
                    await msg.add_reaction(e)
                flagged.add(msg.id)
                state.add_flagged(msg.id)
            except Exception:
                log.exception("이모지 부착 실패")
        log.info(f"-> 일정 {len(items)}건: {items[0].get('작업내용','')[:24]}")
        return True

    # 정산/확인필요 (단건) — key #0 통일
    fields = dict(res["fields"])
    fields["링크"] = link
    r = await asyncio.to_thread(sheet.push, tab, [{"key": f"{msg.id}#0", "fields": fields}])
    if isinstance(r, str) and r.startswith("err"):
        await alert_err("push", f"⚠️ 시트 전송 실패: {r[:100]}")
    log.info(f"-> {tab} ({r}): {fields.get('항목') or (fields.get('내용','') or '')[:24]}")
    return True
```

#### (C-3) 신규 `_reconcile_schedules` + `_clear_schedules`

```python
async def _reconcile_schedules(msg, items, link):
    """일정 #0..#(n-1) 업서트(전체덮기) + 직전 초과분 삭제. state에 건수 저장."""
    n = len(items)
    push_items = []
    for i, f in enumerate(items):
        f = dict(f)
        f["링크"] = link
        # 진행내용 빈값은 키 생략(신규행 빈칸 유지). edit는 전체덮기라 빈값도 보냄 → 별도 처리 위해 항상 포함하되 mode로 구분
        push_items.append({"key": f"{msg.id}#{i}", "fields": f})

    r = await asyncio.to_thread(sheet.push, "일정", push_items, "upsert")
    if isinstance(r, str) and r.startswith("err"):
        await alert_err("push", f"⚠️ 시트 전송 실패: {r[:100]}")
        return

    prev = state.get_msg_item_count(msg.id)
    if prev > n:                                  # 줄었으면 초과행 삭제
        over = [f"{msg.id}#{i}" for i in range(n, prev)]
        await asyncio.to_thread(sheet.delete_rows, "일정", over)
    state.set_msg_item_count(msg.id, n)


async def _clear_schedules(msg, untrack=False):
    """이 메시지의 모든 일정 행 삭제 + 건수 0 + (옵션) 추적 해제·이모지 정리."""
    prev = state.get_msg_item_count(msg.id)
    if prev > 0:
        keys = [f"{msg.id}#{i}" for i in range(prev)]
        await asyncio.to_thread(sheet.delete_rows, "일정", keys)
    state.set_msg_item_count(msg.id, 0)
    if untrack or msg.id in tracked or msg.id in flagged:
        tracked.pop(msg.id, None)
        state.del_tracked(msg.id)
        flagged.discard(msg.id)
        state.del_flagged(msg.id)
        try:
            for e in config.PRIORITY:
                await msg.clear_reaction(e)
        except Exception:
            pass
```

> `sheet.push`에 3번째 인자 `mode`를 추가(§2-D). 신규/수정은 `"upsert"`, 답장은 `"merge"`.

#### (C-4) `on_message` — 답장 갱신 통합 (`bot.py:152-160` 교체)

```python
@client.event
async def on_message(msg):
    if msg.author.bot or msg.guild is None:
        return

    ref = msg.reference.message_id if msg.reference else None

    # 추적 완료 해소 (기존)
    if ref and ref in tracked:
        await resolve(ref, msg.channel, "reply")

    # 답장으로 일정 진행 갱신 → 소비되면 신규 추출 skip
    consumed = False
    if ref:
        consumed = await _apply_reply_update(msg, ref)

    if not consumed:
        await process(msg, live=True)

    state.set_kv("last_seen", time.time())
```

#### (C-5) 신규 `_apply_reply_update`

```python
async def _apply_reply_update(reply_msg, ref_id):
    """답장→원본 일정 진행 갱신. 처리(소비)했으면 True 반환(=신규 추출 skip)."""
    # 원본 일정 건수: 재추출이 아니라 state 기준 (비결정성 회피)
    n = state.get_msg_item_count(ref_id)

    # 진행 문구 경량 추출 (정산 분기·컨텍스트 없음)
    try:
        upd = await asyncio.to_thread(brain.extract_progress, reply_msg.content)
    except Exception:
        await alert_err("ollama", "⚠️ 진행 추출 실패 — Ollama 확인 필요")
        return False
    if not upd or not upd.get("진행내용"):
        return False                              # 단순 답장(ㅇㅋ 등) → 미소비

    prog = upd["진행내용"]
    done = brain.detect_status(prog) == "완료"
    link = jump(reply_msg.guild.id, reply_msg.channel.id, reply_msg.id)

    if n == 0:
        # 원본이 일정으로 등록된 적 없음 → 확인필요 회송 (유령행 금지)
        await _review_reply(reply_msg, ref_id, "답장 대상 일정 없음", link)
        return True

    if n == 1:
        fields = {"진행내용": prog, "상태": ("완료" if done else "진행중")}
        if upd.get("날짜"):
            fields["날짜"] = upd["날짜"]
        await asyncio.to_thread(sheet.push, "일정",
                                [{"key": f"{ref_id}#0", "fields": fields}], "merge")
        return True

    # n >= 2 (모호)
    if done:
        # 완료는 메시지 단위 → 전 인덱스 상태=완료 일괄
        items = [{"key": f"{ref_id}#{i}", "fields": {"상태": "완료"}} for i in range(n)]
        await asyncio.to_thread(sheet.push, "일정", items, "merge")
        return True
    # 부분 진행 모호 → 확인필요 회송
    await _review_reply(reply_msg, ref_id, f"답장 진행 갱신 모호(원본 {n}건)", link)
    return True


async def _review_reply(reply_msg, ref_id, reason, link):
    fields = {
        "내용": (reply_msg.content or "")[:120],
        "추정 작업자": reply_msg.author.display_name,
        "추정 의뢰자": "",
        "사유": f"{reason}",
        "링크": link,
    }
    await asyncio.to_thread(sheet.push, "확인필요",
                            [{"key": f"{reply_msg.id}#0", "fields": fields}], "upsert")
```

#### (C-6) 신규 `on_raw_message_edit`

```python
@client.event
async def on_raw_message_edit(payload):
    ch = client.get_channel(payload.channel_id)
    if ch is None or getattr(ch, "guild", None) is None:
        return
    try:
        msg = await ch.fetch_message(payload.message_id)
    except Exception:
        return
    if msg.author.bot:
        return
    if msg.channel.id in getattr(config, "EXCLUDED_CHANNEL_IDS", []):
        return

    await process(msg, live=False, is_edit=True)    # 이모지 재부착 안 함·컨텍스트 오염 없음

    # 수정된 메시지가 답장이면 원본 갱신도 재적용
    if msg.reference and msg.reference.message_id:
        await _apply_reply_update(msg, msg.reference.message_id)
    state.set_kv("last_seen", time.time())
```

#### (C-7) `catchup()` — 답장 갱신 살리기 (`bot.py:94-115`)

`process(msg, live=False)` 호출 직후, 그 메시지가 답장이면 `_apply_reply_update`도 호출(다운 중 답장 누락 완화, 적대검증 동시성 [이슈10]):

```python
                async for msg in ch.history(after=after, limit=200, oldest_first=True):
                    if msg.author.bot:
                        continue
                    ref = msg.reference.message_id if msg.reference else None
                    consumed = False
                    if ref:
                        consumed = await _apply_reply_update(msg, ref)
                    if not consumed and await process(msg, live=False):
                        count += 1
```

#### (C-8) `_reextract_task` 멀티화 (`bot.py:235-242`)

```python
def _reextract_task(content):
    rows = brain.parse_deadline_lines(brain.ollama(brain.DEADLINE_PROMPT.format(m=(content or "")[:400])))
    if not rows:
        return None
    items = [{"날짜": brain.norm_date(r["기한"]), "작업내용": r["작업내용"],
              "진행내용": r["진행"], "상태": brain.detect_status(r["진행"])} for r in rows]
    return ("일정", items)     # items 리스트 반환
```

#### (C-9) `resolve_loop` 등록 — key `#0` 정규화 + 멀티 반영 (`bot.py:284-304`)

`_reextract_task`가 이제 `("일정", [items…])`를 반환하므로 등록부를 멀티 대응으로 교체:

```python
            tab, payload = ext
            base = str(key).split("#")[0]          # 확인필요 key → base msg.id 추출
            if tab == "일정":
                push_items = []
                for i, f in enumerate(payload):
                    f = dict(f); f["담당자"] = worker; f["링크"] = link
                    push_items.append({"key": f"{base}#{i}", "fields": f})
                r = await asyncio.to_thread(sheet.push, "일정", push_items, "upsert")
                if isinstance(r, str) and r.startswith("ok") and not r.endswith(":0"):
                    state.set_msg_item_count(int(base), len(push_items))  # 등록 건수 기록
                    delete_keys.append(key)
                    log.info(f"resolve(등록->일정 {len(push_items)}건): {base} / {worker}")
                else:
                    await alert_err("confirm_push", f"⚠️ 등록 실패(삭제 보류): {str(r)[:100]}")
            else:   # 정산
                fields = dict(payload); fields["받는이"] = worker; fields["링크"] = link
                r = await asyncio.to_thread(sheet.push, "정산", [{"key": f"{base}#0", "fields": fields}], "upsert")
                if isinstance(r, str) and r.startswith("ok") and not r.endswith(":0"):
                    delete_keys.append(key)
                    log.info(f"resolve(등록->정산): {base} / {worker}")
                else:
                    await alert_err("confirm_push", f"⚠️ 등록 실패(삭제 보류): {str(r)[:100]}")
            done += 1
```

> `_reextract_settle`는 `("정산", fields)` 단건 유지(변경 없음). 위 분기에서 `payload`가 dict면 정산, list면 일정.
> push 성공 판정을 `startswith("ok") and not endswith(":0")`로 강화(적대검증 동시성 [이슈7]: `ok:0`을 성공 오판 → 확인필요 행 유실 방지).

---

### 2-D. `sheet.py`

`push`에 `mode` 인자 추가(기본 `"upsert"`로 호환):

```python
def push(sheet, items, mode="upsert"):
    """items=[{'key':'<id>#<i>','fields':{...}}]. mode: 'upsert'|'merge'."""
    if not items:
        return None
    body = json.dumps({"sheet": sheet, "mode": mode, "items": items},
                      ensure_ascii=False).encode("utf-8")
    req = urllib.request.Request(
        config.WEBHOOK_URL, data=body,
        headers={"Content-Type": "application/json; charset=utf-8"})
    try:
        with urllib.request.urlopen(req, timeout=30) as r:
            return r.read().decode("utf-8", "replace")
    except Exception as e:
        return f"err:{e}"
```

`fetch`/`delete_rows`는 **변경 없음**(key 불투명 전달, `#index` 그대로 동작).

---

### 2-E. `AppsScript.gs`

#### (E-1) `TABS["일정"]` 스키마 (`:10`)

```javascript
"일정":     ["날짜", "작업내용", "진행내용", "담당자", "상태", "D-day", "원본"],
```

#### (E-2) `initTab`의 `W` 맵 (`:143`)

`"작업": 260` 제거, 추가:

```javascript
var W = { "날짜": 100, "작업내용": 240, "진행내용": 220, "담당자": 140, "상태": 92, "D-day": 70, "원본": 64,
          "받는이": 110, "항목": 220, "금액": 100, "내용": 300, "추정 작업자": 110, "추정 의뢰자": 110, "사유": 170, "처리": 92 };
```

> `keyCol = head.length+1`(`:30`), `_sort = head.length+2`(`:225`)는 자동 산출 → 헤더 7칸 확장에 자동 적응. 상수 변경 불필요.

#### (E-3) `doPost` 업서트 본문 → mode 분기 부분병합 (`:39-63`)

`items.forEach(...)` 블록을 아래로 교체. **신규행 행번호를 루프 내 수동 증가**(적대검증 키매칭 [이슈7]: 칸별 setValue + 다중 신규 동시 push 시 같은 행 충돌 방지).

```javascript
    var mode = d.mode || "upsert";   // "upsert"(신규/수정) | "merge"(답장 부분병합)
    var items = d.items || [], n = 0;
    var nextRow = sh.getLastRow() + 1;
    items.forEach(function (it) {
      var f = it.fields || {};
      var exists = map[it.key] !== undefined;

      // merge 모드 + 기존행 없음 = 유령행 생성 금지 → skip
      if (mode === "merge" && !exists) return;

      var row = exists ? map[it.key] : nextRow;
      if (!exists) nextRow++;

      // 현재 행 값(부분병합 보존용)
      var cur = exists ? sh.getRange(row, 1, 1, head.length).getValues()[0] : null;

      head.forEach(function (h, ci) {
        if (h === "D-day" || h === "원본" || h === "날짜" || h === "금액") return; // 특수 처리 별도
        var provided = (f[h] !== undefined && f[h] !== null && String(f[h]).trim() !== "");
        if (mode === "merge") {
          if (provided) sh.getRange(row, ci + 1).setValue(f[h]);   // 비공백만 덮기, 나머지 보존
        } else { // upsert
          if (exists) {
            if (provided) sh.getRange(row, ci + 1).setValue(f[h]); // 기존행: 비공백만 덮기
            // 미제공/빈값 = 보존 (수정 시 진행내용 등 보존)
          } else {
            sh.getRange(row, ci + 1).setValue(provided ? f[h] : ""); // 신규행: 빈칸 초기화
          }
        }
      });

      // 원본 HYPERLINK: 링크 제공 시에만 (미제공=보존)
      var li = head.indexOf("원본");
      if (li >= 0 && f["링크"]) sh.getRange(row, li + 1).setFormula('=HYPERLINK("' + String(f["링크"]).replace(/"/g, "") + '","원본")');

      // 날짜 + D-day
      var dt = head.indexOf("날짜"), dd = head.indexOf("D-day");
      if (dt >= 0 && f["날짜"] !== undefined && String(f["날짜"]).trim() !== "") {
        var pd = parseYmd(f["날짜"]);
        if (pd) sh.getRange(row, dt + 1).setValue(pd).setNumberFormat("yyyy-mm-dd");
        else sh.getRange(row, dt + 1).setValue(f["날짜"]);          // 비표준 날짜 원문
      } else if (!exists && dt >= 0) {
        sh.getRange(row, dt + 1).setValue("");                       // 신규+날짜없음=빈칸
      }
      if (dd >= 0 && dt >= 0) {
        var a1 = sh.getRange(row, dt + 1).getA1Notation();
        sh.getRange(row, dd + 1).setFormula('=IF(ISNUMBER(' + a1 + '),' + a1 + '-TODAY(),"")');
      }

      // 금액(정산): 제공 시에만
      var am = head.indexOf("금액");
      if (am >= 0 && f["금액"] !== undefined && String(f["금액"]).trim() !== "") {
        var num = parseInt(String(f["금액"]).replace(/[^0-9]/g, ""));
        if (!isNaN(num)) sh.getRange(row, am + 1).setValue(num).setNumberFormat("#,##0");
      }

      sh.getRange(row, keyCol).setValue(it.key);
      map[it.key] = row; n++;
    });
    if (head.indexOf("날짜") >= 0) resortSchedule();
    return ContentService.createTextOutput("ok:" + n);
```

> 응답 계약 유지: 성공=`"ok:N"` (N=실제 반영 행수). `merge` skip이면 그만큼 n이 줄어 `ok:0` 가능 → bot이 `endswith(":0")`로 감지.

#### (E-4) `migrateScheduleV2()` 1회성 함수 추가 (§3)

#### (E-5) `doDelete`/`doGet`/`resortSchedule` — 무변경

- `doDelete`: `_key` 완전일치 삭제, 화이트리스트 없음 → `#index` 개별 삭제·일정 삭제 이미 동작.
- `doGet`: 확인필요 전용. 본 스펙은 일정 fetch를 쓰지 않으므로(N은 state로 판정) 무관.

---

## 3) 마이그레이션 절차 (기존 일정 데이터)

### 3-1. 배포 순서 (엄수 — 어기면 데이터 손실)

1. **GAS 먼저 배포**: (E-1)(E-2)(E-3)(E-4). 즉 TABS/initTab/doPost 부분병합/migrate 함수.
2. **봇 정지 상태에서** GAS 에디터에서 `migrateScheduleV2()` 1회 실행 + `migrateKeysV2()`(정산·확인필요 key 정규화, 아래) 1회 실행.
3. **그 다음** brain.py / state.py / sheet.py / bot.py 배포 후 봇 기동.

> 이유: 부분병합 미배포 상태에서 답장 경로가 활성화되면 작업내용/담당자가 `""`로 소멸(적대검증 동시성 [이슈4]).

### 3-2. 일정 탭 마이그레이션 함수 (GAS 추가)

```javascript
// 기존 일정 탭 → 신스키마(진행내용 삽입) + _key "123"->"123#0" 정규화. 봇 정지 후 1회 실행.
function migrateScheduleV2() {
  var ss = SpreadsheetApp.getActiveSpreadsheet();
  var sh = ss.getSheetByName("일정");
  if (!sh) { Logger.log("일정 탭 없음"); return; }
  var lock = LockService.getScriptLock(); lock.waitLock(30000);
  try {
    var h1 = sh.getRange(1, 1, 1, sh.getLastColumn()).getValues()[0];
    if (h1.indexOf("진행내용") >= 0) { Logger.log("이미 마이그레이션됨"); return; }

    var last = sh.getLastRow();
    var oldHead = ["날짜", "작업", "담당자", "상태", "D-day", "원본"];
    var oldKeyCol = oldHead.length + 1;            // 7 = G
    var srcCol = oldHead.indexOf("원본") + 1;       // 6 = F
    var keyVals  = (last >= 2) ? sh.getRange(2, oldKeyCol, last - 1, 1).getValues() : [];
    var dateVals = (last >= 2) ? sh.getRange(2, 1, last - 1, 1).getValues() : [];
    var workVals = (last >= 2) ? sh.getRange(2, 2, last - 1, 1).getValues() : [];
    var ownerVals= (last >= 2) ? sh.getRange(2, 3, last - 1, 1).getValues() : [];
    var statVals = (last >= 2) ? sh.getRange(2, 4, last - 1, 1).getValues() : [];
    var srcForms = (last >= 2) ? sh.getRange(2, srcCol, last - 1, 1).getFormulas() : [];

    sh.clear(); sh.clearConditionalFormatRules();
    var newHead = TABS["일정"], newKeyCol = newHead.length + 1;   // 8 = H
    initTab(sh, "일정", newHead, newKeyCol);

    var iDate = newHead.indexOf("날짜"), iWork = newHead.indexOf("작업내용"),
        iProg = newHead.indexOf("진행내용"), iOwner = newHead.indexOf("담당자"),
        iStat = newHead.indexOf("상태"), iDday = newHead.indexOf("D-day"), iSrc = newHead.indexOf("원본");
    var r = 2;
    for (var i = 0; i < keyVals.length; i++) {
      var oldKey = String(keyVals[i][0] || "");
      if (!oldKey) continue;
      var newKey = /#\d+$/.test(oldKey) ? oldKey : (oldKey + "#0");
      var dv = dateVals[i][0];
      if (dv instanceof Date) sh.getRange(r, iDate + 1).setValue(dv).setNumberFormat("yyyy-mm-dd");
      else if (dv !== "" && dv !== null) sh.getRange(r, iDate + 1).setValue(dv);
      sh.getRange(r, iWork + 1).setValue(workVals[i][0] || "");
      sh.getRange(r, iProg + 1).setValue("");                      // 진행내용 빈칸(b안)
      sh.getRange(r, iOwner + 1).setValue(ownerVals[i][0] || "");
      sh.getRange(r, iStat + 1).setValue(statVals[i][0] || "진행중");
      var fml = (srcForms[i] && srcForms[i][0]) ? srcForms[i][0] : "";
      if (fml) sh.getRange(r, iSrc + 1).setFormula(fml);
      var a1 = sh.getRange(r, iDate + 1).getA1Notation();
      sh.getRange(r, iDday + 1).setFormula('=IF(ISNUMBER(' + a1 + '),' + a1 + '-TODAY(),"")');
      sh.getRange(r, newKeyCol).setValue(newKey);
      r++;
    }
    resortSchedule();
    Logger.log("migrateScheduleV2: " + (r - 2) + "행 이전 완료");
  } finally { lock.releaseLock(); }
}
```

### 3-3. 정산·확인필요 key 정규화 함수 (GAS 추가)

```javascript
// 정산/확인필요 탭의 raw _key("123") → "123#0" 정규화. 봇 정지 후 1회 실행.
function migrateKeysV2() {
  var ss = SpreadsheetApp.getActiveSpreadsheet();
  ["정산", "확인필요"].forEach(function (name) {
    var sh = ss.getSheetByName(name); if (!sh) return;
    var head = TABS[name], keyCol = head.length + 1, last = sh.getLastRow();
    if (last < 2) return;
    var ks = sh.getRange(2, keyCol, last - 1, 1).getValues();
    for (var i = 0; i < ks.length; i++) {
      var k = String(ks[i][0] || "");
      if (k && !/#\d+$/.test(k)) sh.getRange(2 + i, keyCol).setValue(k + "#0");
    }
  });
  Logger.log("migrateKeysV2 완료");
}
```

> 멱등: 이미 `#n` 형태면 건드리지 않음. 두 번 실행해도 안전.

### 3-4. state item_count 백필 (선택)

마이그레이션 후 기존 일정 행들의 item_count는 state에 없다(전부 `#0` 단일로 정규화됨). bot이 이후 그 메시지를 edit/답장할 때 `get_msg_item_count`가 `0`을 반환 → 답장 경로가 "n==0 → 확인필요 회송"으로 빠질 수 있다. 완화책:

- **권장**: 봇 기동 후 첫 `catchup`에서는 백필 불가(history 범위 밖). 따라서 **답장 경로에서 `n==0`이면 회송하되, 사유에 "기존 데이터(추적정보 없음)"를 명시**해 PM이 수동 갱신하도록 한다. 신규 메시지부터는 state가 정확히 쌓이므로 시간이 지나면 자연 해소.
- 더 정확히 하려면: migrate 시 시트의 각 base msg.id별 `#index` 최대치+1을 계산해 별도 export(JSON) → bot 기동 시 `state.set_msg_item_count`로 1회 임포트하는 보조 스크립트를 둘 수 있음(선택, 초기엔 생략 가능).

---

## 4) 단계별 구현 체크리스트

**Phase 0 — state 인프라**
- [ ] `state.py`: `set_msg_item_count`/`get_msg_item_count`/`del_msg_item_count` 추가, KV 스키마 정합 확인.

**Phase 1 — GAS (먼저 배포)**
- [ ] `TABS["일정"]` 신스키마 (E-1).
- [ ] `initTab` `W` 맵 갱신 (E-2).
- [ ] `doPost` mode 분기 부분병합 + 신규행 nextRow 수동증가 (E-3).
- [ ] `migrateScheduleV2()` / `migrateKeysV2()` 추가 (3-2, 3-3).
- [ ] 배포(웹앱 새 버전).

**Phase 2 — 마이그레이션 (봇 정지 상태)**
- [ ] 봇 프로세스 종료 확인.
- [ ] GAS 에디터에서 `migrateScheduleV2()` 실행 → 로그 "N행 이전 완료" 확인.
- [ ] `migrateKeysV2()` 실행 → 로그 확인.
- [ ] 시트 육안 검증: 진행내용 칼럼 존재, _key가 전부 `#0`, 기존 작업내용/담당자/원본 링크 보존.

**Phase 3 — Python (배포)**
- [ ] `sheet.py`: `push(sheet, items, mode="upsert")`.
- [ ] `brain.py`: DEADLINE_PROMPT/DONE_HINTS/`_canon_task`/`detect_status`/`parse_deadline_lines`/PROGRESS_PROMPT/`extract_progress`/`analyze` 일정 분기.
- [ ] `bot.py`: `process(live, is_edit)` / `_reconcile_schedules` / `_clear_schedules` / `on_message` / `_apply_reply_update` / `_review_reply` / `on_raw_message_edit` / `catchup` / `_reextract_task` / `resolve_loop`.
- [ ] 봇 기동.

**Phase 4 — 검증** (§5 테스트)
- [ ] 회귀 5종 + 신규 e2e 6종 통과.

---

## 5) 테스트 케이스

### 5-1. 회귀 (기존 기능 불변 확인)

| # | 입력/동작 | 기대 |
|---|---|---|
| R1 | 정산 메시지 "키키님 모델링비 20만원 입금완료" | 정산 탭 `{id}#0` 행, 받는이=키키, 금액=200000, 상태=완료 |
| R2 | 단일 일정 "빈즈님(채널=빈즈일정정리) 캐릭터 모델링 11/20" | 일정 `{id}#0`, 작업내용=캐릭터 모델링, 담당자=빈즈, 🔴🟡🔵 부착 |
| R3 | 잡담 "다들 점심 드셨어요?" | None, 시트 무변경, 이모지 없음 |
| R4 | 🔴 클릭 → 추적 시작 → ✅ 또는 완료 답장 | 추적 등록 후 resolve("☑️"+완료답장), tracked 제거 |
| R5 | 확인필요 탭 "처리=등록" + 작업자 입력 | resolve_loop이 일정 `{base}#0`로 등록, 확인필요 행 삭제, item_count=1 기록 |

### 5-2. 신규 e2e

| # | 시나리오 | 기대 |
|---|---|---|
| E1 다중 | "마태자님(채널 마태자일정정리) 모델링 7/20, 의상 7/25" | 일정 2행 `#0`(모델링 7/20), `#1`(의상 7/25), 이모지 1세트, item_count=2 |
| E2 답장-단일 | E1과 달리 단일 일정 메시지에 답장 "절반 정도 했어요" | `{ref}#0` 진행내용="절반 정도 진행", 상태=진행중. 작업내용/담당자/날짜/원본 **보존**. 답장 메시지 신규추출 skip |
| E3 답장-완료 | 단일/다중 무관, 추적중 메시지에 "다 끝냈습니다" 답장 | resolve(완료처리) + (다중이면 `#0..#N-1` 상태=완료 일괄). 확인필요 회송 안 함 |
| E4 답장-다중모호 | 다중(2건) 메시지에 "의상 절반 했어요"(미완료) 답장 | 확인필요 탭 `{reply}#0` "답장 진행 갱신 모호(원본 2건)". 일정 행 직접 갱신 안 함 |
| E5 수정-축소 | E1 메시지를 edit "마태자님 의상 7/25"(1건) | `#0`=의상 7/25(전체덮기), `#1` 삭제. item_count=1. 이모지 재부착 없음 |
| E6 수정-증가 | 단일(1건) 메시지를 edit로 3건 | `#0..#2` 업서트, item_count=3. 정렬 후 날짜순 배치 |
| E7 수정-소멸 | 일정 메시지를 edit "아 취소요"(0건) | `#0..` 전부 삭제, item_count=0, 추적중이면 해제+이모지 제거 |
| E8 탭전이 | 일정 메시지를 edit해서 정산 내용으로 | 이전 일정 행 삭제 + 정산 `{id}#0` 생성 |
| E9 유령방지 | 확인필요로 빠졌던(일정 미등록) 메시지에 진행 답장 | n==0 → 확인필요 회송, 일정 탭에 작업내용 빈 유령행 생성 안 됨 |
| E10 과분할방어 | "오늘 7/8 회의록 / 내일 점심 뭐 먹지 / ㅋㅋ" | 5건 이하지만 작업명 환각 시 dateonly/dedup 필터로 억제. 6건+면 확인필요 회송 |

### 5-3. 동시성/정합 검증(수동)
- [ ] E5 직후 봇 재시작 → catchup이 같은 메시지를 1건으로 재추출해도 **삭제는 prev(state)=1, n=1 → 초과삭제 없음**(정상행 유실 없음 확인).
- [ ] 답장 push 시 GAS `ok:0`(merge skip) → bot이 성공 오판 안 함 확인.

---

## 6) 적대검증 이슈 → 반영 매트릭스

| 이슈(검증자) | 심각도 | 반영 |
|---|---|---|
| 후보키 일괄삭제가 정상행 삭제(비결정성) | 높음 | **폐기** → state item_count로 정확 삭제. 삭제는 edit 한정(catchup/reply는 업서트만) (§1-4, C-3) |
| 답장 진행→작업/담당자 소실 / 유령행 | 높음 | mode="merge" 부분병합 + merge 미존재행 skip (E-3, C-5) |
| raw key vs `#0` 공존 → 중복 | 높음 | **모든 탭 #index 통일** + migrateKeysV2 (§1-1, 3-3) |
| 답장 메시지 신규로도 추출→중복 | 높음 | 답장 소비 시 process skip (C-4) |
| edit 축소 인덱스 시프트 → 진행 오승계 | 중간 | edit=전체덮기, 부분병합은 답장 전용 (§1-4) |
| 멀티 과분할(잡담→N행) | 중간 | dateonly 필터 + (작업,기한) dedup + 5건↑ 회송 (A-2, A-3) |
| edit 0건 후 tracked 잔존 | 중간 | `_clear_schedules` untrack+이모지 제거 (C-3) |
| 답장 N 판정 재추출 비결정 | 높음 | state item_count로 판정(재추출 폐기) (C-5) |
| ok:0 성공 오판 | 중간 | `startswith("ok") and not endswith(":0")` (C-9) |
| 다중 신규 동시 push 행충돌 | 중간 | nextRow 수동 증가 (E-3) |
| 탭 전이 고아행 | 중간 | edit 탭전이 시 일정 정리 (C-2) |
| catchup이 답장 갱신 누락 | 중간 | catchup에서 `_apply_reply_update` 호출 (C-7) |
| dedup이 다른작업 합침 | 낮음 | (작업,기한) 튜플 dedup (A-2) |
| 반환 0건 None/items:[] 불일치 | 중간 | **0건=None 단일화** (§1-2) |
| 배포 순서 역전 데이터손실 | 높음 | §3-1 순서 엄수 게이트 |

---

## 7) 검증했으나 무효/안전 (기각 사유)

- **doDelete 화이트리스트로 일정 삭제 막힘** → 무효. `doDelete`는 `TABS[name]`만 체크(`:116`), 화이트리스트 없음 → 일정 삭제 이미 동작. `#index` 완전일치 개별삭제도 정상(`:119,131`).
- **doGet 확인필요 전용이라 일정 fetch 막힘** → 무관. 본 스펙은 일정 fetch를 쓰지 않음(N은 state로 판정). 단 향후 "답장 전 일정 실재 확인"을 fetch로 하려면 doGet 화이트리스트 확장 필요(현재 스펙은 미사용).
- **push→delete 레이스로 _sort stale** → 안전. doDelete는 `_key` 매칭으로 행 재탐색, 남은 행 상대순서 유지 → 정렬 안 깨짐.
- **단일 인스턴스 락/리마인더 이모지 1세트/정산 분기** → 핸들러 추가와 무관, 회귀 0.
- **마이그레이션 중 doPost 끼어듦** → migrate가 `LockService` 잡고 read 후 clear → 직렬화. 단 **봇 정지 후 실행** 운영수칙으로 추가 방어(§3-1).
- **keyCol/_sort 자동 산출** → `head.length+1/+2`라 헤더 확장 자동 적응, 하드코딩 없음.

---

## 8) 미결정/주의

1. **D-day 화면 표기**: 내부 키 `D-day` 유지 확정(전 수식·indexOf 의존). "디데이" 표기 원하면 indexOf 7곳 동시 변경 별건으로.
2. **state item_count 백필**: 마이그레이션된 기존 일정은 item_count가 state에 없음 → 그 메시지에 첫 답장 시 `n==0`으로 확인필요 회송됨(사유에 "기존 데이터" 명시). 신규 메시지부터는 정확. 정밀 백필(시트→state JSON 임포트)은 선택(§3-4).
3. **답장 체인**: `msg.reference`는 직속 부모만 사용(루트까지 거슬러 올라가지 않음). 의도적 단순화.
4. **봇 다운 중 edit 누락**: Discord가 edit 히스토리를 주지 않아 catchup으로 복구 불가(답장은 복구됨). 운영 가이드로 "재시작 후 수정은 한 번 더" 안내.
5. **PROGRESS_PROMPT 비용**: 답장마다 ollama 1회. 답장 빈도 낮아 허용. 폭주 시 `extract_progress` 앞단에 키워드 사전필터(진행/완료/날짜 힌트 없으면 즉시 None) 추가 검토.
6. **보수 모드 5건 상한**: 초기 운영값. 정상 다중(주간계획 등)이 5건 초과면 확인필요로 빠짐 → 데이터 쌓이면 상한 상향 또는 제거.
