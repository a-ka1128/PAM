# 확인필요 처리 (시트 read-back) — 최종 설계 문서

> 멀티에이전트 설계 + 적대적 검증 결과 (2026-06-27). 일어나서 이 문서 보고 "시작"하면 됨.

## 개요

확인필요 탭에 쌓인 "추출 실패/작업자 불명확" 행을 PM이 손으로 보정하고, 봇이 ~3분마다 시트를 읽어 일정 탭으로 등록하거나 폐기하는 단방향→양방향 확장. **기존 webhook 단일 URL(`/exec`)만 사용**하며 GCP/서비스계정 불필요. doGet(읽기)·doPost(기존 upsert + 신규 delete)을 모두 같은 배포 URL에서 처리.

**적대적 검토에서 확정된 5대 차단 결함과 처리:**

1. **`_key`는 링크가 아니라 `str(msg.id)`** (bot.py L135, AppsScript.gs L54). → 전 구간 `key = msg.id` 문자열로 통일. 일정 push도 같은 msg.id를 key로 재사용 → 멱등 upsert.
2. **원본 URL은 `getValues()`로 못 읽음** (원본 컬럼은 `=HYPERLINK(...)` 수식, L45). → doGet이 `getFormulas()`로 URL을 정규식 추출해 `원본링크` 키로 별도 반환.
3. **doPost는 전체 행 덮어쓰기** (L38-41 `head.map`에서 없는 필드는 `""`). → 마킹을 doPost upsert로 하면 내용/추정작업자/사유가 소실됨. **멱등성 모델 = "행 삭제"로 확정**. delete만 부분-컬럼 영향 없음.
4. **`resortSchedule`이 매 일정 push마다 행 이동** (L57). → 시트 변경 엔트리포인트(doPost/doDelete/resortSchedule)를 `LockService.getScriptLock()`로 직렬화.
5. **정산-origin 확인필요 행 존재** (brain.py L135 `사유="정산 추출 실패..."`). → `사유` 문자열로 settle/schedule 재추출 분기. 재추출 입력은 시트에 저장된 **120자 절단본**(L120)뿐임.

---

## 전체 흐름

```
[실시간 경로 — 기존, 변경 없음]
 Discord msg ─ on_message ─ process() ─ brain.analyze
                                          ├─ 일정/정산 → sheet.push(key=msg.id)
                                          └─ 실패 → review() → 확인필요(key=msg.id, 사유 기록)

[read-back 경로 — 신규, @tasks.loop(seconds=180)]
 resolve_loop tick
   ├─ sheet.fetch("확인필요")  ──GET──▶  doGet(e)  [LockService]
   │     ◀── {ok, rows:[{key, 내용, 추정작업자, 추정의뢰자, 사유, 처리, 원본링크}]}
   │         (key=숨김 _key=msg.id, 원본링크=getFormulas로 추출한 URL)
   ├─ 행별 분기 (처리 칸 정확매칭, 사이클당 상한 N=10):
   │   처리=="무시" ──────────────────▶ delete_keys 큐
   │   처리=="등록":
   │     ├─ 추정작업자 빈칸 → skip(행 유지)
   │     ├─ 사유로 분기: "정산.." → _reextract_settle / 그외 → _reextract_task
   │     ├─ 재추출 None → skip(행 유지)
   │     └─ 재추출 OK:
   │           sheet.push(대상탭, key=msg.id, fields{..담당자=추정작업자, 링크=원본링크})
   │             └─ ok? YES → delete_keys 큐 (push 성공 후에만 삭제)
   │                    NO → alert_err (삭제 보류, 다음 사이클 재시도 = upsert 멱등)
   └─ sheet.delete_rows("확인필요", delete_keys) ──POST {action:"delete"}──▶ doDelete [Lock]
         (key 매칭, 행번호 내림차순 deleteRow)
```

---

## Apps Script 변경

(1) `doGet` 신규, (2) `doPost`에 `action:"delete"` 분기, (3) `ACTIONS`→`["등록","무시"]` + `fixActions()`. 모든 쓰기에 `LockService`.

```javascript
// ===== 전역: 드롭다운 교체 (기존 ["의뢰자","작업자","기타","무시"] → ) =====
var ACTIONS = ["등록", "무시"];

// ===== (1) doGet: 확인필요 탭 → JSON (원본 URL은 getFormulas로 추출) =====
function doGet(e) {
  var name = (e && e.parameter && e.parameter.sheet) || "확인필요";
  var out = { ok: true, sheet: name, rows: [] };
  if (name !== "확인필요") { out.ok = false; out.error = "not allowed"; return _json(out); } // 화이트리스트
  var head = TABS[name];
  var sh = SpreadsheetApp.getActiveSpreadsheet().getSheetByName(name);
  if (!sh) return _json(out);                       // 탭 미생성 → 빈 rows (정상)
  var keyCol = head.length + 1, last = sh.getLastRow();
  if (last < 2) return _json(out);

  var lock = LockService.getScriptLock();
  try { lock.waitLock(20000); } catch (err) { out.ok = false; out.error = "locked"; return _json(out); }
  try {
    var vals = sh.getRange(2, 1, last - 1, keyCol).getValues();
    var iSrc = head.indexOf("원본");
    var forms = (iSrc >= 0) ? sh.getRange(2, iSrc + 1, last - 1, 1).getFormulas() : null;
    var iC = head.indexOf("내용"), iW = head.indexOf("추정 작업자"),
        iR = head.indexOf("추정 의뢰자"), iRe = head.indexOf("사유"), iP = head.indexOf("처리");
    for (var r = 0; r < vals.length; r++) {
      var row = vals[r], key = row[keyCol - 1];
      if (key === "" || key === null) continue;     // 빈 행 방어
      var link = "";
      if (forms) {
        var m = String(forms[r][0] || "").match(/HYPERLINK\("([^"]+)"/i);  // URL만 추출
        if (m) link = m[1];
      }
      out.rows.push({
        key: String(key),
        "내용": String(row[iC] || ""),
        "추정작업자": String(row[iW] || ""),   // JSON 키는 공백 제거본 (봇 파서와 합의)
        "추정의뢰자": String(row[iR] || ""),
        "사유": String(row[iRe] || ""),
        "처리": String(row[iP] || ""),
        "원본링크": link
      });
    }
  } finally { lock.releaseLock(); }
  return _json(out);
}

function _json(o) {
  return ContentService.createTextOutput(JSON.stringify(o)).setMimeType(ContentService.MimeType.JSON);
}

// ===== (2) doPost: 최상단 action 분기 + Lock (기존 upsert 하위호환) =====
function doPost(e) {
  var d = JSON.parse(e.postData.contents);
  if (d.action === "delete") return doDelete(d);     // ← 신규 분기
  var lock = LockService.getScriptLock();
  lock.waitLock(20000);                              // ← upsert/resort 직렬화
  try {
    // ... 기존 upsert 로직 L21~57 그대로 (resortSchedule 포함) ...
    var head = TABS[d.sheet];
    if (!head) return ContentService.createTextOutput("unknown sheet: " + d.sheet);
    // (이하 변경 없음)
    if (head.indexOf("날짜") >= 0) resortSchedule();
    return ContentService.createTextOutput("ok:" + n);
  } finally { lock.releaseLock(); }
}

function doDelete(d) {
  var name = d.sheet || "확인필요";
  var head = TABS[name];
  if (!head) return ContentService.createTextOutput("unknown sheet");
  var keys = d.keys || [];
  if (!keys.length) return ContentService.createTextOutput("deleted:0");
  var keySet = {}; keys.forEach(function (k) { keySet[String(k)] = true; });
  var lock = LockService.getScriptLock();
  lock.waitLock(20000);
  try {
    var sh = SpreadsheetApp.getActiveSpreadsheet().getSheetByName(name);
    if (!sh) return ContentService.createTextOutput("deleted:0");
    var keyCol = head.length + 1, last = sh.getLastRow();
    if (last < 2) return ContentService.createTextOutput("deleted:0");
    var ks = sh.getRange(2, keyCol, last - 1, 1).getValues();
    var targets = [];
    for (var i = 0; i < ks.length; i++) {
      var k = ks[i][0];
      if (k !== "" && keySet[String(k)]) targets.push(2 + i);  // 실제 행번호
    }
    targets.sort(function (a, b) { return b - a; });            // 내림차순: 인덱스 밀림 방지
    targets.forEach(function (rn) { sh.deleteRow(rn); });
    return ContentService.createTextOutput("deleted:" + targets.length);
  } finally { lock.releaseLock(); }
}

// ===== (3) 기존 확인필요 탭 드롭다운 재적용 (에디터에서 1회 실행) =====
function fixActions() {
  var sh = SpreadsheetApp.getActiveSpreadsheet().getSheetByName("확인필요");
  if (!sh) return;
  var pi = TABS["확인필요"].indexOf("처리");
  if (pi < 0) return;
  sh.getRange(2, pi + 1, 5000, 1).setDataValidation(
    SpreadsheetApp.newDataValidation().requireValueInList(ACTIONS, true).build());
}
```

**주의:** doGet `waitLock` 실패 시 `{ok:false}` → 봇 그 사이클 skip. 배포는 기존 "모든 사용자(익명)" 유지. 코드 수정 후 **재배포(새 버전)** 필요 — doGet은 신규라 기존 배포에 자동 반영 안 됨.

---

## 봇 변경

`sheet.py`에 `fetch`+`delete_rows`, `bot.py`에 `resolve_loop`+재추출 헬퍼. `brain.py`는 **변경 없음**(심볼 재사용만).

### sheet.py 추가
```python
import urllib.parse  # 상단에 추가

def fetch(sheet):
    """doGet으로 확인필요 탭 읽기. 성공: rows 리스트, 실패: {'err':..}"""
    url = config.WEBHOOK_URL + "?sheet=" + urllib.parse.quote(sheet)
    try:
        with urllib.request.urlopen(url, timeout=30) as r:
            data = json.loads(r.read().decode("utf-8", "replace"))
        if not isinstance(data, dict) or not data.get("ok"):
            return {"err": (data.get("error") if isinstance(data, dict) else "bad-json")}
        return data.get("rows", [])
    except Exception as e:
        return {"err": str(e)}

def delete_rows(sheet, keys):
    """doPost(action=delete)로 _key 일치 행 삭제."""
    if not keys:
        return None
    body = json.dumps({"sheet": sheet, "action": "delete", "keys": keys},
                      ensure_ascii=False).encode("utf-8")
    req = urllib.request.Request(config.WEBHOOK_URL, data=body,
        headers={"Content-Type": "application/json; charset=utf-8"})
    try:
        with urllib.request.urlopen(req, timeout=30) as r:
            return r.read().decode("utf-8", "replace")
    except Exception as e:
        return f"err:{e}"
```

### bot.py 추가 (reminder_tick 패턴 복제, L226 아래)
```python
_resolve_busy = False
RESOLVE_BATCH = 10  # 사이클당 등록 처리 상한 (ollama 호출이라 느림)

def _reextract_task(content):
    """일정 재추출. analyze() 우회 — 작업자 판별 실패가 애초 플래그 원인이므로 DEADLINE만."""
    out = brain.ollama(brain.DEADLINE_PROMPT.format(m=(content or "")[:400]))
    if "|" not in out or out.startswith("없음"):
        return None
    p = [x.strip() for x in out.split("|")] + ["", ""]
    if not p[0] or p[0] in brain.LABELS:
        return None
    return ("일정", {"날짜": brain.norm_date(p[1]), "작업": p[0], "상태": "진행중"})

def _reextract_settle(content):
    """정산 재추출. 사유가 '정산...'인 행 전용."""
    out = brain.ollama(brain.SETTLE_PROMPT.format(m=(content or "")[:400]))
    if "|" not in out or out.startswith("없음"):
        return None
    p = [x.strip() for x in out.split("|")] + ["", "", ""]
    if not p[0] or p[0] in brain.LABELS:
        return None
    return ("정산", {"항목": p[0], "금액": brain.norm_amount(p[1]),
                     "상태": brain.norm_settle_status(p[2])})

@tasks.loop(seconds=180)
async def resolve_loop():
    global _resolve_busy
    if _resolve_busy:               # 느린 사이클 겹침 방지
        return
    _resolve_busy = True
    try:
        rows = await asyncio.to_thread(sheet.fetch, "확인필요")
        if isinstance(rows, dict):  # {'err':..} → 사이클 전체 skip (부분 행동 금지)
            await alert_err("confirm_fetch", f"⚠️ 확인필요 읽기 실패: {str(rows.get('err',''))[:100]}")
            return
        delete_keys, done = [], 0
        for row in rows:
            key = row.get("key")
            action = (row.get("처리") or "").strip()
            if not key or action not in ("등록", "무시"):
                continue            # 빈칸/오타 → no-op
            if action == "무시":
                delete_keys.append(key)
                continue
            # 등록
            if done >= RESOLVE_BATCH:
                break               # 나머지는 다음 사이클
            worker = (row.get("추정작업자") or "").strip()
            if not worker:
                continue            # 작업자 비면 행 유지
            content = row.get("내용") or ""
            reason  = row.get("사유") or ""
            link    = row.get("원본링크") or ""
            try:
                ext = await asyncio.to_thread(
                    _reextract_settle if reason.startswith("정산") else _reextract_task, content)
            except Exception:
                await alert_err("ollama", "⚠️ 재추출 실패 — Ollama 확인 필요")
                continue
            if not ext:
                continue            # 재추출 실패 → 행 유지
            tab, fields = ext
            if tab == "일정":
                fields["담당자"] = worker
            else:                   # 정산
                fields["받는이"] = worker
            fields["링크"] = link
            r = await asyncio.to_thread(sheet.push, tab, [{"key": key, "fields": fields}])
            done += 1
            if isinstance(r, str) and r.startswith("ok"):
                delete_keys.append(key)        # push 성공 후에만 삭제 큐
                log.info(f"resolve(등록→{tab}): {key} {fields.get('작업') or fields.get('항목')} / {worker}")
            else:
                await alert_err("confirm_push", f"⚠️ 등록 실패(삭제 보류): {str(r)[:100]}")
        if delete_keys:
            await asyncio.to_thread(sheet.delete_rows, "확인필요", delete_keys)
    except Exception:
        log.exception("resolve_loop 실패")
    finally:
        _resolve_busy = False

@resolve_loop.before_loop
async def _before_resolve():
    await client.wait_until_ready()
```

### 정확한 통합 지점
- **on_ready (reminder_tick start 블록 뒤)**: `if not resolve_loop.is_running(): resolve_loop.start()`
- **resolve_loop은 `state.set_kv("last_seen", ...)`를 절대 호출하지 않음** — catchup 기준 시각 오염 방지 (reminder_tick L211 베끼지 말 것)
- **담당자/받는이 주입은 봇 측 fields 오버라이드** — GAS는 받은 fields 그대로 upsert하므로 GAS 변경 불필요

---

## 데이터모델·키·멱등성

| 항목 | 결정 | 근거 |
|---|---|---|
| 멱등 키 | `key = str(msg.id)` 전 구간 통일 | bot.py L135, AppsScript.gs L54. 일정 push도 같은 msg.id → 재처리해도 upsert(덮어쓰기)라 중복행 없음 |
| 원본 URL | doGet `getFormulas()`+정규식 추출 → `원본링크` | 원본은 `=HYPERLINK("URL","원본")` 수식. getValues는 "원본"만 반환. 봇은 guild/channel id를 모름 |
| 멱등성 모델 | **행 삭제** (마킹 아님) | 마킹을 upsert로 하면 doPost 전체-행-덮어쓰기로 내용/사유 소실 |
| push/delete 순서 | 재추출→push→`startswith("ok")`확인→delete 큐→일괄 delete | 삭제 먼저 하면 push 실패 시 소실. 부분실패는 다음 사이클 재처리(msg.id 멱등=무해). **effectively-once** |
| delete 매칭 | _key(msg.id) 기준, 행번호 X | PM 동시편집/행추가로 인덱스 어긋나도 정확. 내림차순 deleteRow |
| 재추출 분기 | `사유.startswith("정산")`→SETTLE, 그외→DEADLINE | 정산-origin을 일정으로 보내면 오분류 |
| 재추출 입력 | 시트 저장 120자 절단본 | review() L120. 뒷부분 날짜/금액 잘렸을 수 있음(품질 한계 수용) |

---

## 엣지케이스 대응표

| 증상 | 대응 |
|---|---|
| 재추출 None/"없음"/LABELS echo | push 안 함, 행 유지. 빈 행 생성 X |
| 등록인데 추정작업자 빈칸 | skip(행 유지) |
| push성공+delete실패(부분실패) | delete 큐에 남아 다음 사이클 재시도. push는 멱등이라 무해 |
| PM이 폴링 도중 편집 | delete는 key 기준이라 정확. 작업자는 스냅샷 값(last-write-wins) |
| resortSchedule↔doDelete 동시 | 모든 쓰기 `LockService.waitLock(20000)` 직렬화 |
| doGet 타임아웃/HTML/깨짐 | fetch가 `{err}` → 사이클 전체 skip + alert_err(10분 rate) |
| 확인필요 대량 | 사이클당 `RESOLVE_BATCH=10`, 나머지 다음 사이클. `_resolve_busy` 겹침 방지 |
| 처리값 오타/빈칸 | `("등록","무시")` 정확매칭만 트리거 |
| 정산-origin 등록 | `사유` 분기로 정산 탭, 작업자=받는이 주입 |
| 봇 재시작 중복 폴링 | socket 락(47291)+`is_running()` 가드. 처리된 행은 이미 삭제 |
| 날짜 없는 등록 | 허용 → sortKey 2099로 맨뒤(L130) + 로그 (미결정 #1 참조) |
| msg.id 충돌 | Discord snowflake 전역 유일 |

---

## 단계별 구현 순서 (체크리스트)

- [ ] 1. AppsScript.gs — `ACTIONS = ["등록","무시"]` 교체 (L15)
- [ ] 2. AppsScript.gs — `doGet(e)`+`_json()` 추가 (getFormulas 원본링크, 화이트리스트)
- [ ] 3. AppsScript.gs — `doPost` 최상단 `if(d.action==="delete") return doDelete(d);` + LockService 래핑
- [ ] 4. AppsScript.gs — `doDelete(d)` 추가 (key 매칭, 내림차순 deleteRow, Lock)
- [ ] 5. AppsScript.gs — `fixActions()` 추가
- [ ] 6. 시트 에디터에서 `fixActions()` 1회 실행 (안 하면 PM이 '등록' 입력 불가)
- [ ] 7. 웹앱 새 버전 재배포 (doGet 반영). 배포 URL=기존 WEBHOOK_URL 동일 확인
- [ ] 8. sheet.py — `import urllib.parse` + `fetch()` + `delete_rows()`
- [ ] 9. bot.py — 재추출 헬퍼/`_resolve_busy`/`RESOLVE_BATCH`/`resolve_loop`/`before_loop` 추가
- [ ] 10. bot.py — on_ready에 `resolve_loop.start()` 가드
- [ ] 11. 배포 전 기존 '무시' 값 행 검토 (옛 드롭다운 '무시'가 첫 가동 시 즉시 삭제될 수 있음 — PM 사전 정리)
- [ ] 12. resolve_loop에 last_seen set_kv 없음 재확인

---

## 테스트 방법
- **A. doGet 단독:** `WEBHOOK_URL?sheet=확인필요` GET → `{ok,rows}`, `원본링크`에 실제 URL·`key`가 숫자 msg.id 확인. `?sheet=일정`→`not allowed`
- **B. doDelete 단독:** `{"sheet":"확인필요","action":"delete","keys":["<msg.id>"]}` → `deleted:1`. 없는 key 혼합 → 존재만 삭제, 행 안 어긋남
- **C. 하위호환:** 기존 push → 여전히 `ok:1`
- **D. 등록 e2e:** 행 생성→작업자 채우고 등록→3분 내 일정 추가+행 삭제
- **E. 정산 분기:** `사유=정산..`+등록 → 정산 탭으로
- **F. 무시:** 행 삭제
- **G. 실패:** Ollama 끄고 등록 → 행 유지+alert 1회, 스팸 없음
- **H. 동시성:** push 도는 중 doDelete → Lock 직렬화
- **I. last_seen:** resolve 여러 사이클 후 재시작 → catchup 정상

---

## 미결정 / 유저 확인 필요
1. **날짜 없는 등록 허용?** — 현재 "허용+맨뒤정렬+로그". 거부 원하면 정책 변경
2. **옛 '무시' 값 처리** — 사전 정리 vs 첫 사이클 가드, 택1
3. **doGet 익명 노출 vs 토큰** — 현재 익명(기존 webhook과 동일). 쿼리 토큰 추가 가능
4. **등록 성공 Discord 피드백** — 현재 로그만. OWNER DM/원본 답장 원하는지
5. **배포 URL 동일성** — 재배포 후 GET이 같은 /exec에서 되는지 1회 확인
