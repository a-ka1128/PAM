# 룩플/LooKP PM봇 — 기능 업그레이드 큐레이션 메뉴

> 대상: VTuber 3D 외주 스튜디오 디스코드 PM 자동화 봇 (추출봇 + 리마인더봇)
> 제약: 프라이버시(본문 로컬) · 8GB VRAM(exaone3.5:7.8b ~5.2GB 상주) · 1인 유지(파이썬 중급) · temperature 0 결정성
> 방식: 6개 탐색가(dedup/parse/reliability/memory/domain/sota) × 3렌즈 심판(ROI/제약적합/리스크) 통합. 코드 실측 검증 완료.

---

## 0. 한 줄 결론

이 봇의 다음 도약은 **새 AI 모델·벡터·확률 계열이 아니다.** 그것들은 8GB·프라이버시·1인유지·**replay 결정성 불변식**과 전부 충돌한다. 진짜 지배 전략은:

> **새 모델을 상주시키지 않고, `ollama()` body·`canon()`·`_core_sim_ok`·SQLite·시트 read에 얹히는 결정적·저비용 개선.** 그 정점이 Structured Output이고, 그 앞에 반드시 골든셋이 온다.

---

## 1. 한눈 요약 표

| # | 후보 | 영역 | 한 줄 | 가치/공수 | 제약OK? | 판정 |
|---|---|---|---|---|---|---|
| 1 | 회귀 골든셋 eval | 신뢰성 | 입력→기대출력 쌍 자동 채점, 회귀 감지 | 고/저 | ✅✅✅ | ⭐**1순위** |
| 2 | 다이제스트 DM | 도메인 | 시트 3탭 집계→PM 아침브리핑 1통 | 고/저 | ✅✅✅ | ⭐강추 |
| 3 | Structured Output | 파싱 | `format`에 JSON schema→파이프붕괴 구조적 차단 | 고/중 | ✅✅✅ | ⭐강추(조건) |
| 4 | D-day 이모지 자동선택 | 도메인 | 🔴🟡🔵 3개 다 붙이던 걸 마감거리로 1개 | 중/저 | ✅✅✅ | ⭐강추 |
| 5 | 정확일치 프롬프트 캐시 | 신뢰성 | temp0라 same-prompt=same-out, catchup 재생 급감 | 중/저 | ✅✅✅ | ⭐강추(조건) |
| 6 | 미수금 나이 추적 | 도메인 | 미정산 행 나이순 정렬→다이제스트 승격 | 고/저 | ✅✅△ | ⭐강추 |
| 7 | 자모 유사도 백스톱 | 중복방지 | `_core_sim_ok`에 한글 자모 유사도 게이트 추가 | 중/저 | ✅✅✅ | 🤔조건부 |
| 8 | 별칭 사전 | 중복방지 | PM 승인 동의어를 결정적 병합 자산으로 | 중/중 | ✅✅△ | 🤔조건부 |
| 9 | 감사 로그 (add-on) | 메모리 | 트랜잭션에 이벤트 INSERT 1줄, PM 문의 대응 | 중/저 | ✅✅✅ | 🤔상황부 |
| 10 | 능동학습(확인필요→사전) | 신뢰성 | PM 교정값을 별칭/힌트 사전에 되먹임 | 중/중 | ✅✅△ | 🤔상황부 |
| 11 | 워크로드 카운트 | 도메인 | 담당자별 미완료/임박 건수 집계 | 중/저 | ✅✅✅ | 🤔상황부 |
| 12 | 의뢰자 컬럼 저장 | 도메인 | 지금 뽑고 버리는 client를 시트에 저장만 | 중/저 | ✅✅✅ | 🤔"지금 심기" |
| — | 임베딩/RAG/시맨틱캐시 | 전영역 | 벡터 유사도 매칭 | — | ❌ | ❌기각 |
| — | 신뢰도 자동 상태변경 | 신뢰성 | 낮은 신뢰도→자동 완료/변경 | — | ❌ | ❌기각(강) |
| — | logprobs 신뢰도 게이팅 | 파싱 | 토큰 확률로 애매건 회송 | — | △ | ❌기각 |
| — | GLiNER 한국어 NER | 파싱 | CPU NER 프리패스 | — | △ | ❌기각(미래) |
| — | VLM 도면분석 | 도메인 | 첨부 이미지 비전분석 | — | ❌ | ❌기각(HW벽) |
| — | 신모델 교체(EXAONE4/Qwen3) | 파싱 | exaone3.5 대체 | — | ❌ | ❌기각 |
| — | self-consistency/앙상블 | 신뢰성 | N회 샘플 다수결 | — | ❌ | ❌기각 |
| — | LLM-as-judge | 신뢰성 | 추출을 같은 LLM에 재검증 | — | ❌ | ❌기각 |
| — | MinHash/LSH · Bitemporal · Splink | 중복/메모리 | 대규모 링키지·양시간 | — | △ | ❌기각(과잉) |
| — | 정산 답장 자동매칭 · 상담 자동응대 | 도메인 | 정산 라이프사이클·챗봇 | — | ❌ | ❌기각 |

제약OK 열: [프라이버시 / 8GB·1인유지 / 데이터·전제확인]. △ = 착수 전 확인 1건 필요.

---

## 2. ⭐ 강력 추천 (적은 공수·큰 이득·제약 통과)

> 정직한 트레이드오프: 아래 6개도 "공짜"는 아니다. 각 주의점을 반드시 읽을 것. 특히 3·5·6은 "조건부 강추"다.

### ① 회귀 골든셋 eval — **무조건 1순위 (다른 모든 것의 안전망)**

- **한 줄 정의**: 손으로 라벨링한 `입력→기대출력` 쌍을 프롬프트/모델 변경 때마다 자동으로 돌려 회귀를 수치로 잡는 오프라인 테스트.
- **이 봇 적용점**: 이미 있는 `test_brain.py`·`test_snap_state.py` 확장. `golden/deadline.jsonl`, `golden/settle.jsonl`, `golden/same_task.jsonl`(후보목록+정답번호), `golden/route.jsonl`. `norm_date`/`norm_amount`는 LLM 없이 순수 단위테스트로 100% 커버. LLM 경로는 통과/실패 카운트 + 실패 diff만 출력.
- **현재 대비 이득**: 지금은 프롬프트 예시 한 줄 고칠 때 "안 깨졌나?"를 **감으로** 판단. 특히 `SAME_TASK_PROMPT`/`REPLY_ROUTE_PROMPT`는 예시 하나 추가가 다른 판정을 흔드는 취약 지점. 골든셋이 있으면 "예시7 추가했더니 예시2가 깨졌다"를 즉시 발견.
- **공수**: 골든셋 30~60줄 초기 작성 + 오답 나올 때 한 줄씩 추가. 프로덕션 코드 무변경.
- **⚠️ 주의**: temperature 0이라도 LLM 경로는 완전 결정적이 **아니다**. **hard assert 금지** — 통과/실패 카운트와 실패 케이스 diff만. 아래 ③·⑤·⑦·⑧을 넣기 전에 **반드시 이걸 먼저** 깔아라. 이게 "지금 잘 되는 걸 안 깨뜨렸다"의 유일한 증거다.

---

### ② 다이제스트 DM — **최고 ROI, LLM 0회, 프라이버시 무해**

- **한 줄 정의**: 시트 `일정`/`정산`/`확인필요` 탭을 매일(또는 매주) 집계해 PM에게 DM 1통 요약.
- **이 봇 적용점**: `bot.py`에 `@tasks.loop(hours=24)` 하나 추가. `resolve_loop`가 이미 `sheet.fetch("확인필요")`를 도는 패턴 그대로 `fetch("일정")`/`fetch("정산")` 읽어 순수 파이썬 집계(`collections.Counter`). 출력은 기존 `alert(text)` DM 재사용.
  - 오늘~3일 내 마감 / 지난 마감인데 상태≠완료(연체) / 미정산 금액 합계(`norm_amount` 정수문자열 `int()` 합산) / 어제 완료 건수.
- **현재 대비 이득**: 지금 PM은 시트 3탭을 눈으로 스캔해야 상황을 안다. 리마인더봇은 개별 작업자 재촉만 하지 **PM 관점 전체 조망이 0**. "오늘 뭐부터 봐야 하나"를 봇이 대신 정렬.
- **공수**: ~60줄, LLM 호출 0회, 본문 재전송 0(이미 시트에 있는 파생값만).
- **✅ 전제 확인 완료**: `sheet.fetch(sheet)`는 **탭 전체 rows 리스트를 반환**(sheet.py L31 `data.get("rows", [])`), 실패 시 `{"err":...}` dict. → 성공 시 list, 실패 시 dict이므로 `isinstance(rows, dict)`면 조용히 skip + `alert_err` 재사용.
- **⚠️ 주의**: `resolve_loop`와 **같은 스레드풀/시트 웹훅 경합** — loop 간격을 겹치지 않게. 정산·일정 탭은 누적되면 확인필요보다 행이 많으니 응답 크기 확인.

---

### ③ Structured Output (format=JSON schema) — **파싱 견고성 최대, 단 "교체" 아니라 "얹기"**

- **한 줄 정의**: 프롬프트로 "형식 지켜줘" 부탁 대신, `format`에 JSON schema를 넘겨 llama.cpp가 문법 제약 디코딩(GBNF)으로 스키마 밖 토큰을 원천 차단.
- **검증된 사실(sota)**: Ollama structured output은 `format` 필드에 JSON schema 객체를 넣으면 llama.cpp가 GBNF 문법을 자동 생성해 토큰 샘플링을 제약. `/api/generate`·`/api/chat` 둘 다 raw HTTP로 지원(v0.5.0+, 2024.12 도입). required/enum/array 지원. **로컬 전용**(Cloud 미지원 — 이 봇은 로컬이라 무관). 첫 요청 스키마→FSM 컴파일 50~200ms, 이후 캐시.
- **이 봇 적용점**: `ollama()`(brain.py L44)에 `fmt=None` 인자 추가 → body에 `"format": fmt` 키 하나. `DEADLINE_PROMPT`의 `작업내용|기한|진행` 파이프를 `{"items":[{"task","due","progress"}]}` 스키마로. `SETTLE_PROMPT`의 `상태`는 `enum:["완료","미정산","불명"]`로 잠금. **Pydantic 불필요** — `json.dumps`로 dict schema 직접 삽입(의존성 0).
- **현재 대비 이득**:
  - 라벨-에코 방어(`LABELS` 셋)가 **구조적으로 소멸** — `task` 값에 "작업내용:" 라벨이 통째로 나오는 실패 모드가 문법상 불가능.
  - 파이프 개수 어긋남(`| |` 누락/과다) 원천 차단.
  - `상태` enum이 `norm_settle_status`의 문구 술래잡기를 모델 레벨에서 끝냄.
- **공수/제약**: 추가 모델·VRAM 0. temperature 0과 결합 시 결정성 최상.
- **⚠️ 주의 (심판이 강하게 경고한 지점 — 반드시 지킬 것)**:
  1. **파이프 파서를 "제거"하지 마라.** `parse_deadline_lines`의 `(작업,기한)` dedup·`_DATEONLY_RE` 날짜만-줄 폐기·환각 가드는 **JSON으로도 남는다**(스키마는 array 중복을 못 막음). "코드가 준다"는 과장.
  2. **`enum`에 반드시 `"불명"` 포함** — 강제 enum은 애매할 때 억지로 하나 고르게 만든다. `"불명"`이 있어야 확인필요 큐로 흐른다.
  3. **폴백 병행**: `json.loads` 실패 시 기존 파이프 파서로. 단 이건 "1줄 안전망"이 아니라 **프롬프트 자산 2벌 유지**임을 인지.
  4. **exaone 한국어 값의 JSON 이스케이프 안정성 미검증** — `"작캠용 바디 수정 (개인일정)"` 같은 괄호/특수문자가 값에 들어가면 깨질 수 있고, temp0라 한 번 깨지면 항상 깨진다(조용한 편향). **①골든셋으로 즉시 실측하라.**
  5. **도입 순서 강제**: ①골든셋 고정 → `analyze` 일정 경로 **하나에서만** A/B → 통과 후 `resolve_loop`·`extract_progress`·`_reextract_settle` 4개 호출부 일괄 전환. 하나 놓치면 실시간/확인필요가 다른 포맷을 냄.
- **출처**: [Ollama Structured Outputs 문서](https://docs.ollama.com/capabilities/structured-outputs), [Ollama Blog](https://ollama.com/blog/structured-outputs), [재검증 미비 함정](https://blog.danielclayton.co.uk/posts/ollama-structured-outputs/)

---

### ④ D-day 기반 이모지 자동선택 — **재촉 강도 자동화, 15줄**

- **한 줄 정의**: 지금 사람이 손으로 클릭하는 🔴🟡🔵을, 정규화된 날짜의 D-day 거리로 봇이 골라 부착.
- **이 봇 적용점**: `process()`의 이모지 블록(bot.py L231 `for e in config.PRIORITY: await msg.add_reaction(e)` — 3개 다 붙임). `items[0]["날짜"]`는 이미 `norm_date`로 YYYY-MM-DD. `_is_past`와 같은 파서로 D-day 계산 → D-2 이내 🔴, D-7 이내 🟡, 그 외 🔵.
- **현재 대비 이득**: 현재는 **3개 다 붙이고 사람이 하나 고른다**(리마인더봇이 클릭된 이모지만 추적). 등급 판단이 사람 머릿속에. 날짜 명확한 건은 봇이 미리 골라 재촉 강도 자동 결정 → 사람 개입 1스텝 제거.
- **공수**: ~15줄. 리마인더봇 인터페이스 **무손상**(봇이 골라줘도 사람이 다른 걸 클릭하면 그대로 존중됨).
- **⚠️ 주의**: **여러 일정(items 다건)일 때 어느 날짜 기준인가?** `items[0]`만 보면 오분류 → 다건이면 **가장 임박한 것** 기준. "자동부착 이모지를 사람 클릭 없이 추적 시작"까지는 **권장 안 함**(사람의 명시 승인 게이트는 오배정 방지에 안전) — 이모지 "선택"만 자동화가 스위트스팟.

---

### ⑤ 정확일치 프롬프트 캐시 — **catchup/backfill 재생 비용 급감**

- **한 줄 정의**: 이미 추출한 프롬프트와 **완전히 같은** 입력이 오면 LLM을 안 부르고 캐시된 raw 출력 재사용.
- **이 봇 적용점**: `ollama(prompt)`(brain.py L44) 바로 앞에 `sha1(config.MODEL + prompt)` → `response`를 SQLite `llm_cache(hash, response, created)`에 저장. 진짜 효과 지점은 **`catchup()`/`backfill_cmd` 재생** — 재접속·백필 시 같은 본문이 같은 프롬프트를 만들어 똑같은 LLM 호출을 반복. 스냅샷 replay도 프롬프트 동일 → 히트.
- **현재 대비 이득**: catchup/backfill이 지금 모든 메시지에 exaone 실호출 → 수백 건이면 수 분~수십 분. 캐시로 재실행 급감. 실시간 경로는 히트율 낮지만 손해 0.
- **공수**: DB 한 테이블 + 래퍼 4줄. temperature 0이라 정확일치는 100% 안전.
- **⚠️ 주의 (심판 지적)**:
  - **캐시 대상은 `ollama()` raw 출력만.** `norm_date`가 프롬프트 **밖에서** "내일/이번주 금요일"을 실행시점 기준 정규화하므로(brain.py L99~), 정규화는 매번 재실행해야 날짜가 안 틀린다.
  - 캐시 키에 **`config.MODEL` 필수 포함**(모델 교체 시 stale 방지).
  - "시맨틱(임베딩) 캐시"는 **기각** — VRAM 경합 + 유사도 오탐이 잘못된 재사용.
  - 우선순위는 ①골든셋 뒤. 하루 수백 건이면 캐시 없이도 견디니, catchup/backfill이 실제 병목인지 로그로 확인 후.

---

### ⑥ 미수금 나이 추적 — **실손실 방지, msg_id 시각역산으로 데이터갭 해결**

- **한 줄 정의**: `정산` 탭에서 상태=`미정산` 행을 나이(경과일)와 함께 추적, 오래된 미수금을 다이제스트에 승격.
- **이 봇 적용점**: ②다이제스트의 한 섹션. `norm_settle_status`가 이미 완료/미정산 이진 분류를 하므로 필터 재료 있음. "미정산 N건 / 합계 X원 / 최장 대기 Y일".
- **현재 대비 이득**: 현재 정산은 **한 번 쓰고 끝** — "입금완료" 답장이 나중에 와도 연결 로직이 없어 미수금이 시트 안에서 조용히 썩는다. 이 업종은 소액 다건 외주라 **미수금 누락 = 실손실**.
- **공수**: 집계 LLM 0회.
- **⚠️ 주의 (데이터갭 + 심판 보정)**: 정산 행에 생성일 컬럼이 없으면 **Discord 스노우플레이크 역산** `(msg_id >> 22) + 1420070400000` = epoch ms. 정산 key가 `{msg.id}#0`(bot.py L243)이라 역산 성립. **단**, `resolve_loop`로 들어온 정산은 key base가 확인필요 **원본 메시지** id(bot.py L670)라 = 생성시각이 아니라 원본 시각이 잡힌다. "미수금 나이"에 미묘한 오차 → 정밀한 나이가 필요하면 시트에 생성일 컬럼 추가가 정공법.
- 정산에 "입금완료" 후속 답장을 자동 매칭하는 것까지는 **기각**(아래 ❌ 참조) — 나이 추적이 90% 실익을 훨씬 싸게 커버.

---

## 3. 🤔 상황부/실험적 (조건 맞으면 가치)

> 전부 매칭 코어(`canon` 1단 완전일치·`_core_sim_ok` 검증된 임계·replay 재현성)를 건드릴 수 있어 **①골든셋 선행 + 아래 조건 준수** 필수.

- **⑦ 자모 유사도 백스톱** — `_core_sim_ok`(L385, 임계 0.34)가 짧은 작업명("채색1차" vs "채색 1차 작업")에서 불안정한 걸 자모 단위(유니코드 산술 20줄, 의존성 0)로 안정화. **조건: `_core_sim_ok`를 교체하지 말고 AND 게이트로 추가**(bigram도 통과 AND 자모도 통과여야 병합). `canon()`은 절대 불변 — 건드리면 `CANON_VERSION`↑ → `on_ready`의 `reg_recanon`이 전체 레지스트리 재계산(재부팅 한 번에 매칭 대량 이동). `test_brain.py`의 유사도가드 케이스("카조에님 리깅" vs "의상") 고정.

- **⑧ 별칭 사전** — PM이 승인한 동의어("모델링=모델", "썸네일=섬네일")를 결정적 병합 자산으로. LLM `SAME_TASK_PROMPT` 의존을 결정적으로 줄임. **조건: `canon()`에 삽입 금지**(1단 완전일치에서 백스톱 우회 → LLM 판정 없이 조용히 과병합). 대신 **2단 LLM 진입 직전 후보 정규화**로만. 축적은 PM 명시 승인분만(자동학습 금지). ⚠️ 현재 확인필요는 `등록/무시` 2액션뿐(bot.py L609) — "이 둘 같은 작업" 승인 훅은 **신규 UI + resolve_loop 분기 신설**이지 "얹기"가 아님.

- **⑨ 감사 로그 (add-on만)** — `reg_apply_snapshot`/`reg_set_status`/`reg_revoke_snapshot` 트랜잭션 끝에 이벤트 INSERT 1줄(이미 열린 트랜잭션이라 원자성 공짜). "이 작업 왜 자동완료됐지?" PM 문의·버그 사후분석에 결정적. **조건: 상태 도출 로직 무손상**(완전 이벤트소싱=`reg_apply_snapshot` 전면 재작성은 기각). 트랜잭션 실패 시 로그도 롤백됨을 이해하고 넣을 것.

- **⑩ 능동학습 (확인필요→사전)** — 지금 `resolve_loop`(bot.py L593~)이 등록 처리 후 재추출→push로 끝나고 **학습이 없다**(확인필요 큐 = 일회성 소각장). PM 교정값을 `learned_hints` 테이블에 저장 → 다음엔 결정적 매칭으로 LLM 없이 해소. **조건: 사전 축적만 강추, few-shot 자동주입은 결정성 위반(replay 재현성)이라 수동 승인 게이트 한정.** ⑧과 짝.

- **⑪ 워크로드 카운트** — `일정` 탭을 `담당자`로 group-by, 1인당 미완료/임박 건수. ②다이제스트의 한 섹션. **조건: 숫자만 표시, "번아웃" 자동판정은 기각**(심리 단정=오해·팀 신뢰 훼손, 30명이면 PM이 숫자 보면 충분).

- **⑫ 의뢰자 컬럼 저장 ("지금 심어두기")** — 현재 `find_client`가 의뢰자를 뽑아놓고 `일정` 탭에 안 넣어 **유실 중**(`analyze`에서 뽑고 버림). 지금 `"의뢰자"` 필드로 저장만 해두면(items 1줄 + 시트 헤더 1개) 미래 기능(클라이언트별 미수금·이력)의 공통 재료가 된다. **조회 UI는 나중, 저장만 지금.**

---

## 4. ❌ 기각 (냉정하게)

| 후보 | 사유 |
|---|---|
| **임베딩/벡터DB 코사인 매칭** | 방향이 반대 — 이 봇은 "1차/2차·여름/겨울·앞/뒷면"을 **구분**해야 하는데(`SAME_TASK_PROMPT` 실증 예시) 코사인은 붙인다. CPU로 VRAM을 피해도 **알고리즘 부적합**이 상위 사유. |
| **RAG/동적 few-shot/시맨틱 캐시** | **replay 결정성 불변식 위반**(진짜 벽). `reg_channel_items` 정렬이 "같은 스냅샷=같은 매칭"에 의존 → 벡터/사례 상태 의존 판정은 재생 시 다른 결과 → `prev_*` 되감기·`deleted_new` 어긋남. 8GB가 풀려도 도입 불가. 정적 few-shot 예시 몇 개 추가가 90% 대체(비용 0). |
| **신뢰도→자동 상태변경** | 사용자가 **2026-07-03에 `allow_missing=False`로 자동완료를 껐다**(bot.py L361, 코드로 확인). 자동 상태 전이 부활은 방금 내린 결정과 정면 충돌. 신뢰도는 **확인필요 회송까지만** 허용. |
| **logprobs 신뢰도 게이팅** | `parse_same_task`가 `"-> 3"`, `"몰라요"` 등 한국어 장식 자유텍스트를 뱉어 "번호 토큰 특정"이 토크나이저 분할로 불가. Structured Output 도입 시 출력 구조 또 바뀌어 이중 재작성. 인프라 0비용이나 얹힘 난도가 1인 유지 초과. |
| **GLiNER 한국어 NER** | exaone과 역할 중복 + torch/transformers 2모델 파이프라인 유지비. Structured Output 도입 후 한계이득 축소. (검증: taeminlee/gliner_ko F1 75.99%, CPU 가능하나 CC-BY-NC — exaone도 NC라 라이선스는 기존 수용 제약, 진짜 사유는 유지비.) 인물 경계가 실제 병목으로 판명되면 PERSON 스팬만 미래 국소 도입. |
| **VLM 도면분석** | **8GB 물리적 벽.** exaone ~5.2GB 상주 + VLM 최소 4~7GB = 동시 상주 불가, 매번 언로드/리로드로 응답성 붕괴. 남는 2.8GB 실용 VLM 없음. |
| **신모델 교체(EXAONE4.0 32B/1.2B, Qwen3)** | 32B=8GB 물리 불가(양자화도 18GB+). 1.2B=다중필드 추출 정확도 하락. Qwen3(Apache 2.0)는 라이선스 이점 있으나 한국어 추출이 exaone3.5(어휘 50% 한국어)를 능가한다는 직접 벤치 없음. 교체=프롬프트·파서·골든셋 전면 재튜닝 = 최대 회귀면. **exaone3.5 유지가 기본값.** |
| **self-consistency/앙상블/투표** | temperature 0이라 same-prompt=same-out → 투표가 원리적으로 무의미. temp>0으로 다양성 만들면 결정성 아키텍처(replay·dedup 재현성) 파괴 + 호출 N배. |
| **LLM-as-judge (자기검증)** | 호출 2배 + 판정자도 같은 exaone 7.8b라 자기 오류를 자기가 못 잡는 상관오류. `_core_sim_ok`·`LABELS` 결정적 가드가 같은 목적을 호출 증가 없이 달성. |
| **MinHash/LSH** | 채널당 후보 수십 개에 서브선형 도구는 오버엔지니어링 교과서 사례. 완전탐색이 이미 즉각적. |
| **Bitemporal(양시간)** | 모든 CRUD가 버전 체이닝으로 2배 복잡. 필요 시간정보는 ⑨감사로그가 훨씬 싸게 100% 커버. 1인 스터디에 실패율 높은 함정. |
| **Fellegi-Sunter/Splink/dedupe/recordlinkage** | 라벨된 학습셋 없음(30명이면 영영 안 모임) + 대규모 정형 DB 링키지용 = 규모 미스매치. 정신만 ⑦/게이트에 경량 차용. |
| **정산 답장 자동매칭** | 정산엔 스냅샷/라우팅 엔진이 없어 신규 엔진 필요(복잡도↑) + **돈 오매칭 리스크 > 이득**. ⑥미수금 나이 추적이 실익 90%를 싸게 커버. |
| **상담 자동응대** | 로컬 exaone로 대외 고객 응답 생성 = 오답/부적절 발언 시 되돌릴 수 없는 신뢰 훼손 + 봇 정체성(추출)에서 이탈 + 1인 유지 불가. 상담 채널 **신규 문의 건수 카운트만** 다이제스트에 포함 가능(요약 생성은 기각). |

---

## 5. 영역별 톱픽 1개

| 영역 | 톱픽 | 이유 |
|---|---|---|
| **중복방지** | ⑦ 자모 유사도 백스톱 | 검증된 임계의 짧은작업명 불안정을 0의존성·VRAM0로. 단 "교체 아닌 AND 추가", canon 불변. |
| **파싱** | ③ Structured Output | 파이프붕괴 실패를 구조적으로 제거. VRAM0, 코드 얹힘 완벽. 단 파이프파서 유지 + 폴백 + 골든셋 선행. |
| **신뢰성** | ① 회귀 골든셋 | 유일하게 무위험, 다른 모든 변경의 선행 안전망. |
| **도메인** | ② 다이제스트 DM | 최고 ROI, LLM0, 프라이버시 무해, 업종 통증(마감·미수금·과부하) 정면. |

---

## 6. 1인 실행 순서 (권장)

```
1) ① 골든셋 eval          ← 반드시 먼저. 무위험. 이후 모든 변경의 증거.
2) ② 다이제스트 + ④ D-day 이모지 + ⑫ 의뢰자 저장   ← 코어 무접촉, LLM0, 시트 read-only.
3) ③ Structured Output    ← 파이프파서 남긴 채 analyze 일정 경로만 A/B → 통과 후 4개 호출부.
4) ⑤ 프롬프트 캐시         ← catchup/backfill 병목 확인 후.
5) ⑥ 미수금 나이 → ⑨ 감사로그 → ⑦ 자모백스톱(AND) → ⑧ 별칭 + ⑩ 능동학습
```

핵심 원칙 3가지:
1. **검증된 불변식을 교체하지 말고 추가하라** — `canon` 1단 매칭, `_core_sim_ok` 임계, replay 재현성, `allow_missing=False`(2026-07-03 결정)는 건드리는 순간 조용히 깨진다.
2. **새 모델을 상주시키지 마라** — 8GB는 exaone 하나로 이미 빠듯. VRAM0 개선이 지배전략.
3. **자동 상태변경은 금지, 확인필요 회송까지만** — 사람의 명시 승인 게이트를 남기는 게 이 봇 철학이자 사용자 결정.

---

## 부록: 검증된 SOTA 사실 (근거)

- **Ollama Structured Output**: `format`에 JSON schema → llama.cpp가 GBNF 문법 자동생성으로 토큰 제약. `/api/generate`·`/api/chat` raw HTTP 지원(v0.5.0+). required/enum/array 지원. **로컬 전용**(Cloud 미지원). 함정: 응답 전체 재검증 안 함(중간 끊김 시 깨진 JSON 가능 → 파싱 검증 필요). — [문서](https://docs.ollama.com/capabilities/structured-outputs) · [블로그](https://ollama.com/blog/structured-outputs)
- **Ollama logprobs**: v0.12.11+ native/OpenAI API 둘 다 지원. (본 봇은 토큰 특정 복잡도로 기각.)
- **임베딩 모델 크기**: bge-m3 = 가중치 1.2GB(MIT, 567M, 1024dim)이나 **실제 상주 ~1.8~2.2GB**(활성화 포함). exaone 여유 2.8GB에 GPU 공존 시 keep_alive 파괴 위험 → CPU 상주만 가능. nomic-embed ~0.7GB. — [bge-m3](https://huggingface.co/BAAI/bge-m3)
- **GLiNER 한국어**: taeminlee/gliner_ko konne dev F1 75.99%(P72.51/R79.82), 15개 엔티티, CPU 최적, **CC-BY-NC**. — [모델](https://huggingface.co/taeminlee/gliner_ko)
- **EXAONE 4.0**: 32B(NC, 8GB 불가) / 1.2B(NC, 온디바이스용·정확도↓). exaone3.5:7.8b는 어휘 50% 한국어. — [GitHub](https://github.com/LG-AI-EXAONE/EXAONE-4.0)
- **Qwen3 4B/8B**: Apache 2.0(상업 자유), 구조화출력 강점, 한국어 어휘는 exaone 우위. 직접 한국어 추출 벤치 부재로 무근거 교체 지양.
- **rapidfuzz**: C++ 고속 퍼지매칭, MIT, VRAM0, Win/Linux 휠 존재. (⑦ 자모유사도와 동일 슬롯 경쟁 — 의존성0인 자모유사도 근소 우위, 둘 다 넣을 필요 없음.) — [PyPI](https://pypi.org/project/RapidFuzz/)

**전 후보 로컬 실행 = 프라이버시 제약 100% 통과. 외부 LLM API 후보는 애초에 없음.**

---

관련 파일:
- `D:\Study\DIscordBot\AutoLinkBot\linkbot\brain.py` — `ollama()` L44(format/캐시 삽입점), `canon()` L303(별칭·불변 유지), `_core_sim_ok` L385(자모 AND 게이트), `parse_deadline_lines` L242(파이프파서 유지), `SAME_TASK_PROMPT`(임베딩 부적합 실증)
- `D:\Study\DIscordBot\AutoLinkBot\linkbot\state.py` — `reg_apply_snapshot`(감사로그 INSERT·replay 재현성), `CANON_VERSION`/`reg_recanon`(재계산 트랩), 별칭·캐시 신규 테이블
- `D:\Study\DIscordBot\AutoLinkBot\linkbot\bot.py` — `allow_missing=False` L361(자동변경 금지 근거), 이모지 블록 L231(D-day), `resolve_loop`(능동학습 훅·다이제스트 패턴), 확인필요 등록/무시 2액션 L609(별칭 UI 신설 필요)
- `D:\Study\DIscordBot\AutoLinkBot\linkbot\sheet.py` — `fetch()` L23(탭 전체 rows 반환·다이제스트 재료, 확인 완료), `push` upsert/merge 계약
- `D:\Study\DIscordBot\AutoLinkBot\linkbot\test_brain.py` — 골든셋 확장 지점
