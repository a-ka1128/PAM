# =========================================================
# config.example.py  →  config.py 로 복사해서 채워 넣으세요.
# (config.py 는 .gitignore 처리되어 커밋되지 않습니다)
# =========================================================

# 디스코드 봇 토큰  (개발자 포털 > 내 앱 > Bot > Reset Token)
TOKEN = "여기에_봇_토큰_붙여넣기"

# 봇을 조작할 수 있는 사람(본인)의 디스코드 유저 ID
OWNER_ID = 343290913172226049

# (서버 ID는 하드코딩하지 않습니다 — 봇을 초대하면 자동으로 인식되고
#  명령어가 그 서버에 즉시 등록됩니다.)

# 수집에서 제외할 채널 ID 목록 (민감 채널 등)
EXCLUDED_CHANNEL_IDS = []

# 작성자/행위자 ID를 해시 처리할지 (True면 누가 썼는지 익명화, 패턴은 분석 가능)
HASH_AUTHORS = False

# 구조 스냅샷(채널/역할/인원) 자동 저장 주기(시간). 0이면 시작 시 1회만.
SNAPSHOT_INTERVAL_HOURS = 12

# /백필 에서 일수를 생략했을 때 기본 일수
DEFAULT_BACKFILL_DAYS = 14
